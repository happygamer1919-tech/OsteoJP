# QA Pass — 2026-06-02

**Tester:** Max (sm33xy) via Claude in Chrome  
**Environment:** Production — `osteojp-platform.vercel.app`  
**Tenant:** `3a2d0711-fbdb-4ce9-b940-b6a87e3d3560`  
**Role tested:** owner (triboimax635@gmail.com)  
**Commit at time of test:** `2efb380` (main, post PR #79)  
**Previous QA pass:** `docs/qa-pass-2026-06-01.md`

---

## Summary

All three critical blockers from the previous pass (BUG-04, BUG-05, BUG-06) are confirmed fixed. One new critical blocker found: form templates not seeded into production database. One regression found: BUG-08 (date locale) not applied in production despite PR #69 being merged.

| Severity | Count |
|---|---|
| 🔴 Critical | 1 (new) |
| 🟡 Medium | 1 (regression) |
| 🟢 Low | 2 (new) |
| ✅ Passing routes | 9 |

---

## ✅ Passing

| Route | Notes |
|---|---|
| `/dashboard` | Loads correctly — Tenant ID, Função, Pacientes visíveis (51). Minimal info block — no charts (intentional for now). |
| `/agenda` | Week view loads. PT week header ✅ "1 a 5 de junho de 2026". Day columns: "Segunda 1, Terça 2..." ✅. Therapist + location filters present. |
| `/agenda → + Nova Marcação` | Modal opens with all fields: Utente, Serviço, Terapeuta, Sala, Localização, Data, Hora, Duração, Repetição, Estado, Notas. ✅ |
| `/patients` | 50 patients listed alphabetically with NIF + phone. Search bar present. "Novo Utente" button navigates to `/patients/new`. |
| `/patients/new` | All fields present in PT. "Criar Utente" and "Cancelar" buttons present. (Date bug — see BUG-08 regression below.) |
| Patient profile → `+ Novo Episódio` | ✅ **FIXED (was BUG-05)** — Creates episode immediately, navigates to `/clinical/episodes/[uuid]`. Episode shows date, status "Aberto", therapist name, and `+ Nova ficha neste episódio` button. |
| `/clinical` | Table renders with correct columns. "Sem fichas clínicas." message as expected. "Nova Ficha" button navigates to `/clinical/new`. ✅ **FIXED (was BUG-06)** |
| `/admin/services` | All 7 services listed (1ª Avaliação, Fisioterapia, Massagem Terapêutica, NESA, Osteopatia, Pilates Terapêutico, RPG). "Preços por localização" collapsible present per service. |
| `/admin/settings` | Nome = OsteoJP, NIF = 501200427, Telefone = 272339903, Morada = Rua Fernando Namora 6, Castelo Branco. All pre-filled correctly. |
| `/r/testtoken` | ✅ Safe branded error page — "Ligação inválida ou expirada" with patient-friendly message. No crash, no stack trace, no data leak. |

---

## 🐛 Bug Log

### BUG-11 — "Modelo" dropdown empty on `/clinical/new`
**Severity:** 🔴 Critical  
**Route:** `/clinical/new`  
**Description:** The "Modelo *" (template) dropdown has zero options. Clinical records cannot be created — selecting a template is a required step before the form can be submitted.  
**Root cause:** Form templates exist on disk (`packages/db/seed/form-templates/`) and the seed script exists (`packages/db/seed/form-templates.ts`, runnable via `pnpm --filter @osteojp/db run seed:form-templates`) but have never been run against production Supabase.  
**Fix required (Ivan):** Run the seed script against production with service-role credentials:
```bash
SEED_TENANT_ID=3a2d0711-fbdb-4ce9-b940-b6a87e3d3560 \
DATABASE_URL=<supabase-service-role-url> \
pnpm --filter @osteojp/db run seed:form-templates
```
**Blocker for:** Loom demo (Step 3), full clinical workflow end-to-end test.

---

### BUG-08 — Date format regression (mm/dd/yyyy still showing)
**Severity:** 🟡 Medium  
**Route:** `/patients/new`  
**Description:** The "Data de nascimento" input still shows US date format (mm/dd/yyyy). This was fixed in PR #69 (`lang="pt-PT"` added to the input) and marked resolved in the previous QA pass. It appears the fix is not taking effect in production — possible build cache issue or the fix only applies to the edit form, not the create form.  
**Expected:** dd/mm/yyyy (PT locale)  
**Action:** Ivan to verify the fix is applied to both `/patients/new` (create) and `/patients/[id]/edit` (edit) forms.

---

### BUG-12 — Patient count mismatch: list shows 50, dashboard shows 51
**Severity:** 🟢 Low  
**Route:** `/patients` vs `/dashboard`  
**Description:** The patients list shows "50 resultado(s)" while `/dashboard` shows "Pacientes visíveis: 51". One patient is visible to the tenant but not appearing in the list. Possibly a soft-deleted patient, a merge survivor, or an RLS edge case.  
**Action:** Ivan to investigate — likely the "Teste QA Paciente" seed record or the Ana Ferreira duplicate marker.

---

### BUG-13 — Clinic email empty in `/admin/settings`
**Severity:** 🟢 Low  
**Route:** `/admin/settings`  
**Description:** "Email de contacto" field is blank. Not a code bug — this is a pending input from owner (João Pedro).  
**Action:** Chase owner for clinic email address. No code change needed.

---

## Previously Critical — Now Confirmed Fixed ✅

| Bug | Fix PR | Confirmed |
|---|---|---|
| BUG-04 — No navigation | #73 | ✅ Sidebar present on all routes |
| BUG-05 — "+ Novo Episódio" does nothing | #72 | ✅ Creates episode and navigates |
| BUG-06 — "Nova Ficha" does nothing | #71 | ✅ Navigates to `/clinical/new` |
| BUG-07 — Sex field plain text | #69 | ✅ Dropdown confirmed working |
| BUG-10 — Brand token inconsistency | #70 + #76 | ✅ Not observed in this pass |

---

## Observations (Not Bugs)

- **Ana Ferreira duplicate flag** — Patient profile shows "Este utente é marcado como duplicado". This is seed data artifact from PR #52, not a code bug.
- **All service prices €0.00** — Intentional. Per-location pricing not yet configured by owner.
- **Agenda grid empty** — Expected for test account with no real appointments.
- **Dashboard minimal** — Info block only (Tenant, Função, Pacientes visíveis). No charts or summary cards. Intentional for Phase 3; Phase 4 candidate.

---

## Next Steps

1. **Ivan:** Run form templates seed against production (BUG-11) — blocks demo
2. **Ivan:** Verify BUG-08 date locale regression on create form
3. **Ivan:** Investigate BUG-12 patient count mismatch
4. **Max → João Pedro:** Chase clinic email for `/admin/settings` (BUG-13)
5. **Max:** Once BUG-11 fixed — re-test full clinical workflow and record Loom demo
