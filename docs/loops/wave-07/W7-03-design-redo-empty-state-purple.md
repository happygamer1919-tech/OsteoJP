# Loop W7-03 - Design redo: empty-state ornament removal + patient tabs + purple equity (Wave 07 Correcoes QA)

GATE: **Wave 07 Correcoes QA, DESIGN PASS, runs LAST, OWNER-MERGE ONLY (owner visual gate).** The owner REJECTED the W6-06 output on the deployed app: Registos clinicos and Documentos tabs judged worse than before, purple not perceptible, the unwanted line still present. This loop redoes the design. **GREEN MUST invoke `/ui-ux-pro-max` and paste invocation evidence.** **Audit phase first** (written per-surface plan committed in the loop branch), implementation second, then the **OWNER VISUAL GATE** (all checks green is necessary but NOT sufficient; the owner reviews the preview and merges). **Migration-free** (presentation only). Starts from **fresh `origin/main`**; never stacked.

## Field 1. Scope and ground truth

Redo the Wave 06 design work the owner rejected. Owner verdict on the deployed app (2026-07-14): W6-06 (#577/#578) output REJECTED - Registos clinicos and Documentos tabs are worse than before, the logo purple is not perceptible, and the unwanted line is still present. This loop delivers the mandatory removal, a proper tab redesign, a contained destructive-actions card, and a perceptible-but-tasteful purple equity, gated on the owner's visual sign-off.

Ground truth (recon at authoring 2026-07-14, embed - executor runs with ZERO memory):
- **`/ui-ux-pro-max` is MANDATORY** and its invocation evidence must be pasted (owner mandate). Drive the audit and implementation with it; do not improvise.
- **The "unwanted line" is the empty-state ornament, NOT the W6-06b danger card.** W6-06b (#578) reworked `PatientActions` into a bordered "Acoes destrutivas" card, but the line the owner still sees is the **decorative azulejo motif band above the empty-state icon**: `HeritageBand` (`packages/ui/src/components/HeritageBand.tsx`, a space-12 motif band recolored to `accent-2-200`), rendered by `EmptyState` (`packages/ui/src/components/EmptyState.tsx` line ~54) whenever the `heritage` prop is set. It renders today in **Registos clinicos** "Sem registos clinicos" (`apps/web/app/patients/[id]/page.tsx` ~line 329, `<EmptyState ... heritage />`) and **Documentos** "Sem documentos" (`apps/web/app/patients/[id]/PatientDocuments.tsx` ~line 147, `heritage`), and elsewhere the `heritage` prop is used (e.g. `apps/web/app/patients/page.tsx` first-run empty state).
- **MANDATORY REMOVAL:** remove that horizontal decorative squiggle/scallop ornament (`HeritageBand`) from the empty-state **platform-wide wherever that component renders**. Empty states become clean: **icon, title, subtitle, primary action only** - nothing above the icon. Recon every `EmptyState heritage` call site and the `EmptyState` component itself; the cleanest seam (drop the `heritage` band render from `EmptyState`, or remove the `heritage` prop at all call sites) is a `/ui-ux-pro-max`-driven decision, but the END STATE is: no motif band above any empty-state icon anywhere it renders.
- **Registos clinicos + Documentos tabs redesign:** give both proper layout and hierarchy. Tabs render via `apps/web/app/patients/[id]/profile-tabs.tsx` (`ProfileTabs`) over `packages/ui/src/components/Tabs.tsx`; the Documentos body is `apps/web/app/patients/[id]/PatientDocuments.tsx`; the Registos body is in `apps/web/app/patients/[id]/page.tsx`. Empty-state strings: `patients.emptyRecordsTitle` = "Sem registos clinicos", `patients.emptyRecordsHelp`, `patients.emptyDocumentsTitle` = "Sem documentos".
- **Acoes destrutivas card:** `apps/web/app/patients/_components/patient-actions.tsx` (rendered in `apps/web/app/patients/[id]/page.tsx`). Restyle and **contain** it so it does not sit as a raw block at the bottom of every tab. **ANY proposal to relocate destructive actions to a different tab or section is a PRODUCT question** - record it in the audit for owner ruling; **never self-decide** the relocation. In-place restyle/containment is in scope; moving it is not, without an owner ruling.
- **Purple mandate (perceptibility):** apply the logo purple (accent-1 `#8B1863` + its existing scale tints) to **defined, visible roles**: the **active tab indicator**, **selected states**, **secondary accents**, and **Estatisticas chart accents**, toward the owner equity **55% white+grey / 25% cyan / 20% purple**. The owner judged W6-06b's purple imperceptible (it shifted only the patient-profile Tabs active underline to `accent-1-700`); this loop makes purple **perceptible where it belongs** across the named roles while keeping the equity ratio. Use existing accent-1 tints; **AA preserved** on every purple pairing (reuse/extend the W6-06a AA table; accent-1-700 `#8B1863`-scale meets AA on white, base cyan must not be used for text).
- **Estatisticas chart accents:** the W6-05 Estatisticas dashboard (`apps/web/app/` statistics route) chart(s) take purple accents per the equity (the MVP chart is a dependency-free hand-rolled SVG per Q-W6-05-1; no charting vendor added).
- **Bodychart markers untouched:** the W5-25/W5-28 nine-entry `fill-marker-*` / `stroke-marker-*` palette in `apps/web/app/globals.css` is NOT modified. The purple equity must not touch it.
- **Canonical hexes never change** (accent-1 `#8B1863`, accent-2 `#45B9A7`, grey `#98B2C2`); `packages/ui/src/tokens.test.ts` stays green. The equity is a token-USAGE shift, not a re-hex.
- **Migration-free, presentation only:** no schema, seed, or migration. Any ficha-touching change keeps W5-13 compat green. UI-STYLE.md updated where wording needs to reflect the redo.

**Scope:** (audit) a written per-surface plan (current state, change, rationale) committed in the loop branch, produced with `/ui-ux-pro-max`; (impl) remove the empty-state `HeritageBand` ornament platform-wide (clean icon/title/subtitle/action only); redesign the Registos clinicos + Documentos tabs (layout + hierarchy); restyle and contain the Acoes destrutivas card in place (relocation = product question in the audit, never self-decided); apply a perceptible purple equity to the active tab indicator, selected states, secondary accents, and Estatisticas chart accents toward 55/25/20; AA preserved; canonical hexes unchanged; bodychart markers untouched; UI-STYLE.md updated as needed. Then the OWNER VISUAL GATE. pt-PT i18n (both files) for any new copy.

## Field 2. Ordered steps
1. **A0 isolation guard:** fetch origin; `git worktree add ../osteojp-w7-03-design-redo origin/main -b osteojp-w7-03-design-redo`; assert toplevel + clean tree + HEAD == `origin/main` tip. HALT (Field 6) if any fails.
2. **Load `/ui-ux-pro-max`** and use it to drive both the audit and the implementation (mandatory; paste invocation evidence).
3. **AUDIT PHASE FIRST (committed doc):** produce a written per-surface plan - for each surface (empty states platform-wide, Registos tab, Documentos tab, Acoes destrutivas card, active tab indicator, selected states, secondary accents, Estatisticas chart) record **current state, the change, the rationale**. Include any **relocation-of-destructive-actions proposal as a PRODUCT question for owner ruling** (never self-decided). Commit this audit doc in the loop branch (e.g. `docs/loops/wave-07/W7-03-audit.md`) before implementation.
4. **Mandatory removal:** remove the empty-state `HeritageBand` ornament platform-wide; empty states become icon + title + subtitle + primary action only.
5. **Tabs redesign:** give Registos clinicos and Documentos proper layout + hierarchy per the audit.
6. **Destructive-actions card:** restyle and contain `patient-actions.tsx` in place so it is not a raw bottom-of-tab block; do not relocate without an owner ruling.
7. **Purple equity:** apply accent-1 purple to the active tab indicator, selected states, secondary accents, and Estatisticas chart accents toward 55/25/20; AA preserved; canonical hexes fixed; bodychart untouched. Update UI-STYLE.md wording as needed.
8. **Gates:** `pnpm lint`, `pnpm typecheck`, `pnpm test` (incl. `tokens.test.ts` canonical-hex guard, an empty-state-has-no-motif-band test, a purple-selected-state test, W5-13 compat for any ficha-touching change), `pnpm build`, `pnpm test:e2e` (empty states render clean with no band; the tabs read as redesigned; the destructive card is contained; purple is perceptible on the named roles; AA holds). JSON.parse both i18n files in the gate.
9. **OWNER VISUAL GATE (see merge policy):** with all required checks AND all three Vercel deploys green, **push the branch, paste the osteojp-platform Vercel PREVIEW URL plus the list of surfaces to inspect, and HALT.** GREEN does NOT self-merge. The owner reviews the preview and merges. A rejected preview loops back with owner notes.

## Field 3. Definition of done (machine-verifiable up to the owner gate)
- **Audit-doc PROOF:** the committed per-surface plan (current state / change / rationale) exists in the loop branch, including any destructive-actions relocation flagged as a product question. Paste its path + summary.
- **`/ui-ux-pro-max` used PROOF:** paste the invocation evidence (owner mandate).
- **Migration-free + canonical-hex PROOF:** `git diff --name-only origin/main` shows ZERO files under `packages/db/migrations/`, `supabase/migrations/`, `.github/workflows/`; `packages/ui/src/tokens.test.ts` passes unchanged. Paste both.
- **Ornament-removed PROOF:** no `HeritageBand` motif band renders above any empty-state icon anywhere (Registos, Documentos, patient list first-run, and every other `heritage` call site); empty states are icon/title/subtitle/action only. Paste before/after + the guard test.
- **Tabs-redesign PROOF:** Registos clinicos + Documentos read as properly laid-out with hierarchy (before/after). Paste it.
- **Destructive-card PROOF:** the Acoes destrutivas card is restyled and contained, not a raw bottom block; no relocation without an owner ruling (relocation, if proposed, is in the audit as a product question). Paste before/after.
- **Purple-equity PROOF:** purple is perceptible on the active tab indicator, selected states, secondary accents, and Estatisticas chart accents, toward 55/25/20; purple is accent-1 (no new hex). Paste it.
- **AA PROOF:** every purple/foreground pairing meets AA in situ. Paste the re-verified table.
- **Bodychart-untouched PROOF:** `git diff origin/main -- apps/web/app/globals.css` shows the `marker-*` palette unchanged; the bodychart renders identically. Paste it.
- **W5-13 compat GREEN** for any ficha-touching change. Paste it.
- **Suite counts** with all gates green.
- **OWNER GATE record:** the branch is pushed, all required checks + three Vercel deploys are green (read from the checks API), and the loop HALTED with the osteojp-platform PREVIEW URL + surface list for the owner. **No self-merge.**

## Field 4. Verification (paste evidence)
The committed audit doc, the `/ui-ux-pro-max` invocation evidence, the migration-free + canonical-hex diff, the ornament-removed before/after + guard test, the tabs-redesign before/after, the destructive-card before/after, the purple-equity proof, the AA table, the bodychart-untouched diff, passing W5-13 compat, suite counts, the green-checks record, and the PREVIEW URL + surface list handed to the owner.

## Field 5. Restrictions and scope boundary
- **A0 worktree isolation** off fresh `origin/main`. **`/ui-ux-pro-max` mandatory** (invocation evidence pasted). **Audit-doc-first;** no implementation commit before the audit plan is committed.
- **OWNER-MERGE ONLY (owner visual gate).** GREEN NEVER self-merges this loop, even with all checks green. Push, paste the preview URL + surfaces, HALT for the owner.
- **Empty-state ornament removed platform-wide;** empty states are icon/title/subtitle/action only, nothing above the icon.
- **Destructive-actions relocation is a PRODUCT question** - record it in the audit for the owner; never self-decide moving it to another tab/section. In-place restyle/containment only, without an owner ruling.
- **Presentation only.** No schema, seed, migration, or data change.
- **Canonical brand hexes never change** (`tokens.test.ts` stays green). Purple equity is a usage shift to accent-1 tints, not a re-hex.
- **Bodychart marker palette (W5-25/W5-28) is untouched** - no change to `apps/web/app/globals.css` `marker-*` tokens.
- **AA preserved** on every restyled surface, especially every purple pairing. Ficha-touching changes keep W5-13 compat green.
- pt-PT i18n (both files, keep-both on rebase, JSON.parse both in the gate); no emoji; UI-STYLE.md. **Never force-push / `--admin`.** Plain hyphens only. SYNTHETIC-DATA-ONLY for verify.

## Field 6. Halt loud if (halt file to `~/osteojp-mailbox/escalations` + osascript, then stop; product/scope to `docs/design/QUESTIONS.md` with a recommended default)
- The A0 guard fails (not toplevel, dirty tree, or HEAD != `origin/main` tip).
- Achieving a perceptible purple at AA on a surface would require changing a canonical hex or touching the bodychart palette - HALT with the pairing; use an existing accent-1 tint that meets AA, never re-hex a brand token or touch markers.
- Any surface needs the destructive actions MOVED (not just restyled) to read well - record it as a product question in the audit + QUESTIONS with a recommended default; do not self-relocate.
- A redesign fix cannot be done presentation-only (needs a data/schema change) - surface it; do not add a migration in this design loop.
- **The owner rejects the preview** - loop back with the owner's notes; do not merge, do not force a self-merge. Reaching all-checks-green is necessary but never sufficient here.

## Field 7. Report back
The committed audit doc, the `/ui-ux-pro-max` invocation evidence, the migration-free + canonical-hex diff, the ornament-removed + tabs-redesign + destructive-card before/afters, the purple-equity proof, the AA table, the bodychart-untouched diff, passing W5-13 compat, suite counts, the green-checks record, and the PREVIEW URL + surface list handed to the owner for the visual gate.

**Merge policy (Wave 07 Correcoes QA - W7-03 OWNER VISUAL GATE, new standing rule born from the W6-06 QA failure):** all required checks green (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deployment checks (osteojp-api, osteojp-platform, osteojp-portal) green, read from the checks API not the banner, is **NECESSARY but NOT sufficient**. GREEN pushes the branch, pastes the **osteojp-platform Vercel PREVIEW URL plus the list of surfaces to inspect**, and **HALTs**. The owner visually reviews the preview and **merges himself**. **GREEN NEVER self-merges W7-03.** A rejected preview loops back with the owner's notes. This loop is migration-free; a surfaced migration would additionally make it OWNER-MERGE with live-apply evidence (none expected). Workflow files are never touched. Never force-push / `--admin`; never merge on red required checks; never merge without the owner's visual sign-off.
