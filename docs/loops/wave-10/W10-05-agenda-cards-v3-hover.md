# Loop W10-05 - Agenda cards v3 + hover dashboard (Wave 10 Dados Reais e Isolamento)

GATE: **Wave 10 Dados Reais e Isolamento, migration-free, DISPLAY LAYER, OWNER VISUAL GATE.** Inverts the agenda card face so the PATIENT NAME is first and largest, and adds one unified rich hover popup on EVERY card that extends and REPLACES the W9-06 note-only hover. Migration-free, no data or axis change; reads only fields already on `AgendaAppointment`. Runs AFTER W10-04 merged and `origin/main` fast-forwarded. Starts from **fresh `origin/main`**; never stacked. **OWNER VISUAL GATE:** all checks green is NECESSARY but NOT sufficient; GREEN pushes + pastes the osteojp-platform PREVIEW URL + the agenda surface, then HALTs; the owner merges.

## Field 1. Scope and ground truth

Two display-layer changes on the agenda appointment card, in one pass:
- **(A) Card face hierarchy inverts.** The PATIENT NAME becomes the first and largest element on the card face. Therapist identity stays as the coloured left SPINE + the dot (W9-05); the service tint stays on the card BODY; strikethrough-cancelled and confirmation-tick semantics are unchanged; same-hour overlap rendering keeps names legible.
- **(B) One unified rich hover popup on EVERY card.** A structured hover/focus popover appears on every card (extends and REPLACES the W9-06 note-only hover - one unified component, not a second overlay): time and duration, patient name, service, therapist, location, lifecycle + confirmation state, note preview when present, and created-by + created-at (portal bookings show "Reserva online (portal)").

Ground truth (recon at authoring 2026-07-20, embed - executor verifies + extends against the current card, executor runs with ZERO memory; all file:line refs verified read-only at authoring):

- **Agenda card `AppointmentBlock` (`apps/web/app/agenda/agenda-grid.tsx:390-556`).** The positioned `group` wrapper (`:439-448`) holds the card button + any escaping popover (the button is `overflow-hidden`, so a popover must be a sibling INSIDE the wrapper). Current face order (`:456-537`): conflict label (`:465-470`), **time row** (`:471-494`, time at `:472-474`), **patient name** (`:495-518`, `text-sm font-medium`, currently the 2nd/3rd line, `data-testid="agenda-card-patient"`), **therapist row** (`:522-530`, colour dot `:527`), **service line** (`:531`, only when `rawHeight >= 96`). **Inversion target:** patient name becomes the first and largest element (promote above the time row, larger weight/size); therapist stays the spine (`:464`, `w-1.5`, `tColor.fill`) + dot; service tint stays on the body (`${tint}`, `:456`).
- **Therapist colour (`apps/web/lib/scheduling/therapist-color.ts`):** `therapistColor(id)` (`:65`) FNV-1a-hashes the therapist id into a 7-hue `-700` palette (`:32-40`); the card uses `tColor.fill` for the spine + dot. **Unchanged by this loop** (W9-05 lock; `tokens.test.ts` stays green; no new hex).
- **Lifecycle/confirmation visuals (unchanged):** cancelled = strikethrough on the patient name (`:497`, `cancelled` at `:408`); the confirmation tick (`ConfirmationIndicator`, `:492`) is SUPPRESSED when cancelled (W9-05 ruling, `:486-491`). `ConfirmationIndicator` (`apps/web/app/agenda/confirmation-indicator.tsx`) accepts `showLabel` and can be reused in the popup as text. The popup RESTATES lifecycle + confirmation as text; it does not change the axes or any data (the dual axes `status`/`confirmation_state` stay LOCKED, DECISIONS 2026-07-01).
- **The W9-06 hover to replace:** the agenda card has its OWN inline note popover (`agenda-grid.tsx:542-553`, `data-testid="agenda-card-note"`, `group-hover:block group-focus-within:block`, note-presence icon `:478-485`) - this is what the unified popup REPLACES on the agenda card. The shared `NoteHoverCard` component (`apps/web/app/agenda/note-hover-card.tsx`, note-only) is mounted on the MARCACOES row (`marcacoes-view.tsx:226`), a SEPARATE surface. **This loop's popup is for the AGENDA card;** whether to also migrate the marcacoes-row `NoteHoverCard` to the unified component is an explicit scope call (default: leave marcacoes on the existing note-only hover this wave; the unified popup ships on the agenda card).
- **All popup fields already exist on `AgendaAppointment` (`apps/web/lib/scheduling/types.ts:28-68`) - NO new query, NO migration:** `patientName` (`:31`), `practitionerName` (`:33`), `locationName` (`:41`), `serviceName` (`:43`), `startsAt`/`endsAt` (`:45-46`, duration = derived, there is no explicit duration field), `status` (`:47`), `notes` (`:48`) + `hasNote` (`:59`), `confirmationState` (`:54`) + `confirmationReceivedAt` (`:55`)/`confirmationChannel` (`:56`), `createdBy`/`createdByName` (`:65-66`, NULL = portal booking), `createdAt` (`:67`, row insert time, distinct from `startsAt`), plus secondary participants (`:37-39`).
- **i18n keys already exist** in `packages/i18n/src/strings.pt.json` (EN mirror at the same line numbers): `appointment.status.*` (`:960-964`), `appointment.confirmationPending/Confirmed/Declined` (`:248-250`), `appointment.confirmationReceivedAt` (`:251`), `appointment.createdBy` (`:252`), **`appointment.createdByPortal` = "Reserva online (portal)"** (`:253`), `appointment.noteHoverLabel` (`:255`), `appointment.hasNoteLabel` (`:256`). Add only what is genuinely new (e.g. a duration label, a "criado em" label) to BOTH files (JSON.parse both).
- **Reference (do NOT copy literally):** the clinic's Fisiozero shows a plain black tooltip (`time - patient`, `service | therapist`, `clinic`). Ours must be a clearly STRUCTURED card following `.claude/skills/osteojp-design` (55/25/20 equity, AA, colour-not-only), not a plain black tooltip.
- **Staff-only, portal never gains this popup:** the card, `therapist-color`, and the note hover are all `apps/web` (staff); `apps/portal` imports NONE of them and its `AppointmentView` carries no notes/createdBy/confirmationState (recon confirmed) - there is no portal-leak risk, and the portal must NOT gain this popup.
- **Standing (post W10-02):** E2E on **local `127.0.0.1` Supabase with SYNTHETIC data ONLY**; the cloud is real-data-only; disposable test patients; never Maria Joao Silva (retired); NO cloud QA data.

**Scope:** (A) the agenda card face renders the patient name first + largest, therapist as the spine + dot, service tint on the body, with strikethrough-cancelled + tick semantics + overlap legibility unchanged; (B) one unified rich hover/focus popup on every card (time+duration, patient, service, therapist, location, lifecycle+confirmation, note preview when present, created-by + created-at with the portal label), replacing the agenda card's inline note popover. Migration-free, display layer only; zero query/model/axis change. Every state cue has a non-colour form (the popup is text; colour is reinforcement).

## Field 2. Ordered steps
1. **A0 isolation guard:** fetch origin; assert `origin/main` contains W10-04's merge; `git worktree add ../osteojp-w10-05-agenda-cards-v3 origin/main -b osteojp-w10-05-agenda-cards-v3`; assert toplevel + clean tree + HEAD == tip. HALT (Field 6) if any fails.
2. **Invert the card face:** promote the patient name to the first + largest element; keep the therapist spine + dot and the service body tint; keep strikethrough-cancelled + the suppressed-when-cancelled tick; keep same-hour overlap legible (the W9-05 patient-name-always-rendered behaviour). No data/axis change.
3. **Build the unified popup:** one component rendering time+duration (derived from `startsAt`/`endsAt`), patient, service, therapist, location, lifecycle + confirmation (reusing `ConfirmationIndicator` with `showLabel` + the status labels), note preview when `hasNote`/`notes` present, and created-by + created-at (`createdByName ?? "Reserva online (portal)"`). Mount it as the escaping sibling inside the positioned `group` wrapper on EVERY card, on hover AND keyboard focus. **Replace** the inline note popover (`agenda-grid.tsx:542-553`). Follow `.claude/skills/osteojp-design` (structured card, AA, colour-not-only).
4. **i18n:** reuse the existing keys; add only genuinely new labels to BOTH `strings.pt.json` + `strings.en.json` (JSON.parse both). Correct Portuguese diacritics.
5. **Tests:** component (patient name is first + largest; therapist spine/dot + service tint intact; cancelled still strikes + drops the tick; overlap keeps names legible); the popup renders on every card with all fields, shows the portal label for a NULL `createdBy`, shows the note preview only when a note exists, and restates lifecycle+confirmation as text. **E2E** on local synthetic data: hover/focus a card -> the structured popup shows time/patient/service/therapist/location/state; a portal-booked card shows "Reserva online (portal)"; the portal app has NO such popup.
6. **Gates:** `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm test:e2e`. JSON.parse both i18n files. `tokens.test.ts` green (no new hex). Confirm `git diff --name-only origin/main` shows ZERO migration + ZERO workflow files.
7. **OWNER VISUAL GATE:** push; paste the osteojp-platform PREVIEW URL + the agenda surface steps (a day with same-hour overlaps, multiple therapists, a cancelled appointment, a portal-booked appointment, and a card with a note); HALT for the owner to inspect the card face + the hover popup before merging.

## Field 3. Definition of done (machine-verifiable)
- **Migration-free PROOF:** `git diff --name-only origin/main` shows ZERO files under `packages/db/migrations/`, `supabase/migrations/`, `.github/workflows/`. Paste it.
- **Card-face PROOF:** the patient name is the first + largest element; the therapist spine + dot and the service body tint are intact; strikethrough-cancelled + suppressed-when-cancelled tick + overlap legibility are unchanged. Paste the component test + a before/after description.
- **Unified-popup PROOF:** every card shows one structured popup with time+duration, patient, service, therapist, location, lifecycle+confirmation (as text), note preview when present, and created-by + created-at; a NULL `createdBy` renders "Reserva online (portal)". Paste the test. The inline note-only popover is REPLACED (one component, not two).
- **No-new-query / no-axis-change PROOF:** the popup reads only existing `AgendaAppointment` fields; `status`/`confirmation_state` axes and all data unchanged. Paste the citation.
- **Palette PROOF:** `tokens.test.ts` green (no new hex; therapist palette unchanged).
- **Portal-exclusion PROOF:** the popup lives in `apps/web` only; the portal has no such popup (cite the portal `AppointmentView` carrying no notes/createdBy).
- **OWNER VISUAL GATE PROOF:** the preview URL + agenda steps pasted; the loop HALTED for owner merge (NOT self-merged).
- **Suite counts** with all gates green.

## Field 4. Verification (paste evidence)
The migration-free diff, the card-face + unified-popup + no-axis-change + palette + portal-exclusion proofs, suite counts, the PREVIEW URL + agenda steps, PR number.

## Field 5. Restrictions and scope boundary
- **A0 worktree isolation** off fresh `origin/main` (contains W10-04). **Migration-free:** display layer only; if a schema change surfaces, HALT (do not add a migration here).
- **The dual axes are LOCKED.** strikethrough = cancelled and the tick semantics are UNCHANGED; the popup restates state as text; never merge `status`/`confirmation_state`, never change stored data.
- **Read only existing `AgendaAppointment` fields** - no new query, no model change. The therapist palette + `tokens.test.ts` are unchanged (no new hex).
- **One unified component** - the popup REPLACES the agenda card's inline note popover, it does not add a second overlay. Colour is never the only cue (the popup is text).
- **Staff-only.** The popup ships in `apps/web` only; the portal NEVER gains it. No note/provenance content reaches the patient portal.
- pt-PT i18n (both `packages/i18n/src/strings.pt.json` + `strings.en.json`, JSON.parse both); no emoji; plain hyphens only; no em/en dashes. `.claude/skills/osteojp-design` + UI-STYLE.md + 55/25/20 equity + AA. **Never force-push / `--admin`.**
- **Standing test-data rule (post W10-02):** E2E on local `127.0.0.1` Supabase with SYNTHETIC data ONLY; the cloud is real-data-only; disposable test patients; never Maria Joao Silva (retired); NO cloud QA data.

## Field 6. Halt loud if (halt file to `~/osteojp-mailbox/escalations` + osascript, then stop; product/scope to `docs/design/QUESTIONS.md` with a recommended default)
- The A0 guard fails, OR `origin/main` does NOT contain W10-04's merge.
- Inverting the card or building the popup cleanly would require a schema change, a new query/model field, or an axis change - HALT (display layer only; the axes are locked; the popup uses existing fields).
- A structured popup meeting AA cannot be produced without drifting a canonical hex - HALT to QUESTIONS with a recommended default (never drift a canonical hex).
- Any path would surface note/provenance content to the patient portal - HALT-LOUD (staff-only invariant).

## Field 7. Report back
The migration-free diff, the card-face + unified-popup + no-axis-change + palette + portal-exclusion proofs, suite counts, the PREVIEW URL + agenda steps, PR number.

## Merge policy (embed, Wave 10 Dados Reais e Isolamento)
- **W10-05 is the OWNER VISUAL GATE** (standing rule for visual-heavy loops since W7-03). All required checks (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deploys (osteojp-api, osteojp-platform, osteojp-portal) green (read from the checks API NOT the banner) is NECESSARY but NOT sufficient. GREEN pushes + pastes the osteojp-platform PREVIEW URL + the agenda surface steps, then HALTs; **the owner inspects the card face + the hover popup on the preview and merges.** GREEN NEVER self-merges.
- **Runs after W10-04 merged**, fresh `origin/main`, never stacked. Workflow files NEVER touched. JSON.parse both i18n files in every gate. HALT-LOUD on scope/product/reality mismatch; any portal note/provenance exposure escalates instantly.
