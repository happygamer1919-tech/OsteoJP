# Loop W11-01 - Recon + split plan (Wave 11 Separacao de Producao)

GATE: **Wave 11 Separacao de Producao, READ-ONLY RECON, INCLUDING AUTHORIZED CLOUD READS, NO DB WRITE, NO CODE, NO DASHBOARD CHANGE.** Inventories the current Supabase project `jaxmkwoxjcgzkwxgbayx` end to end (auth, RLS, storage, extensions, secrets by NAME only, schema snapshot at head `0037`, exact real-data footprint per table) and outputs a versioned `SPLIT PLAN v1` with a freeze-window runbook and a rollback story, so W11-02/03/04 execute ground truth rather than a guess. **No writes of any kind, no dashboard mutation, no secret VALUES printed.** Runs FIRST in the production-split chain. Starts from **fresh `origin/main`**; never stacked. **OWNER-MERGE** (docs; touches the production topology plan, so the owner reviews and merges).

## Field 1. Scope and ground truth

Produce a committed findings document `docs/recon/W11-01-split-plan.md` that (1) inventories the current Supabase project `jaxmkwoxjcgzkwxgbayx` (region Central EU / Frankfurt) across auth, RLS, storage, extensions, secrets-by-name, and the safety mechanisms; (2) captures the schema snapshot at migration head `0037` and confirms the `packages/db/migrations` <-> `supabase/migrations` mirror parity; (3) states the EXACT real-data footprint per table (counts only, no PII); and (4) ends in a versioned **SPLIT PLAN v1** with a freeze-window runbook and a rollback story. This is an audit: **read-only, no code, no migration, no DB write, no dashboard change.** Cloud reads ARE authorized for the inventory but only inside a `SET TRANSACTION READ ONLY` transaction for DB objects, counts only, no row PII printed, no credentials or secret VALUES printed, nothing written; dashboard settings are READ and transcribed (values that are secrets are recorded by NAME only, never their contents).

**Why the split (owner brief 2026-07-20 carried, W10-02 execution note, Q-W10-01-2):** the current project `jaxmkwoxjcgzkwxgbayx` was the dev/build project that became the live project; it still carries the owner-accepted BLOCKED residue island (4 soft-deleted patients / 5 signed + 21 draft clinical_records, immutability-pinned) plus whatever synthetic build history predates real usage. The W4-11 and Q-W10-01-2 rulings deferred the clean separation to "the prod-project split." Wave 11 performs it: stand up a NEW, clean Supabase project (Frankfurt, Pro) as the production system of record, migrate ONLY the real data, and repoint the three Vercel projects, leaving the old project frozen and untouched as the rollback.

Ground truth (recon at authoring 2026-07-21, embed as the STARTING map - the executor verifies + extends it against the live project + the current schema; the executor runs with ZERO memory). All `schema.ts` refs are `packages/db/src/schema.ts`; migration refs are under `packages/db/migrations/` (the `supabase/migrations/` copies must be an exact mirror):

- **Project identity + region:** current project `jaxmkwoxjcgzkwxgbayx`, Central EU (Frankfurt) - confirm region and plan tier. The NEW project MUST also be Frankfurt (`eu-central`), Vercel `fra1`, Resend EU - hard EU-residency rule (CLAUDE.md rule 8); a US-region resource for stored data is a HALT.
- **Migration head is `0037`** (`0037_service_packs`, journal `meta/_journal.json` last entry idx 37). Inventory MUST snapshot the schema AT head `0037` and confirm `packages/db/migrations/*.sql` (37 files, `0001`..`0037`) is byte-mirrored by `supabase/migrations/`, and that the `journal-sync` invariants hold (every `.sql` has a journal entry and vice versa). The new project's schema is stood up FROM these committed migrations in W11-02 - so the migrations, not a hand dump, are the schema source of truth.
- **The safety mechanisms that MUST be reproduced on the new project (all live in migrations, so a from-migrations apply reproduces them - CONFIRM each exists on the new project after W11-02):**
  - **RLS on every domain table** keyed on the JWT `tenant_id` claim (defense-in-depth; `0001_rls.sql` + per-table policies in later migrations). Enumerate EVERY policy (table, name, command, `USING`/`WITH CHECK` predicate) as the checklist W11-02 verifies.
  - **The clinical-records immutability trigger** `clinical_records_enforce_immutability` (`0001_rls.sql:232-255`, redefined `0005:56-91`) - `BEFORE UPDATE OR DELETE`, blocks `locked`/`signed` even for `service_role`. Must be present + enabled (`tgenabled='O'`) on the new project.
  - **The append-only `record_annulments`** relation (`0035`, SELECT + INSERT policies only, no UPDATE/DELETE) and the **`audit_log`** append-only writes.
  - **The Supabase Auth hook** that injects `tenant_id` + `user_role` into the JWT (the claim the whole RLS + `can`/`assertCan` model depends on). Record exactly how it is configured (the hook function + its registration in the Auth settings) - this is the single most easily-missed piece of a project split and its absence silently breaks isolation.
- **Auth settings to transcribe (values by name/shape, secrets by NAME only):** enabled providers (email/password, any OAuth), email templates (confirm/invite/recovery/magic-link) + their redirect/`SITE_URL` + additional redirect URLs, JWT expiry + the JWT secret (by name, NEVER the value), SMTP sender config (Resend EU) by name, the custom access-token / auth hook registration, and any rate-limit or security settings that differ from default.
- **Storage buckets:** enumerate every bucket (name, public/private - all clinical/patient buckets are PRIVATE, signed-URL only per CLAUDE.md), its size/object-count footprint (counts only), and its storage RLS policies. Attachments (`attachments`, `schema.ts:799-819`) reference storage objects - the object migration is part of W11-03, so record the bucket contents footprint here.
- **Extensions:** list installed Postgres extensions (e.g. `pgcrypto`/`uuid` for id generation, `pg_cron`/`pgmq`/`vault` if present) so W11-02 enables the same set BEFORE applying the schema (a missing extension breaks the migration apply).
- **Secrets by NAME only:** enumerate the env-var / project-secret / Vault entry NAMES the app and jobs depend on (`DATABASE_URL`, `DATABASE_URL_DIRECT`, the Supabase anon + service-role keys, `NEXT_PUBLIC_*`, the ingestion HMAC secret, Twilio/Resend/IfThenPay/InvoiceXpress/Stripe integration secrets) as the checklist W11-02/W11-04 rotate to the new project. **NEVER print a secret VALUE.**
- **The EXACT real-data footprint per table (counts only, no PII), the migration payload:** the retained REAL set from the W10-02 end state - `users` (19), `services` (25), `service_location_prices` (23), `service_packs` (14), `locations` (2, CB + LV), `tenants` (1), plus `tenants.settings` (incl. the delete-password secret hash) - AND the accepted BLOCKED residue island (`patients` 4 all soft-deleted, `clinical_records` 26 = 5 signed + 21 draft, `clinical_episodes` 3, `attachments` 5, `patient_note_revisions` 3, `appointments` 5). Re-count against the LIVE project (real usage has begun since W10-02, so counts will have grown) and state the CURRENT per-table footprint. **Decide + record, per table, whether the residue island travels to the new project or is left behind** (this is a plan decision the owner rules; see the SPLIT PLAN, and Q-W11-01-1 below).
- **Standing (post W10-02):** the cloud DB is REAL DATA ONLY; all QA/synthetic on local `127.0.0.1`. This recon reads the REAL project, so counts only, no PII, no secret values, no writes.

**Scope:** a committed `docs/recon/W11-01-split-plan.md` with (a) the project inventory (auth, RLS policy list, storage buckets, extensions, secrets-by-name, safety mechanisms incl. the auth hook), (b) the schema snapshot at head `0037` + mirror-parity confirmation, (c) the exact per-table real-data footprint (counts only), and (d) a versioned **SPLIT PLAN v1** = the W11-02 provisioning checklist + the W11-03 freeze-window data-migration runbook (with the exact expected per-table counts, the HALT-on-mismatch protocol, and the Preview-smoke-before-repoint step) + the W11-04 repoint checklist + the ROLLBACK story (old project untouched until the owner declares cutover final). Read-only; NO code, NO migration, NO DB write, NO dashboard change. The only writes are the findings doc under `docs/recon/` and the BACKLOG row flip on close.

## Field 2. Ordered steps
1. **A0 isolation guard:** fetch origin; assert `origin/main` is at the Wave 10 close incl. #618 (`75c56a1`, head `0037`); `git worktree add ../osteojp-w11-01-recon-split-plan origin/main -b osteojp-w11-01-recon-split-plan`; assert toplevel + clean tree + HEAD == tip. HALT (Field 6) if any fails.
2. **Schema snapshot + mirror parity (repo, read-only):** confirm the migration head is `0037`, `packages/db/migrations/` has `0001`..`0037` with a matching journal, and `supabase/migrations/` mirrors it; note any drift (a drift is a finding W11-02 must resolve before applying).
3. **Project inventory (authorized reads, no write, no dashboard change):** transcribe the auth settings (providers, templates, redirect/SITE_URL, JWT expiry, the auth hook registration, SMTP by name), enumerate every RLS policy (table/name/command/predicate), every storage bucket (name/visibility/footprint/policies), every installed extension, and every secret by NAME only. Confirm the immutability trigger is enabled and the append-only relations + audit_log are present. Print NO secret values, NO row PII.
4. **Real-data footprint (authorized reads, counts only):** inside `SET TRANSACTION READ ONLY`, count every table's CURRENT rows (real usage has grown since W10-02); state the retained-real set and the residue island separately. No PII.
5. **Write `docs/recon/W11-01-split-plan.md`** with sections (a)-(c) above, then a **SPLIT PLAN v1**:
   - **W11-02 provisioning checklist:** new project (Frankfurt, Pro), the exact extension set to enable FIRST, schema applied from the committed migrations via manual `drizzle-kit` apply (cwd `packages/db`, `DATABASE_URL_DIRECT` on 5432, journal verified to head `0037`), auth mirrored (providers/templates/redirects/JWT/the auth hook), storage buckets recreated (name/visibility/policies), secrets set by name, plus the Vercel project-setup checklist reminder (CLAUDE.md, owner-performed) and a post-apply verification that RLS + the immutability trigger + the auth-hook claim flow work (the `supabase-setup.md` RLS SQL test).
   - **W11-03 freeze-window runbook:** the exact ordered dump/restore of the REAL data, per-table EXPECTED before/after counts, a read-only pre-flight against those expected counts, the **HALT-on-mismatch protocol identical to W10-02** (any count/FK/trigger surprise HALTs with zero further writes), the storage-object migration, and the app + portal smoke against the NEW project via Preview envs BEFORE any Production repoint. The window opens ONLY on the owner phrase `AUTORIZO MIGRACAO plan vN` (W11-03). Include the mandatory CYAN checkpoints (owner invokes CYAN immediately before authorization and immediately after completion).
   - **W11-04 repoint checklist:** the exact Vercel env vars to swap across the 3 projects (Production `DATABASE_URL` pooler 6543, `DATABASE_URL_DIRECT` 5432, Supabase anon + service-role keys, any `NEXT_PUBLIC_*`), the redeploy order, the smoke, and the rollback = old project untouched until the owner declares cutover final.
   - **ROLLBACK story:** the old project `jaxmkwoxjcgzkwxgbayx` is READ-ONLY-then-FROZEN, never mutated or deleted during the split; rollback at any step is repointing the Vercel envs back. Retention: frozen [30] days after cutover, then the owner decides (W11-05).
   - **Version the plan `SPLIT PLAN v1`** so W11-03's authorization phrase can name the version.
6. **File open questions** with recommended defaults (e.g. Q-W11-01-1: does the BLOCKED residue island travel to the new project or stay behind on the frozen old project? Recommended default: LEAVE the island behind - a clean production project holds only live real data, and the frozen old project preserves the signed residue for any legal-retention need; migrating immutable signed synthetic records into the clean project would re-import exactly what the split exists to shed). Never self-decide a data-disposition question.
7. **Gates (docs-only):** `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` still green; confirm `git diff --name-only origin/main` shows ONLY files under `docs/`.

## Field 3. Definition of done (machine-verifiable)
- **No-write PROOF:** `git diff --name-only origin/main` shows ONLY `docs/` files (no code, migration, workflow, no DB write, no dashboard change). Paste it.
- **Inventory PROOF:** `docs/recon/W11-01-split-plan.md` exists with the auth settings, the full RLS policy list, the storage buckets, the extensions, the secrets-by-name list, and the safety mechanisms (immutability trigger enabled, append-only relations, audit_log, the auth hook). NO secret values, NO PII.
- **Schema PROOF:** the snapshot is at head `0037`; `packages/db/migrations` <-> `supabase/migrations` mirror parity + journal-sync stated (or the drift named).
- **Footprint PROOF:** the exact current per-table real-data counts (retained-real set + residue island stated separately), counts only.
- **SPLIT PLAN PROOF:** the doc ends in a versioned `SPLIT PLAN v1` with the W11-02 provisioning checklist, the W11-03 freeze-window runbook (expected per-table counts + HALT-on-mismatch + Preview-smoke-before-repoint + CYAN checkpoints), the W11-04 repoint checklist, and the rollback story.
- **Questions PROOF:** the residue-disposition question (and any other data-disposition point) is filed with a recommended default, never self-decided.
- **Gates green** (docs change does not break lint/typecheck/test/build).

## Field 4. Verification (paste evidence)
The no-write diff, the findings doc path + section headers, the inventory (auth/RLS/storage/extensions/secrets-by-name/safety), the schema-head + mirror-parity confirmation, the per-table footprint, the versioned SPLIT PLAN v1 + rollback story, the filed questions, gates green, PR number.

## Field 5. Restrictions and scope boundary
- **A0 worktree isolation** off fresh `origin/main` (Wave 10 close incl. #618, head `0037`). **READ-ONLY: no code, no migration, no schema change, no workflow, no DB WRITE, no dashboard MUTATION.** Cloud reads are authorized for the inventory but only inside `SET TRANSACTION READ ONLY` for DB objects, counts only, no row PII, no credential/secret VALUES.
- **Do NOT provision, migrate, or repoint anything** - this loop states the plan; W11-02 provisions, W11-03 migrates (owner-gated), W11-04 repoints. No new Supabase project, no Vercel change here.
- **Immutability + append-only + RLS + the auth hook are facts to MAP so they can be REPRODUCED, never to defeat.** The residue island's disposition is an owner question, never a force-delete.
- **EU residency is a hard rule:** the plan MUST specify Frankfurt for the new project; any US-region resource for stored data is a HALT-worthy finding, not a routine note.
- Plain hyphens only; no emoji; no em/en dashes; correct pt-PT diacritics in any quoted strings. **Never force-push / `--admin`.** READ-ONLY. No PII, no secret values.
- **Standing test-data rule (in force):** the cloud is real-data-only; this loop reads the REAL project for counts/footprint, writes nothing, and creates no synthetic data anywhere.

## Field 6. Halt loud if (halt file to `~/osteojp-mailbox/escalations` + osascript, then stop; product/scope to `docs/design/QUESTIONS.md` with a recommended default)
- The A0 guard fails, OR `origin/main` is not at the Wave 10 close incl. #618 (head `0037`).
- The migration mirror is drifted (`packages/db/migrations` != `supabase/migrations`, or a journal-sync invariant fails) - record it as the finding W11-02 must resolve before applying, and flag it prominently (a from-migrations apply of a drifted mirror stands up the WRONG schema).
- Producing a useful finding would require a DB WRITE, a dashboard mutation, provisioning, or printing a secret value / PII - HALT (this loop is strictly read-only; provisioning is W11-02, the migration is W11-03).
- The auth hook (tenant_id/role claim injection) cannot be located or its config cannot be read - HALT with the finding; without an exact reproduction spec the new project would silently break isolation.
- A live security/privacy exposure (a public clinical bucket, a missing RLS policy, a disabled immutability trigger, a leaked secret) is found during the reads - HALT-LOUD with the finding rather than folding it into the doc as a routine item.

## Field 7. Report back
The no-write diff, the findings doc path + section headers, the inventory, the schema-head + mirror-parity confirmation, the per-table footprint, the versioned SPLIT PLAN v1 + rollback story, the filed questions, gates green, PR number.

## Merge policy (embed, Wave 11 Separacao de Producao)
- **W11-01 is OWNER-MERGE (docs, read-only, no code, no migration, no DB write, no dashboard change).** Although it is docs-only, it defines the production-split topology + the freeze-window runbook, so the owner reviews and merges (it is the plan W11-02/03/04 execute). All required checks (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deploys green (read from the checks API NOT the banner) is NECESSARY. A live security/privacy exposure found during recon is an instant HALT-LOUD, not a merge.
- **Runs FIRST in the production-split chain**, fresh `origin/main`, never stacked. W11-02 does not start until W11-01 is merged AND the owner accepts SPLIT PLAN v1. Workflow files NEVER touched. Plain hyphens only. HALT-LOUD on scope/product/data/reality mismatch; any immutability-bypass or EU-residency violation escalates instantly.
