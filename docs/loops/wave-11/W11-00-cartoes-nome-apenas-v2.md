# Loop W11-00 - Cartoes nome apenas v2 (Wave 11 Separacao de Producao)

GATE: **Wave 11 Separacao de Producao, CODE loop, DISPLAY LAYER, migration-free, OWNER VISUAL GATE.** #618 (W10-05b) already stripped the agenda card FACE to the patient name only in the source, yet the owner reports production STILL renders the icon/time/therapist/service on the face at overlap widths. This loop is DIAGNOSIS-FIRST: prove what production is actually serving before touching code, then close the gap in whichever layer is at fault, and harden the tests to assert the ABSENCE of face detail (not just the presence of names). Runs FIRST in Wave 11 (it is the live-use blocker). Starts from **fresh `origin/main`** (contains #618); never stacked. **OWNER VISUAL GATE:** all checks green is NECESSARY but NOT sufficient; GREEN pushes + pastes the osteojp-platform PREVIEW URL + the 3-overlap steps in BOTH views, then HALTs; the owner merges.

## Field 1. Scope and ground truth

**Defect (owner, live use, 2026-07-21):** #618 was intended to make the agenda card face name-only, but in production the face still shows the icon/time/therapist/service rows, crowding the patient name at overlap widths - the exact symptom W10-05b set out to fix.

**The source is ALREADY name-only on `origin/main` (verified read-only at authoring, `origin/main` @ 75c56a1 = #618).** So the defect is NOT "the source still renders detail" - it is a gap between the source and what the owner sees. The diagnosis runs in a FIXED ORDER; do not skip to a code edit.

Ground truth (recon at authoring 2026-07-21, executor verifies + extends read-only; the executor runs with ZERO memory; every file:line verified against `origin/main` @ 75c56a1):

- **There is ONE agenda card renderer, `AppointmentBlock` (`apps/web/app/agenda/agenda-grid.tsx:388-486`).** Both the Dia and the Semana views route through the SAME `AgendaGrid` -> `AppointmentBlock`: `agenda-view.tsx:258` renders `<AgendaGrid>`, and the only Dia/Semana difference is the day span (`agenda-view.tsx:107`, `step = effectiveView === "week" ? 7 : 1`). **There is no separate week-view card component.** So a source sweep of `AppointmentBlock` covers BOTH views - confirm this is still true (grep for any second appointment-card renderer; if one exists, it is the culprit and MUST be swept too).
- **The current face (the `<button>`, `agenda-grid.tsx:433-472`) is name-only:** a decorative therapist spine (`:445`, `w-1.5`, `tColor.fill`, `aria-hidden`), then ONE element - `data-testid="agenda-card-patient"` (`:459-471`) - containing the therapist-colour DOT (`data-testid="agenda-card-therapist-dot"`, `:464-468`, `aria-hidden`) and the patient-name span (`:469`, `min-w-0 break-words`). **No time row, no service text, no therapist-name text, no note/confirmation/User/Repeat icons, no +1 or Sem-nota badge on the face.** The service tint is on the button body (`tint`, `:456`); `cancelled` strikes the name (`line-through`, `:461`); the `ring-warning` conflict ring is non-text (`:456`). Everything else lives in the W10-05 hover popup (`AppointmentHoverPanel`, sibling at `:479-483`, `data-testid="agenda-card-hover"`, `group-hover:block group-focus-within:block`). **If the executor re-reads this face and it is NOT name-only, the source regressed after 75c56a1 - fix it in `AppointmentBlock` and note the regression.**
- **Leading diagnosis A - a STALE DEPLOY.** The osteojp-platform Production deployment may be serving a build from BEFORE #618 (or before #616). Determine the exact commit Production is serving (Vercel dashboard / the deployment's committed SHA / a build-stamp) and compare it to 75c56a1 (#618). **If Production is behind #618, the face-detail is a stale-build artifact, not a code defect** - the fix is to get Production onto post-#618 `main` (a redeploy, owner/CI action), and this loop's deliverable is that finding plus the owner-gate proof on a CURRENT preview, with NO source change.
- **Leading diagnosis B - a STALE CLIENT BUNDLE (service worker / PWA cache).** The platform ships PWA/service-worker behaviour (PWA polish is a live backlog track). A service worker can serve a cached pre-#618 JS bundle to the owner's browser even after Production redeploys, so the owner sees the OLD face while a fresh incognito load shows the new one. Check for a registered service worker / precache on the agenda route; if a stale bundle is being served, the gap is client-cache, and the fix is the cache/versioning story (document it), not the card markup. Confirm whether an incognito/hard-reload load already shows the name-only face - if it does, this is diagnosis B.
- **Leading diagnosis C - an UNSWEPT SOURCE PATH.** Only if Production is confirmed at/after #618 AND a clean (uncached) load STILL shows face detail: there is a render path the sweep missed. Candidates to check read-only: a second card component, a mobile-specific Dia layout, a print/export view, or the `appointment-hover-card.tsx` self-render (`:155`). Sweep every agenda card render path (Dia AND Semana, including the `layoutOverlaps` 1/3-width branch, `agenda-grid.tsx:126-147`) and make the FACE name-only everywhere detail leaks.
- **The tests do NOT currently prove the face is name-only (this is the step-3 gap).** The unit test `agenda-grid.test.tsx` still carries W9-05-era PRESENCE assertions on the FULL render string - `expect(render(...)).toContain(THERAPIST_A.name)` (`:70`, `:94-95`) and `.toContain(CONFIRM_LABEL)` (`:102`) - which now pass ONLY because the therapist name + confirmation label live in the HOVER PANEL markup (a sibling of the button), NOT because they are on the face. The E2E `agenda-cards.spec.ts` asserts therapist-name + confirmation ABSENCE but only *within the card button scope* and only in **Dia** view (`:45-46`, `:74`, `:103` "Day view"), and the 3-overlap test asserts only name PRESENCE + wrapping (`:108-109`), never the ABSENCE of time/service/icon text on the face. **Absence-of-face-detail is never asserted, and Semana is never exercised at overlap** - so a regression that put detail back on the face would pass CI today.
- **The three `ttt` appointments on the cloud are owner-sanctioned residue for THIS visual gate ONLY.** The owner deletes them right after merge. This loop creates NO cloud QA data; all E2E is local `127.0.0.1` synthetic (post-W10-02 standing rule).

**Scope:** prove what production serves (diagnosis A/B/C in order); close the gap in the layer at fault (a redeploy/cache finding with NO source change if A or B; a source fix in `AppointmentBlock` and any second render path if C or a regression); and harden the tests so the FACE is asserted to contain ONLY the patient full name (wrapping, never truncating) and to be FREE of time/service/therapist-name/icon/badge text, in BOTH Dia and Semana at 3-overlap width. Keep the therapist stripe + dot + service tint + strikethrough-cancelled; the W10-05 hover popup (`AppointmentHoverPanel`) is UNTOUCHED and carries every detail that leaves the face. Migration-free, display layer, no data/axis/query change, no new i18n expected. The writes are (if C/regression) `agenda-grid.tsx`, the agenda tests in every case, the loop file, and the board row.

## Field 2. Ordered steps
1. **A0 isolation guard:** fetch origin; assert `origin/main` is at #618 (contains W10-05b `75c56a1`); `git worktree add ../osteojp-w11-00-cartoes-nome-apenas-v2 origin/main -b osteojp-w11-00-cartoes-nome-apenas-v2`; assert toplevel + clean tree + HEAD == tip. HALT (Field 6) if any fails.
2. **Re-read the source face (read-only):** open `AppointmentBlock` (`agenda-grid.tsx:388-486`) and confirm the `<button>` face is name-only (spine + dot + patient-name span; no time/service/therapist-name/icon/badge). Grep for any SECOND agenda-card renderer. If the face regressed OR a second renderer prints detail, that is diagnosis C/regression - record it and go to step 5.
3. **Diagnosis A (stale deploy):** determine the exact commit the osteojp-platform Production is serving; compare to 75c56a1 (#618). If Production is BEHIND #618, the face-detail is a stale build - record this finding as the primary diagnosis; the resolution is a redeploy to post-#618 `main` (owner/CI). Do NOT edit the card source for a stale-deploy cause.
4. **Diagnosis B (stale client bundle):** check for a service worker / PWA precache on the agenda route; confirm whether a clean incognito / hard-reload load already shows the name-only face. If a stale cached bundle is the cause, record the cache/versioning finding; the fix is the cache story (document; a source change to the card is NOT warranted for a cache cause).
5. **Diagnosis C / regression fix (ONLY if the source is at fault):** if Production is at/after #618 AND a clean load still shows face detail, sweep EVERY agenda card render path (Dia AND Semana, including the overlap 1/3-width branch) and make the face name-only everywhere; keep the spine + dot + service tint + strikethrough-cancelled; do NOT touch `AppointmentHoverPanel`, the Marcacoes view, `therapist-color.ts`, or i18n. Clean up any now-unused imports.
6. **Harden the tests (ALWAYS, regardless of A/B/C):** rewrite the FACE assertions to scope to the face element (the button / `agenda-card-patient`, EXCLUDING the hover sibling) and assert (a) the patient full name is present and wraps (`break-words`, not `truncate`), and (b) the face contains NO time text, NO service text, NO therapist-NAME text, and NO icon/badge - as ABSENCE assertions. Replace the stale unit presence assertions (`toContain(therapistName)` / `toContain(CONFIRM_LABEL)` that match the hover) with face-scoped checks. Extend the Playwright 3-overlap test to BOTH Dia AND Semana at 1/3 width, asserting name presence + wrapping AND the absence of face time/service/therapist/icon text; keep the proof screenshot per view.
7. **Gates:** `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm test:e2e`. `tokens.test` green (no new hex). Confirm `git diff --name-only origin/main` shows ZERO migration + ZERO workflow files and ZERO i18n changes (unless a genuinely new label is needed - then BOTH files, JSON.parse both).
8. **OWNER VISUAL GATE:** push; paste the osteojp-platform PREVIEW URL + the diagnosis (A/B/C with the Production SHA evidence) + the 3-overlap steps in BOTH Dia AND Semana (a day with 3 simultaneous appointments + a cancelled one) + the per-view Playwright screenshots; HALT for the owner to verify the face is name-only at overlap on the preview before merging. **The three `ttt` cloud appointments are owner-sanctioned residue for this gate only; the owner deletes them right after merge.**

## Field 3. Definition of done (machine-verifiable)
- **Diagnosis PROOF:** the loop states which of A (stale deploy), B (stale client bundle), or C (unswept source / regression) was the cause, with the Production SHA vs 75c56a1 evidence and the clean-load observation. If A or B, NO card-source change was made and the finding + the redeploy/cache resolution is recorded. If C/regression, the source fix is described.
- **Name-only PROOF (source, if touched):** the face renders ONLY the patient name (no time/service/therapist-name/icon/badge text); a face-scoped test asserts the name present + wrapping (not `truncate`) AND asserts the ABSENCE of time/service/therapist/icon text on the face. Paste it.
- **Both-views overlap PROOF:** a Playwright test books 3 overlapping appointments and, in BOTH Dia AND Semana at 1/3 width, asserts the names are readable (present, wrapped) AND the face carries no time/service/therapist/icon text; per-view screenshots pasted.
- **Kept-cues PROOF:** the therapist spine + dot, the service-tint body, and strikethrough-cancelled on the name are intact; `tokens.test` green (no new hex).
- **Hover-untouched PROOF:** `AppointmentHoverPanel` / `appointment-hover-card.tsx` and the Marcacoes view are unchanged (`git diff` shows neither touched); the hover still carries all detail.
- **No-i18n / migration-free PROOF:** `git diff --name-only origin/main` shows ZERO migration, ZERO workflow, ZERO i18n (unless a new label was genuinely required, then in BOTH files).
- **Suite counts** with all gates green. **OWNER VISUAL GATE:** the preview URL + both-view 3-overlap steps + the diagnosis pasted; HALTED for owner merge (NOT self-merged).

## Field 4. Verification (paste evidence)
The diagnosis (A/B/C + Production SHA evidence), the name-only + both-views-overlap + kept-cues + hover-untouched + no-i18n/migration-free proofs, the per-view Playwright screenshots, suite counts, the PREVIEW URL + both-view steps, PR number.

## Field 5. Restrictions and scope boundary
- **A0 worktree isolation** off fresh `origin/main` (contains W10-05b #618). **Agenda cards only, Dia + Semana**; Marcacoes list UNTOUCHED. Migration-free, display layer; if a schema/query/model change surfaces, HALT.
- **Diagnosis before code.** A stale-deploy (A) or stale-client-bundle (B) cause is a FINDING + a redeploy/cache resolution, NOT a card-source edit; do not "fix" name-only source that is already name-only. A source edit is warranted ONLY for diagnosis C or a proven regression.
- **Do NOT touch the W10-05 hover popup** (`AppointmentHoverPanel`) - it carries the detail that left the face. **Keep** the service-tint body, therapist stripe + dot + colour, strikethrough-cancelled. **No new hex** (`tokens.test` green). No new i18n string expected.
- The dual axes stay LOCKED; no data/query/model change. Staff-only; the portal is unaffected.
- pt-PT; no emoji; plain hyphens only; no em/en dashes. **Never force-push / `--admin`.**
- **Standing test-data rule (in force since W10-02):** E2E on local `127.0.0.1` synthetic data ONLY; the cloud is real-data-only. The three cloud `ttt` appointments are owner-sanctioned residue for the visual gate ONLY; this loop creates NO cloud QA data and the owner deletes them post-merge.

## Field 6. Halt loud if (halt file to `~/osteojp-mailbox/escalations` + osascript, then stop; product/scope to `docs/design/QUESTIONS.md` with a recommended default)
- The A0 guard fails, OR `origin/main` does NOT contain #618 (W10-05b `75c56a1`).
- The Production deployment SHA cannot be determined (dashboard access the owner holds) - HALT with the exact blocker so the owner reads it off Vercel; the redeploy is the owner's action.
- Making the face name-only cleanly would require a schema/query/model change, editing the hover popup, or a new i18n string beyond a simple label - HALT (display layer only; hover untouched).
- The name-only face cannot stay readable at 3-overlap without drifting a canonical hex - HALT to QUESTIONS with a recommended default (never drift a canonical hex).

## Field 7. Report back
The diagnosis (A/B/C + evidence), the name-only + both-views-overlap + kept-cues + hover-untouched proofs, the per-view screenshots, suite counts, the PREVIEW URL + both-view steps, PR number.

## Merge policy (embed, Wave 11 Separacao de Producao)
- **W11-00 is the OWNER VISUAL GATE.** All required checks (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deploys (osteojp-api, osteojp-platform, osteojp-portal) green (read from the checks API NOT the banner) is NECESSARY but NOT sufficient. GREEN pushes + pastes the preview URL + the diagnosis + the both-view 3-overlap steps + screenshots, then HALTs; the owner verifies the face is name-only at overlap and merges. GREEN NEVER self-merges.
- **Runs FIRST in Wave 11**, fresh `origin/main` (contains #618), never stacked. Migration-free, hover untouched, Marcacoes untouched, workflow files NEVER touched. HALT-LOUD on scope/product/reality mismatch. This loop is a UI blocker and is INDEPENDENT of the W11-01..05 production-split chain (it may run in parallel with W11-01 authoring/recon, but merges on its own visual gate).

## Field 8. Diagnosis outcome + execution record (GREEN, 2026-07-21)

**VERDICT: DIAGNOSIS A - STALE PRODUCTION DEPLOY. No source defect, no client cache. No card-source change made. Tests hardened (step 6, mandatory regardless of A/B/C).**

### A0 isolation (passed)
Worktree `../osteojp-w11-00-cartoes-nome-apenas-v2` off fresh `origin/main` @ `7efdb3f` (#619), which contains #618 `75c56a1` (`git merge-base --is-ancestor 75c56a1 origin/main` = true). Clean tree, HEAD == origin/main tip. (Local `main` was stale at `479cb47` on entry; `git fetch` reconciled before the guard.)

### Step 2 - source is already name-only (read-only, no regression)
`AppointmentBlock` (`apps/web/app/agenda/agenda-grid.tsx:434-471`) face `<button>` = therapist spine (`:449`, aria-hidden) + one `agenda-card-patient` span (`:458-470`) carrying the therapist-colour dot (`:464-468`, aria-hidden) + the patient-name span (`:469`, `min-w-0 break-words`). No time / service / therapist-name / icon / badge on the face. Grep confirms ONE renderer: `AgendaGrid` -> `AppointmentBlock`, both Dia + Semana via `agenda-view.tsx:258` (`step = week ? 7 : 1`); no second card-face component. Diagnosis C ruled out on `origin/main`.

### Step 3 - Diagnosis A PROVEN (the Production SHA, read off Vercel)
The deployment currently aliased to the Production domain `app.osteojp.pt` (osteojp-platform, `prj_sIUzsxnGm7xJ1EL3tkXMcr5zfIbm`):
- `dpl_8rjBarztYU2EUihWrxUbg1abGH2k`, `target: production`, `readyState: READY`, region `fra1`
- `githubCommitRef: main`, **`githubCommitSha: 479cb4729e53d96a2b6349c0b010fce3c934017a` = #617** (Wave 10 close-out)
- created `2026-07-21T15:48:39Z`; `alias` includes `app.osteojp.pt`, `osteojp-platform.vercel.app`

Timeline (Vercel deployment list, osteojp-platform):
- Every `main` merge from ~12:42Z through #617 `479cb47` @ **15:48Z** auto-deployed to Production.
- #618 `75c56a1` merged to `main` at **17:07:38Z** (PR #618, `gh pr view 618`). #619 `7efdb3f` merged at **18:30Z**.
- **After #617, Production auto-deploy STOPPED. `75c56a1` (#618) has ZERO deployments of any target/state; the newest Production-target deployment is `479cb47` (#617).**

Production is therefore serving the pre-#618 build (the W10-05 v3 detailed face). The owner's INCOGNITO re-test still showed detail because incognito removes only client cache (service worker / PWA) - it does NOT change what the Production SERVER serves, which is #617. Diagnosis B (stale client bundle) is thereby eliminated by the same incognito evidence; Diagnosis A is the cause.

### Resolution (OWNER action - the redeploy)
Get Production onto post-#618 `main`: redeploy / promote `osteojp-platform` Production to current `origin/main` (>= `75c56a1`). This is the owner's action (Field 6). NO card-source edit is warranted. See Q-W11-00-1 (QUESTIONS): auto-deploy stopped after #617, so a one-off redeploy fixes the symptom but the deploy pipeline itself needs the owner's check.

### Step 6 - tests hardened (mandatory, done)
The pre-W11-00 assertions passed only via the HOVER markup. Now FACE-SCOPED (the `<button>` carrying `agenda-card-patient`, excluding the `agenda-card-hover` sibling):
- **Unit** `agenda-grid.test.tsx`: `expectNameOnlyFace()` asserts the patient full name present + `break-words` (never `truncate`) AND the ABSENCE on the face of therapist-name / service / time / confirmation text, and that the face's ONLY testids are `agenda-card-patient` + `agenda-card-therapist-dot` (no icon/badge). Applied to single, cancelled, and 3-overlap renders. Stale `toContain(therapistName)` / `toContain(CONFIRM_LABEL)` full-string assertions replaced with face-scoped checks (therapist name asserted in the hover, absent from the face). **Negative control:** injecting a service row onto the face fails exactly these 3 tests; reverting returns green - the guard catches the regression #618 could not.
- **E2E** `agenda-cards.spec.ts`: `expectNameOnlyFace(card)` (button-scoped) asserts name present + dot kept + absence of therapist/service/time/confirmation on the face; the 3-overlap test now runs BOTH `view=day` AND `view=week` at 1/3 width with a per-view proof screenshot (`test-results/w11-00-3-overlap-{day,week}.png`).

### Gates (local, from repo root)
- `pnpm typecheck`: 9/9 successful.
- `pnpm lint`: 0 errors (7 pre-existing warnings, none in the two changed files).
- `pnpm test`: 1239 passed / 5 skipped / 1 todo (143 files); `agenda-grid.test.tsx` 14/14; `tokens.test` green (no new hex).
- `pnpm build`: `web:build` GREEN (the only app changed). `portal:build` fails LOCALLY on a missing `NEXT_PUBLIC_SUPABASE_URL` - pre-existing + change-independent: the portal has only `.env.example` on every checkout and builds only with Vercel-injected env in CI. Zero portal files touched. Authoritative build gate = the 3 Vercel deploys (checks API).
- `pnpm test:e2e`: NOT run locally (Docker daemon down -> no local `127.0.0.1` Supabase). Authoritative Playwright E2E = CI (per the Wave 11 merge policy, read from the checks API); it runs against local-synthetic data and produces the per-view screenshots. E2E was NOT run against the Preview (Preview uses the cloud DB = real-data-only; synthetic bookings there would violate the standing rule).

### Diff scope (migration-free, hover/Marcacoes untouched)
`git diff --name-only origin/main`: `apps/web/app/agenda/agenda-grid.test.tsx`, `apps/web/e2e/agenda-cards.spec.ts`, plus this loop file + board + DECISIONS + QUESTIONS docs. ZERO migration, ZERO workflow, ZERO i18n, ZERO `agenda-grid.tsx` (source unchanged - diagnosis A), ZERO `appointment-hover-card.tsx` / Marcacoes.
