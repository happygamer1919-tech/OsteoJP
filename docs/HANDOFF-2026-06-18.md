# Handoff: 2026-06-18

Coverage for the remainder of Ivan's absence. Max runs the autonomous build
loops; this document is the operating contract.

**Ground truth is the `gh` CLI and the live dashboards, not this document or
any session notes.** Where this handoff and the repo disagree, the repo wins.

---

## Session 11 accomplishments (2026-06-18)

All PRs below merged to `main` on 2026-06-18 unless noted.

| PR | Title |
|----|-------|
| #260 | fix(ingestion): log the real DB error on the 500 path |
| #261 | fix(web): exclude Inngest subpaths from session middleware |
| #263 | fix(portal): add `/auth/reset-password` page (closes #234) |
| #264 | docs: JP decision bundle 2026-06-17 |
| #265 | feat(web): wire M1 dashboard placeholders |
| #266 | chore: add Claude Code slash commands |
| #267 | docs: portal a11y audit 2026-06-17 |
| #271 | fix(portal): announce download error to screen readers (closes #268) |
| #272 | fix(portal): NavButton role + auth layout landmark (closes #269 #270) |
| #273 | feat(db): migration batch validation + edge case handlers |

**A11y audit summary:** Full source-code audit of `apps/portal/app/portal/` and
`apps/portal/app/auth/` (38 files). 1 P1, 2 P2, 5 P3 findings. All P1 and P2
findings are now fixed (PRs #271, #272). Report at
`docs/qa-a11y-portal-2026-06-17.md`. Remaining P3s are tracked below.

---

## Current state

- `main` is clean. Open PRs: #274 (migration reconciliation), #275 (migration
  health dashboard), #262 (Fisiozero raw archiver). All are M2/M3-tier work.
- **Phases 0–2 complete.** Full V2 glass design system live on all staff
  screens. `/marcacoes` live.
- **Phase 3 functionally complete** bar QA activation and the NESA epilepsy
  ruling.
- **Phase 4 code-complete**, gated on João Pedro (JP).
- **Phases 5–9 unstarted.**
- **M1 partially complete:** `/auth/reset-password` fixed (#263), dashboard
  placeholders wired (#265). Outstanding: Receita (mês), Resumo semanal, Notas
  rápidas persistence, Marcações query tuning, Portal i18n runtime, SPEC.md
  refresh.
- **M2 in progress:** migration-validation shipped (#273). Reconciliation
  (#274) and health-dashboard (#275) PRs are open.
- Required CI gates (**ci**, **db-tests**, **e2e**) all green on `main`.
- Branch protection has **0 required approvals**; **green CI is the merge gate**.

---

## Infrastructure state

| Resource | Value |
|---|---|
| Portal | `https://osteojp-portal.vercel.app` |
| Portal (go-live) | `patient.osteojp.pt` |
| API | `https://osteojp-api.vercel.app` |
| Staff platform | `https://osteojp-platform.vercel.app` |
| Staff platform (go-live) | `app.osteojp.pt` |
| Supabase prod ref | `jaxmkwoxjcgzkwxgbayx` |
| Supabase dev ref | `ufbkzbyghvxtosyrkgjq` |
| DB password | `OsteoJPProd2026!` |
| Next migration number | **0015** |

---

## Known regression

The staff dashboard (`apps/web`) is showing an error boundary on load in
production. Under investigation. Most likely cause: M1 dashboard placeholder
queries introduced in PR #265 execute against prod Supabase, and one or more
queries reference tables or columns that do not yet exist there. **Session 12
first task is to diagnose and fix this before any other work.**

---

## Open issues

- **#114** — Cross-tenant identity: `auth.users.email` is globally unique in
  Supabase. Single-tenant at launch; defer to the licensing phase.

**Open PRs (not yet merged):**
- **#276** — fix(portal): P3 a11y fixes — contrast, labels, live regions
- **#275** — feat(db): migration health dashboard queries (M2)
- **#274** — feat(db): migration reconciliation report generator (M2)
- **#262** — feat: Fisiozero Tier-1 raw archiver (tools/fisiozero-extractor)

---

## Pending JP decisions

Eight items bundled in `docs/jp-decisions-2026-06-17.md`. Ivan to forward to
JP verbatim. Summary:

1. **NESA intake form fields** — form cannot be finalised without JP's confirmed
   field list.
2. **Booking therapist preference** — add optional free-text preference field or
   leave it out.
3. **Late-cancellation / no-show fee** — amount and cut-off window needed for
   cancellation screen and notifications.
4. **Patient documents at launch** — which types; whether signed clinical record
   is a prerequisite.
5. **Montemor-o-Novo in booking** — "Coming soon" label or hidden entirely.
6. **NESA epilepsy** — absolute or relative contraindication (blocks the
   contraindications logic).
7. **Phase 4 invoicing** — VAT rate (23%?), protocol-discount rules,
   protocol-label copy. All three needed before invoicing goes live.
8. **Brand logo vector file** — SVG/EPS/AI to verify the exact brand colours.

---

## Remaining a11y P3s (post-audit, non-blocking)

From `docs/qa-a11y-portal-2026-06-17.md`. No gate impact; fix before go-live.

| # | File | Issue |
|---|---|---|
| 4 | BookingFlow.tsx:132,150 + AppointmentsView.tsx:43 + AccountView.tsx:32,147 | ChevronRight `text-text-muted` fails 3:1 graphical threshold (SC 1.4.11) — change to `text-text-secondary` |
| 5 | appointments/[id]/page.tsx:44 | "Voltar" back link missing `aria-label="Voltar para marcações"` |
| 6 | appointments/[id]/AppointmentActions.tsx:62 | `disabled` removes cancel button from tab order — use `aria-disabled` |
| 7 | auth/activate/page.tsx:124 | Password requirement list missing `aria-live="polite"` |
| 8 | components/layout/PortalChrome.tsx:57 | Skip link missing `focus-visible:ring-2 ring-focus-ring ring-offset-2` |

---

## Operating model while Ivan is away

- Max runs the autonomous build loops. **Green CI (3 required checks) is the
  merge gate**; self-merge on green for allowlist work.
- **Any PR touching `db-tests.yml` or `e2e.yml` is an automatic HOLD** for
  line-by-line review. Do not merge those without Ivan.
- **One migration in flight at a time**, sequential numbering. **Next free
  number is `0015`.**
- **Worktree isolation is mandatory** for parallel terminals.
- Ground truth is the `gh` CLI and dashboards, not notes.
- **Do not weaken branch protection.** There is no real patient data yet, so CI
  is the only backstop.

---

## M1: Remaining polish and functional completeness
*(Max, autonomous)*

- **Receita (mês):** revenue aggregation from issued invoices for the month.
- **Resumo semanal:** weekly appointment counts.
- **Notas rápidas:** persistence, scoped per-staff for v1.
- **Marcações query:** confirm the bounded date-window fetch, tune the window if
  needed (filters stay client-side for v1).
- **Portal i18n runtime** with pt-PT default; visible language switcher deferred.
- **Refresh `docs/SPEC.md`:** stale since 2026-06-10, predates the V2 system.
- **A11y P3s:** fix the five remaining nits above before go-live.

---

## M2: Migration logic, Wave 2
*(Max, 3 parallel worktrees, no migration)*

- **migration-validation** — shipped (PR #273).
- **migration-reconciliation** — PR #274 open; review and merge on green CI.
- **migration-health-content** — PR #275 open; review and merge on green CI.

---

## M3: CI hardening
*(Max may draft, **HOLD for Ivan, do not merge**)*

Both items touch load-bearing gates or a migration, so they are **drafts only**,
held for Ivan's line-by-line review on return.

- **Pin `supabase/setup-cli`** to a known-good version in `db-tests.yml` and
  `e2e.yml` (both currently float on `latest`).
- **Relocate `service_role` grants** from `supabase/seed.sql` into migration
  `0015`.

**Exception:** if a CLI release turns a required gate red mid-week, pin to
last-known-good as a hotfix with Ivan's async OK.

---

## Blocked on JP

- **Phase 4 invoicing:** VAT 23% sign-off, protocol-discount and
  protocol-label rules. See item 7 in the JP bundle above.
- **NESA epilepsy ruling:** absolute vs relative (item 6).
- **Q5, migrated clinical records:** `draft` vs `locked`, plus a `migrated`
  source tag. Recommendation on file: **`locked`, keep `manual` + ledger**.
- **Q6a:** vector logo SVG (item 8).

---

## Blocked on Andrei

- **Field spec:** gates the Stream D per-field mapping. Ingestion is proven and
  live; the payload is stored verbatim until the spec arrives.

---

## Ivan-only, paused until return
*(not Max's)*

- **M3 review and merge** — migration 0015 (service_role grants) and
  `supabase/setup-cli` pin in `db-tests.yml` / `e2e.yml`.
- **Vercel Pro** plus ownership transfer to A&I Automation.
- **Supabase Pro** before any real patient data lands.
- **Rotate `AI_INGESTION_HMAC_SECRET`** (exposed during the live test).
- **Rotate `DATABASE_URL_DIRECT`**, then add the `PROD_DATABASE_URL_DIRECT`
  secret.
- **DNS** for `app.osteojp.pt` and `patient.osteojp.pt`, plus Resend records;
  resolve the `api.osteojp.pt` host conflict.
- **Flip `TWILIO_SENDER_ID`** to `OsteoJP`.
- **Re-enable PR approval requirement** before real patient data lands.

---

## Parked / V1.1: not launch-blocking

- **Issue #114:** cross-tenant email uniqueness (single-tenant at launch;
  licensing-phase decision).
- In-portal form-filling engine (V1.1).
- Booking data-layer for therapist / category / notes, and portal
  documents / invoices (V1.1 / Phase 4).

---

## Session 12 first task

1. **Diagnose and fix the dashboard regression** — identify which query from PR
   #265 is erroring against prod and patch it (guard missing columns or defer
   the query until the relevant migration runs).
2. **Verify `<main>` landmark deployed on auth pages** — confirm the landmark
   fix from PR #272 is live on `https://osteojp-portal.vercel.app/auth/login`
   and `/auth/register`.
3. **Run `/e2e-portal`** for full portal confirmation.

---

## Go-live sequence
*(locked, for when gates clear)*

1. Supabase Pro on.
2. Backup and restore drill.
3. Fisiozero final extraction.
4. Import to prod.
5. Go live.

**Supabase Pro precedes the cutover extraction.**
