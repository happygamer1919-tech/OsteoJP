## Wave 01 Loop Queue

> Board ownership: this file is the single source of truth for live loop status and sequencing (DRAFT / READY / IN-FLIGHT / DONE / HALTED). WAVE-01.md describes wave scope, STATE.md holds ground truth, DECISIONS.md holds locked calls, LOOP-DISPATCH.md holds the dispatch mechanism. For what is runnable now, read this file only.

### Coordination protocol
Status flow: DRAFT (stub, no committed loop file) -> WRITTEN (loop file authored and committed under docs/loops/, awaiting gate/dispatch) -> READY (gate cleared, dispatchable) -> IN-FLIGHT (terminal running) -> DONE (PR merged to main, verified) -> or HALTED (briefing-vs-reality mismatch, see QUESTIONS.md).
Rules:
- One MIG-lane loop IN-FLIGHT at a time, sequential numbering. UI-lane loops run one at a time per executor.
- On marking a loop DONE: append to DECISIONS.md if a decision was locked, update STATE.md if ground truth changed, then re-scan DRAFT rows and flip any whose gate is now cleared to READY.
- Anything touching db-tests.yml or e2e.yml is an automatic hold for Ivan, never self-merged.
- gh CLI is ground truth when indexed history is stale.

### Max pickup rule
When you return from other work, do not start mid-queue. Read this manifest top to bottom. Run the top UI-lane row that is READY. Mark it IN-FLIGHT before you start. Mark it DONE on green CI and merge. Then proceed to the next READY UI-lane row. Do not touch any MIG-lane row, those are Ivan's. Do not self-merge anything touching db-tests.yml or e2e.yml.

### MIG lane (Ivan)
| ID | Loop | Status | Gate |
|----|------|--------|------|
| 0022 | patient columns | DONE | none |
| 0023 | therapist-service mapping | DONE | 0022 DONE |
| 0024 | appointment confirmation state | DONE | 0023 DONE |
| 0025 | event schema (SPEC-events, incl appointment_status_changed) | DONE | 0024 DONE |
| 0026 | appointment lifecycle, gated completion + per-visit notes | DONE (#415) | 0025 DONE (JP ruling: soft warning, DECISIONS 2026-07-01) |
| 0027 | multi-therapist booking | DONE (#416) | 0026 DONE |
| 0028 | batch scheduling engine | DONE (#417) | 0027 DONE AND availability query DONE AND availability_templates dev seed merged |
| seed | availability_templates dev seed (purple, migration-free) | DONE (#406; guard rework #412, seed role-ID fix #414) | none |
| 0029 | patient number (loop: docs/loops/0029-patient-number.md) | DONE (#435 consumes; migration applied, head 0029) | trigger auto-assign, owner-approved deviation (DECISIONS 2026-07-02); backfill 105==105, 0 nulls, contiguous 1..105 |
| TBD | patient_notes append-only relation | DONE as 0030 (W2-01 #452) | shipped as `patient_note_revisions` in Wave 02 per the JP full-history ruling (DECISIONS 2026-07-02/03); backfill 10/10 |

### Ivan non-migration code (parallel-safe, not migration-numbered)
| Item | Status | Gate |
|------|--------|------|
| availability query (read-only, booked vs free) | DONE (#396) | none, parallel-safe with one in-flight migration |
| schedule-again clone endpoint (loop: docs/loops/schedule-again-clone.md) | DONE (#419) | none |
| FA-1 users-seed natural-key fix (loop: docs/loops/users-seed-natural-key-fix.md) | DONE | landed — seed user FKs resolve by `(tenant_id, email)`; latent risk closed (STATE 2026-07-02) |
| finance KPI report (revenue per therapist/service) | QUEUED (gate cleared) → carried to Wave 04 candidates | VAT answered — CIVA art. 9 exemption, gross=final (DECISIONS 2026-07-02); not scoped in Wave 03; carried forward |

### UI lane (Max)
| Item | Status | Gate |
|------|--------|------|
| remove Proximas marcacoes card | DONE (#385) | none |
| rename Revisao to Revisao Consulta | DONE (#384) | none |
| stale comment fix at page.tsx:360 | DONE (#383) | none |
| patient profile surfacing: profession + region (new in 0022), city + notes (already existed) | DONE (#393) | 0022 DONE |
| auto-select service from therapist | DONE (#445) | 0023 DONE |
| confirmation thumbs on appointment preview | DONE (#441) | 0024 DONE |
| no-note indicator on completed appointments | DONE via W2-04 (#456) | shipped as present-state `EXISTS(appointment_notes)` "Sem nota" indicator; supersedes halt #440 |
| availability panel in new-appointment flow | DONE | availability query (#396) consumed by the new-appointment flow; live |
| fichas-as-tab inside patient profile | DONE via W2-06 (#458) | Registos tab completed; `/clinical` nav item removed, deep links intact; supersedes halt #446 |
| schedule-again action on patient profile | DONE (#442) | clone endpoint DONE (#419) |
| batch failure pop-up | DONE via W2-05 (#457) | Recorrente routed through `batchSchedule` + failure dialog; W3-02 (#469) later lifted the dialog to the top layer; supersedes halt #439 |
| patient ID next to NIF | DONE (#435) | 0029 patient number applied (STATE head 0029); zero-padded display per JP ruling |

### Next-wave candidates (Wave 02 intake)
> Added 2026-07-03 from the wave-01 close-out rulings. Candidates only — not yet scoped into loops.

| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| NESA contraindication warning (flags + booking-time warning) | DECISIONS 2026-07-03 entry A (JP) | soft warning at booking, no hard block; next-wave build |
| note_present capture (PURPLE backend, migration-free) | DECISIONS/QUESTIONS 2026-07-03 (Q-ROW8-1) | prerequisite for row-8 no-note UI indicator (#440) |
| finance KPI report (revenue per therapist/service) | DECISIONS 2026-07-02 (VAT: CIVA art. 9 exemption) | gate cleared; queued until scoped as its own loop |
| fiscal integration (fatura-recibo, insurer/protocol discounts) | DECISIONS 2026-07-03 entry B (JP) | GATED: must be re-specced against the licensed-partner model first; InvoiceXpress relay lock possibly superseded |

## Wave 02 Loop Queue

> Opened 2026-07-03. Same board rules as Wave 01 (status flow, one MIG-lane loop in flight, db-tests.yml/e2e.yml = Ivan hold). Loop files land under `docs/loops/wave-02/`. **Wave-02 lane override (DECISIONS 2026-07-03 "Wave 02 is single-executor"): every W2 loop — migration, UI, PURPLE, docs — belongs to the GREEN runner; the historical GREEN/PURPLE/UI tags apply to future waves, not this one.** Sequencing gates are in the Gate column; DRAFT rows have no committed loop file yet.

| ID | Item | Status | Lane | Gate / note |
|------|------|--------|------|-------------|
| W2-01 | migration 0030 `patient_note_revisions` (loop: `docs/loops/wave-02/W2-01-mig-0030-patient-note-revisions.md`) | DONE (#452) | GREEN | none — patient-notes design ruling received (DECISIONS 2026-07-03); applied on dev, backfill 10/10, append-only + RLS tests green (300 total) |
| W2-02 | UI quick-fix batch (5 items, migration-free) (loop: `docs/loops/wave-02/W2-02-ui-quickfix-batch.md`) | DONE (#454) | GREEN runner | owner ruling 2026-07-03: wave-02 single-executor, UI lane closed this wave — migration-free; 5 items shipped, item 2 already enforced in data layer + regression-tested |
| W2-03 | location data cleanup (live-DB data op) (loop: `docs/loops/wave-02/W2-03-location-cleanup.md`) | DONE (#455) | PURPLE (GREEN runner) | executed per owner ruling 2026-07-03 (Option A, escalated recon mismatch): 2 active rows on fixture ids (OsteoJP (LV)/(CB)), 2 appts repointed, Montemor+manual rows archived, 275 appts unchanged, zero deletes, idempotent |
| W2-04 | no-note indicator UI on completed appointments (loop: `docs/loops/wave-02/W2-04-ui-no-note-indicator.md`) | DONE (#456) | GREEN runner | owner ruling 2026-07-03: wave-02 single-executor, UI lane closed this wave. "Sem nota" on completed+noteless appts across agenda/Marcações/patient-tab/preview via present-state EXISTS(appointment_notes); supersedes halt #440 |
| W2-05 | batch failure pop-up wiring (partial-success) (loop: `docs/loops/wave-02/W2-05-ui-batch-failure-popup.md`) | DONE (#457) | GREEN runner | owner ruling 2026-07-03: wave-02 single-executor, UI lane closed this wave. Recorrente routed through batchSchedule + failure dialog (per-row rebook); NO engine change needed (HALT gate cleared); supersedes halt #439 |
| W2-06 | fichas tab completion (list + create entry point) (loop: `docs/loops/wave-02/W2-06-ui-fichas-tab-completion.md`) | DONE (#458) | GREEN runner | owner ruling 2026-07-03: wave-02 single-executor, UI lane closed this wave. Registos tab: Nova ficha (scoped reuse) + per-ficha Nova versão (reuse versionRecordAction); /clinical nav item removed, list route kept unlinked (no orphan); /clinical/[id] deep links intact; supersedes halt #446 |
| W2-07 | migration 0031 NESA contraindication flags (loop: `docs/loops/wave-02/W2-07-mig-0031-nesa-contraindications.md`) | DONE (#453) | GREEN | AFTER W2-01 (0030) merged — one migration in flight; per DECISIONS 2026-07-03 ruling A; applied on dev, 3 boolean cols default false, db suite 303 green |
| W2-08 | NESA booking warning UI (loop: `docs/loops/wave-02/W2-08-ui-nesa-warning.md`) | DONE (#460) | GREEN runner | owner ruling 2026-07-03: wave-02 single-executor, UI lane closed this wave. Soft warning (never blocks) across 3 surfaces: patient checkboxes, service admin flag, reactive drawer Banner (both booking paths); per DECISIONS 2026-07-03 ruling A |
| W2-09 | batch V2 engine — explicit per-slot datetime list (loop: `docs/loops/wave-02/W2-09-engine-batch-v2.md`) | DONE (#461) | GREEN runner | migration-free; `batchSchedule` now accepts an explicit slot list OR a recurrence rule (discriminated), both converge on one booking loop via pure `resolveBatchSlots`; recurrence callers unchanged; per-slot alternatives; supersedes the V1 recurrence-only path |
| W2-10 | batch V2 UI (Agendar lote: count, every-X, per-date time pickers, summary, confirm) (loop: `docs/loops/wave-02/W2-10-ui-agendar-lote.md`) | DONE (#462) | GREEN runner | AFTER W2-09 merged (met). Agendar lote (count/every-X/per-date times/summary/confirm) submits the explicit slot list to the W2-09 engine; V1 recorrente REMOVED (single entry); reuses W2-05 failure dialog; pure lote.ts helper |
| W2-11 | patient notes tab + Notas Rápidas rewire to patient quick-note (loop: `docs/loops/wave-02/W2-11-ui-patient-notes.md`) | DONE (#463) | GREEN runner | recon: quick_notes = per-staff scratchpad (0 rows, no patient_id) → nothing to migrate; profile Notas tab (append-only) + Notas Rápidas patient-selector rewire; patients.notes flipped off UI (column untouched); migration-free |
| W2-12 | working-hours admin UI — availability template CRUD per therapist in Administração (weekdays, hours, location) (loop: `docs/loops/wave-02/W2-12-ui-working-hours-admin.md`) | DONE (#464) | GREEN runner | migration-free CRUD over `availability_templates` at /admin/working-hours (list/create/edit/archive); overlap not ambiguous (consumer merges) but reject-overlap + end>start enforced; active locations only; creates reflect in the booking panel |
| W2-13 | SMS confirmation flow — SPEC ONLY, no build this wave (Twilio PT, signed SIM/NÃO link page, flips 0024 `confirmation_state`) (loop: `docs/loops/wave-02/W2-13-spec-sms-confirmation.md`) | DONE (#465) | GREEN runner | docs-only: `SPEC-sms-confirmation.md` authored (tenant-from-token, HMAC single-scope, flips 0024 channel=sms, no reply-parsing) + 4 QUESTIONS logged (Twilio vendor, copy, send times, opt-out). No build; build gated on the vendor decision |

### Wave 02 deferred (recorded, not scheduled this wave)
- **2-clients-same-slot UI** — `booking_group_id` (0027) exists in schema, no UI yet. Deferred.
- **Marcações section stays** — no rename/relocation of the Marcações nav section this wave.
- **Fisiozero import** — sequenced LAST (after all Wave 02 build), gated on the pre-real-data gates (separate-prod-project + signed DPA, DECISIONS 2026-07-01 / 2026-07-02).

## Wave 03 candidates

> **CONSUMED at Wave 03 close (2026-07-06).** The Wave 03 scope pass (owner + Rodica, DECISIONS 2026-07-05) drew a different, tightly-scoped set of ten loops (W3-01..W3-10, all DONE). Of the candidates below, only "Superseded Max halt PRs closure" was scoped into Wave 03 (W3-10 #477, DONE). The remaining candidates (SMS build, finance KPI, Fisiozero import, Portal V2, CI db-gate hardening, preview DB isolation, legacy-shelf consolidation, dangling-branch pruning) were NOT taken this wave and are **carried forward into the Wave 04 candidates section below**. Kept here as the historical intake record.
>
> Added 2026-07-03 at Wave 02 close. UNORDERED, NOT committed scope — candidates for the next wave's planning pass, not loops yet. No lane/gate assignments until scoped.

- **SMS confirmation BUILD** — build the flow specified in `docs/design/SPEC-sms-confirmation.md`. GATED on Twilio vendor confirmation + EU residency/DPA (owner-confirmable) and JP's copy/send-time/opt-out answers (QUESTIONS 2026-07-03). No build until those clear.
- **Finance KPI report scoping** — the 0025 `analytics_events` capture is live and the VAT ruling is on file (CIVA art. 9 exemption, gross=final, DECISIONS 2026-07-02); scope it as its own loop. Note: the fiscal partner-model re-spec (DECISIONS 2026-07-03 entry B) applies to any fatura-recibo/discount work, not to the internal KPI report.
- **Fisiozero import build** — the import side (mapping + reconciliation), using dev SAMPLE data only; the live import stays gated on the pre-real-data gates (separate-prod-project + signed DPA). Import-collision HALT policy already recorded (DECISIONS 2026-07-02).
- **Portal V2 redesign** — GATED on owner mockups; no scope until provided.
- **CI db-gate hardening** — the db-gated step soft-passed a failing inner test during #449; hardening it touches `.github/workflows/db-tests.yml` → automatic OWNER HOLD, never self-merged (QUESTIONS 2026-07-03).
- **Preview DB isolation** — Vercel Preview envs share the single Supabase project with the deployed app; isolate preview DB as future infra (QUESTIONS 2026-07-03), part of the separate-prod-project work.
- **Legacy-shelf consolidation loop** — migrate the still-open `docs/QUESTIONS.md` items and any live `docs/DECISIONS.md` content onto the canonical `docs/design/` shelf, then leave pointer stubs (QUESTIONS 2026-07-03).
- **Dangling-branch pruning pass** — sweep merged/abandoned feature + docs branches (and their worktrees) left over from Waves 01–02; housekeeping only.
- **Superseded Max halt PRs closure** — post closing "superseded by W2-04/05/06" comments on the abandoned halt PRs **#440** (→ W2-04 #456), **#439** (→ W2-05 #457), **#446** (→ W2-06 #458); replacements all merged (QUESTIONS 2026-07-03 housekeeping ticket). Comment-only, no revert/reopen. → SCOPED into **W3-10**.

## Wave 03 Loop Queue

> Opened 2026-07-05 from the owner + Rodica QA scope pass (rulings recorded DECISIONS.md 2026-07-05). Active board — this is the runnable set. Same board rules as prior waves (status flow, ONE migration-lane loop in flight at a time, `db-tests.yml`/`e2e.yml` = owner hold, never self-merged). Loop files land under `docs/loops/wave-03/`. Sequencing gates are in the Gate column. All ten loops are authored and committed (WRITTEN); execution is GREEN's.
>
> Pre-real-data gate status at wave open: the **DPA gate is CLOSED** (signed, DECISIONS 2026-07-05); the **separate-prod-project gate remains OPEN** (DECISIONS 2026-07-01), so branch protection is not yet re-hardened and dev-phase merge rules still apply. No real patient data may enter until the prod-project gate also closes.

| ID | Loop | Status | Lane | Gate / note |
|------|------|--------|------|-------------|
| W3-01 | estado-removal-fix (`docs/loops/wave-03/W3-01-estado-removal-fix.md`) | DONE (#468) | UI (migration-free) | recon: UI already hid the Estado selector on create (W2-02 #454, e2e-covered); real gap was server-side — create + batch now hardcode `scheduled`/`pending`, never from payload. Axes stay orthogonal (DECISIONS 2026-07-01) |
| W3-02 | batch-failure-dialog-focus (`docs/loops/wave-03/W3-02-batch-failure-dialog-focus.md`) | DONE (#469) | UI (migration-free) | failure dialog was an in-flow overlay behind the modal drawer (inert; Escape/clicks hit the discard guard); now its own showModal <dialog> in the top layer via shared `useAnimatedDialog` — focused, isolated from "Descartar alterações?", edit-and-rebook works |
| W3-03 | booking-form-reorder (`docs/loops/wave-03/W3-03-booking-form-reorder.md`) | DONE (#470) | UI (migration-free) | Terapeuta first, Serviço below + auto-fill from therapist's first mapped service (fallback; W3-04 primary absent on main), editable override honored; reads `therapist_services` (0023), ordered created_at asc |
| W3-04 | primary-service-admin (`docs/loops/wave-03/W3-04-primary-service-admin.md`) | DONE (#471) | Admin (migration-free) | primary = earliest-created `therapist_services` mapping (no schema change, no UPDATE); re-designation = delete+insert of the others; admin "Serviço principal" on /admin/staff; W3-03 consumes it unchanged (already reads oldest-first) |
| W3-05 | tenant-settings-home (`docs/loops/wave-03/W3-05-tenant-settings-home.md`) | DONE (#472) | MIG-conditional → migration-free | verdict: MIGRATION-FREE — `tenants.settings` jsonb (RLS `tenants_tenant_isolation`, not client-exposed) hosts secrets under `settings.secrets`. Helper `lib/admin/tenant-secret.ts`; contract in DECISIONS. Head stays 0031. Unblocks W3-06 |
| W3-06 | password-gated-appointment-delete (`docs/loops/wave-03/W3-06-password-gated-appointment-delete.md`) | DONE (#473) | Server + UI (migration-free) | hard-delete behind scrypt-hashed tenant password (W3-05 home, admin-only settings:manage); refuses linked notes/records/invoices; child-first RETURNING delete + PII-free audit snapshot; admin password-change in Administração |
| W3-07 | location-delete-when-unreferenced (`docs/loops/wave-03/W3-07-location-delete-when-unreferenced.md`) | DONE (#474) | Admin (migration-free) | delete enabled only for zero-appointment locations (else archive-only + disabled + tooltip); FKs handled non-destructively (null services/analytics, child-first delete of location-scoped config); archived stays hidden from dropdowns |
| W3-08 | agenda-6day-24h (`docs/loops/wave-03/W3-08-agenda-6day-24h.md`) | DONE (#475) | UI (migration-free) | week view = 6 days Mon–Sat (WEEK_DAYS 5→6, propagates to grid + fetch range); 24h already app-wide (central formatTimeOfDay + pt-PT Intl + native pickers), zero meridiem hits; tests + e2e |
| W3-09 | working-hours-real-schedule (`docs/loops/wave-03/W3-09-working-hours-real-schedule.md`) | DONE (#476) | PURPLE (live-DB data op) | guarded idempotent op set 5 dev therapists to Mon–Fri 08:00–20:00 + Sat 09:00–13:00 (primary location); upsert+archive, no delete; BEFORE 34/Sat=0 → AFTER 30 active/34 archived → re-run zero-delta |
| W3-10 | close-superseded-prs (`docs/loops/wave-03/W3-10-close-superseded-prs.md`) | DONE (comment-only; no PR) | gh-only housekeeping | confirmed #440→#456, #439→#457, #446→#458 all MERGED and each ALREADY carries the correct superseded-by comment (posted at the W2-04/05/06 merges); no duplicate posted, no revert/reopen/re-close |

### Wave 03 sequencing notes
- **One migration in flight:** only W3-05 may author a migration (0032, and only if recon finds no suitable existing tenant-settings home). Confirm 0031 is the latest on main and no migration PR is open before it authors.
- **W3-06 gates on W3-05 merged** (needs the hashed-secret home). **W3-03 soft-gates on W3-04** (primary representation) but ships the reorder regardless with a documented fallback.
- All other loops (W3-01, W3-02, W3-07, W3-08, W3-09, W3-10) are independent and parallel-safe against a single in-flight migration.

> **WAVE 03 CLOSED — 2026-07-06.** All ten loops merged (W3-01 #468 … W3-10 #477), zero halts, zero escalations. Migration head UNCHANGED at 0031, mirror parity 32/32. Suites: web 685, db 56 local + 255 DB-gated. Two on-branch CI-red fixes cleared before merge (W3-02 Playwright hidden-dialog assertion #469; W3-08 pt-PT Sábado weekday render #475). Halt-desk mailbox pattern ran its second wave with zero escalations. Real therapist entry (Max, admin UI) is IN PROGRESS — staff data, not patient data. See STATE.md 2026-07-06 close audit and the Wave 04 candidates section below.

## Wave 04 Loop Queue — early batch (two hotfix loops, ahead of the rest of Wave 04)

> Opened 2026-07-06 from owner QA after Wave 03 close. This is the ACTIVE, runnable board. These two loops ship AHEAD of the rest of Wave 04 authoring because **real therapist data entry (Max, in progress) is BLOCKED on W4-01(a)**. Same board rules as prior waves (status flow, ONE migration-lane loop in flight at a time, `db-tests.yml`/`e2e.yml` = owner hold never self-merged). Loop files land under `docs/loops/wave-04/`. Halt protocol for this batch is **CLASSIC** (no CYAN desk running): on any blocker, stop and report to Ivan with the mismatch, options, and a recommended default. Both loops are **migration-free**; head stays 0031.

| ID | Loop | Status | Lane | Gate / note |
|------|------|--------|------|-------------|
| W4-01 | equipa-team-upgrade (`docs/loops/wave-04/W4-01-equipa-team-upgrade.md`) | DONE (#480) | Admin (migration-free) | (a) Equipa primary-service dropdown listing ALL active services — INSERT when a therapist has zero mappings (fixes "Sem serviços"; e.g. Catarina Vieira), delete+insert re-designation when mappings exist (W3-04 mechanism, no UPDATE, 0023 no-grant); (b) per-therapist working-hours entry point from the Equipa row (recon: reuse W2-12 `/admin/working-hours` Horários CRUD if it scopes per-therapist, else build the minimal per-therapist view). **Unblocks Max's real therapist entry.** pt-PT, admin-only server-enforced |
| W4-02 | 24h-picker-sweep (`docs/loops/wave-04/W4-02-24h-picker-sweep.md`) | DONE (#481) | UI (migration-free) | recon-first: find EVERY time-INPUT widget (Nova marcação Hora picker confirmed offender + Agendar lote / working-hours / reschedule siblings), convert all to 24h `00:00`–`23:59`, remove AM/PM columns entirely; input-construction grep pass (not just format tokens) + value round-trip proof; zero AM/PM anywhere incl. inputs |

### Wave 04 early-batch board notes — owner-QA findings (2026-07-06)
- **QA finding vs W3-04 (#471) — design gap, not a regression.** W3-04 built primary-service **re-designation among a therapist's EXISTING `therapist_services` mappings** (delete+insert, no UPDATE per 0023). It never wired the **zero-mapping case**: a therapist with no mappings (e.g. **Catarina Vieira**) shows **"Sem serviços"** with **no control at all**, so no first/primary service can be assigned and Nova marcação has nothing to auto-fill. → scoped into **W4-01(a)**: the dropdown lists all active tenant services and INSERTs the first mapping through the existing append path.
- **QA finding vs W3-08 (#475) — a second component path missed.** W3-08 converted 24h **display** (confirmed correct) but its DoD grep matched meridiem **format tokens** only. The **custom time-picker widget in Nova marcação** builds its own `09 / 00 / AM–PM` columns from literal arrays/state — not a format token — so the token-grep could not see it. **Same failure class as the W3-01 estado regression (a second, unsearched component path).** → scoped into **W4-02**: recon widened to time-INPUT construction, all custom pickers converted to 24h, AM/PM columns removed.
- **Remaining Wave 04 authoring follows after this cleanup batch.** The rest of Wave 04 (AI recording pipeline, camera/photo capture, cleanup/data-hygiene, and the carried Wave 03 candidates below) is authored AFTER these two hotfix loops land, so Max's real therapist entry is unblocked first.

## Wave 04 Loop Queue — authored batch (W4-03 … W4-11)

> Authored 2026-07-06 (YELLOW docs lane). Loop files under `docs/loops/wave-04/`, 7-field Loop Package each. Same board rules as prior waves (status flow, ONE migration-lane loop in flight at a time, `db-tests.yml`/`e2e.yml` = owner hold never self-merged). Halt protocol is **CLASSIC** (no CYAN desk): on any blocker, stop and report to Ivan with the mismatch, options, and a recommended default. **All loops migration-free; head stays 0031.** Standing rules embedded in every loop: pt-PT UI copy; **all build + dry-run work SYNTHETIC-DATA-ONLY** (real-data go-live separately gated, owner ruling 2026-07-06); **LIVE-DATA CAUTION** — real therapist accounts (Max's) live on dev tenant `3a2d0711-fbdb-4ce9-b940-b6a87e3d3560`, never mutated; secrets fingerprints-only, never printed.

**EXECUTION ORDER (strict):** W4-03 → W4-04 → W4-05 → **MAX GATE** (Max confirms real-therapist entry complete) → W4-11 → W4-06 → W4-07 → W4-08 → W4-09 → W4-10. Rationale: W4-11 cleanup runs after W4-03 merges + the Max gate and BEFORE the recording chain, so the W4-10 dry run fires on a clean dev tenant that still holds the real therapists (DECISIONS 2026-07-06). The recording chain (W4-06 → W4-09) is strictly sequential; W4-10 is last and carries an EXTERNAL DoD (André confirmation).

| ID | Loop | Status | Lane | Gate / note |
|------|------|--------|------|-------------|
| W4-03 | nova-marcacao-auto-select (`docs/loops/wave-04/W4-03-nova-marcacao-auto-select.md`) | DONE (resolved-unreproducible, docs-only, owner QA 2026-07-07) | UI+server (migration-free) | **RESOLVED-UNREPRODUCIBLE.** Owner live QA (Ivan, 2026-07-07) confirmed Definir + Nova marcação Serviço auto-select **fires correctly**. Matches GREEN recon: the W3-03 read round-trip was proven correct and covered by a passing e2e; the 2026-07-06 QA symptom was environment/transient, no code defect. Closed docs-only, **zero code change**. Duplicate Fisioterapia service rows = owner UI housekeeping (synthetic seed artifacts, no loop action; any cleanup rides W4-11). Original defect note (2026-07-06):  Serviço does not auto-fill in Nova marcação for a W4-01-written primary (Tiago Reis). Recon-first — prove hypothesis (a) stale read / (b) event not re-triggered / (c) W4-01 write shape diverges from W3-03 read; second-component-path rule: recon + fix ALL Serviço-rendering paths (Nova marcação, Agendar lote, batch rebook, schedule-again). No UPDATE to `therapist_services` (0023 42501). Reproduce on a W4-01-written fixture on the E2E seed tenant, never on Tiago Reis |
| W4-04 | spec-ai-recording (`docs/loops/wave-04/W4-04-spec-ai-recording.md`) | DONE (#483) | SPEC-only (zero code) | Authors `docs/design/SPEC-ai-recording.md` — full M1 webhook contract (`audio_url` presigned GET 1h, `audio_filename`, `patient_id`, `doctor_id`, `consultation_started_at/ended_at`, template `osteopathy`); `x-make-apikey` header (vault-only); MediaRecorder webm/opus 32 kbps mono + Whisper 25 MB / 14.4 MB-per-hour / 90-min rationale; Chrome-only + pt-PT block; machine-stamped timestamps feeding the idempotency key; direct-to-S3 PUT never via Vercel; stub-before-Record; consent (actor+timestamp); identity-human vs twelve-clinical-fields-AI. **Merged before the build chain consumes it** |
| W4-05 | camera-to-ficha (`docs/loops/wave-04/W4-05-camera-to-ficha.md`) | DONE (#484) | UI+storage (migration-free) | Rodica request, JP-approved (photos in fichas). Recon-first: what "Adicionar anexo" does today + whether upload works. Then in-page `getUserMedia` capture (preferred over file-input `capture` — the "never in her gallery" requirement) → attach to a **synthetic** patient's ficha anexos via the existing signed-URL path (CLAUDE.md rule 8, never public). Rodica real-phone check relayed by Ivan closes the loop |
| — | **MAX GATE** | — | gate | Max confirms real-therapist entry complete (relayed by Ivan). W4-11 does not start until satisfied |
| W4-11 | scripted-cleanup (`docs/loops/wave-04/W4-11-scripted-cleanup.md`) | DONE-partial (Option B, owner ruling 2026-07-07) | GUARDED live-DB data op (migration-free) | Runs AFTER W4-03 merged AND the MAX GATE, BEFORE W4-06. Purge synthetic patients + their appointments/`patient_note_revisions`, analytics test events, and the 5 dev fixture therapists + their `availability_templates`/`therapist_services`. **Preserve by exclusion:** all real therapist accounts (Max's), locations, services, roles, tenant settings. Natural-key resolution (never hardcode FK ids), counts before/after, child-first, `RETURNING`, zero-delta re-run. Any classification ambiguity → HALT (DATA) |
| W4-06 | quick-create-stub-consent (`docs/loops/wave-04/W4-06-quick-create-stub-consent.md`) | DONE (#489) | UI+server (migration-free) | Recording chain step 1. Start-consultation: existing-patient select OR new stub (name required, phone optional, "Criar e iniciar gravação"); 0029 trigger numbers on NULL (no schema). Consent checkbox before Record, stored actor+timestamp, server-enforced. Out of scope: merge-patients (roadmap), 30-day stub cleanup (Wave 05) |
| W4-07 | recording-ui (`docs/loops/wave-04/W4-07-recording-ui.md`) | DONE (#490) | UI (migration-free) | Depends W4-06 + SPEC. MediaRecorder webm/opus 32 kbps mono, Chrome-only gate + pt-PT block, Record→`consultation_started_at` / Stop→`consultation_ended_at` machine-stamped. Produces the blob; **no upload here** (W4-08) |
| W4-08 | presigned-put-flow (`docs/loops/wave-04/W4-08-presigned-put-flow.md`) | DONE (#491) | backend+client (migration-free) | Depends W4-07. Backend signs presigned PUT with the scoped AWS key (`PutObject`+`GetObject` on `osteojp-audio-intake` only, eu-central-1) from Vercel env; browser PUTs direct to S3, never via a Vercel route (4.5 MB limit). CORS is André's side (locked to the EMR origins). Round-trip proof: signer response + presigned GET 200, credential-free. Key never printed/in code |
| W4-09 | post-upload-webhook (`docs/loops/wave-04/W4-09-post-upload-webhook.md`) | DONE (#492) | backend (migration-free) | Depends W4-08. Backend generates presigned GET (1h) + fires the M1 webhook with the full contract + `x-make-apikey` (env/vault only). DoD: a fire returns success with all contract fields present; payload pasted redacted (no key, presigned-URL token truncated) |
| W4-12 | location-auto-select (`docs/loops/wave-04/W4-12-location-auto-select.md`) | DONE (#486) | UI+server (migration-free) | Owner addition (Ivan 2026-07-06). Booking Terapeuta selection auto-fills Localização from the therapist's location assignment (derived from `availability_templates`, migration-free): exactly one active location → auto-fill; zero/multiple → manual stays; always editable, manual pick never clobbered. Fires on the SAME event as the W3-03 Serviço auto-fill via `getTherapistLocationIds` + `pickAutoFillLocation` + a `userChangedLocation` ref. Three drawer paths inherit it; schedule-again copies source (unaffected). Dedicated E2E fixtures (single- + multi-location therapists) |
| W4-10 | first-test-fire-e2e (`docs/loops/wave-04/W4-10-first-test-fire-e2e.md`) | DONE (external confirmation relayed by Ivan 2026-07-08; #493 machine DoD merged) | backend/dry-run (migration-free) | **LAST.** Depends W4-09 merged AND W4-11 completed. Synthetic quick-created patient; real therapist id as `doctor_id` **READ-ONLY** (zero mutation); audio = laptop mic or generated webm/opus (Jabra re-test is a follow-up, owner ruling). MUST carry `x-make-apikey` + `audio_filename` (André's module-26 token). **DoD part 1 (machine):** draft lands `pending_review` via the existing HMAC ingestion endpoint, DB evidence pasted. **DoD part 2 (external):** André confirms receipt + token exposure, relayed by Ivan → AWAITING-EXTERNAL mailbox note, loop closes on the relay |

> **W4-11 dev-tenant synthetic residue (owner ruling B, 2026-07-07):** the Option-B partial cleanup deleted all deletable synthetic data (74 patients + subtree, 108 appointments, 64 fixture `availability_templates`; fixture users deactivated, not deleted) and left, by design, **49 locked/signed synthetic clinical_records + their 31 pinned patients + the pinned fixture users**. The clinical-records immutability trigger (rule 4) and audit_log append-only (rule 6) were never touched; no bypass script exists. **Revisit this residue at the pre-real-data gate when the prod Supabase project splits — the residue is dev-only by construction.** Evidence pasted in DECISIONS 2026-07-07.

## Wave 04 Loop Queue — design batch (W4-13 … W4-18)

> Authored 2026-07-06 (YELLOW docs lane, batch 2). Loop files under `docs/loops/wave-04/`, 7-field Loop Package each. Owner-QA-driven surface redesigns. **This batch is MIGRATION-FREE and GATE-INDEPENDENT of the recording chain — the owner may pull it forward if the recording chain (W4-06 → W4-10) is blocked on a gate.** Same board + halt rules as the batch above (CLASSIC halt). Standing rules embedded in every loop: pt-PT UI copy; functionality-preserving unless the loop states otherwise; **LIVE-DATA CAUTION** — real therapist accounts (Max's) + their `availability_templates`/`therapist_services` on dev tenant `3a2d0711-fbdb-4ce9-b940-b6a87e3d3560` are never mutated, all verification on the E2E seed tenant; redesigns move Playwright selectors → update specs **on-branch**, **never touch `db-tests.yml`/`e2e.yml`**.

**BATCH ORDER (W4-13 FIRST — design anchor, then 14→18):** W4-13 establishes the visual system in `docs/design/UI-STYLE.md`; **W4-14 → W4-18 depend on W4-13 merged and conform to UI-STYLE.md.** Within 14→18 there is no hard inter-dependency (they touch different surfaces) — run in the listed order or parallelize as capacity allows, each conforming to UI-STYLE.md.

| ID | Loop | Status | Lane | Gate / note |
|------|------|--------|------|-------------|
| W4-13 | equipa-dashboard-redesign (`docs/loops/wave-04/W4-13-equipa-dashboard-redesign.md`) | DONE (#496) | Admin UI (migration-free) | **DESIGN ANCHOR — runs FIRST.** Full-width invite area (+ team summary counts) + team dashboard table (Nome, Email, Função, Serviço principal, Estado badge, Ações) with row-actions grouped (menu/drawer). Preserves ALL existing Equipa functionality exactly incl. the **password-gated therapist delete (gate unchanged)** and the no-UPDATE primary-service dropdown. **Commits `docs/design/UI-STYLE.md`** (card/table/spacing/badge/button/toolbar/Tailwind-v4 tokens) that W4-14→W4-18 follow. Refinement, not rebrand |
| W4-14 | horarios-redesign (`docs/loops/wave-04/W4-14-horarios-redesign.md`) | DONE (#497) | Admin UI+server (migration-free, functional change) | Depends W4-13. Per-therapist cards + one `Editar horário` **top-layer modal** (`showModal`, W3-02): weekday toggles, per-day 24h hours (W4-02 `TimeField`, 15-min step) + location, one `Guardar` through the W2-12 CRUD paths; **in-modal row delete, NO password** (owner ruling 2026-07-06); deep link `?t=<id>` preselects. Zero availability-read regression (`getTherapistAvailability`, booking). **LIVE-DATA CAUTION strongest here** |
| W4-15 | servicos-delete-and-redesign (`docs/loops/wave-04/W4-15-servicos-delete-and-redesign.md`) | DONE (#498) | Admin UI+server (migration-free, functional change) | Depends W4-13. Per-service delete, **NO password**, reference-guarded (W3-07 pattern): zero-reference hard-delete (`RETURNING`), referenced = archive-only (disabled control + pt-PT tooltip). **Recon confirms the ACTUAL service reference set** (appointments / `therapist_services` / `service_location_prices` / any others) before build. Restyle the Serviços tab per UI-STYLE.md (table, Estado badges, cleaner Preços por local) |
| W4-16 | pacientes-redesign (`docs/loops/wave-04/W4-16-pacientes-redesign.md`) | DONE (shipped in prior V2 wave; owner re-QA at wave close 2026-07-07, docs-only) | UI (migration-free, display-only) | Depends W4-13. List → structured table (Paciente + avatar initials, NIF, Nº de paciente, Telemóvel, chevron), search unchanged; detail → dashboard (identity header + dados/notas/anexos/marcações). **Display-only: zero data-model change; append-only `patient_note_revisions` untouched, `patients.notes` stays ignored; anexos signed-URL behavior unchanged** |
| W4-17 | agenda-header-redesign (`docs/loops/wave-04/W4-17-agenda-header-redesign.md`) | DONE (#499) | UI (migration-free) | Depends W4-13. Unified toolbar (prominent segmented Dia/Semana, date picker, Hoje, prev/next grouped) + a structured range chip replacing the floating week-range text, carrying a **live appointment count** for the visible range (owner default); `Todos os terapeutas` + `Todas as localizações` filters aligned into the toolbar row. **W3-08 six-day Mon–Sat + 24h grid untouched** |
| W4-18 | inicio-redesign (`docs/loops/wave-04/W4-18-inicio-redesign.md`) | DONE (#500) | UI (migration-free) | Depends W4-13. Sixth quick-action tile `Revisão Consulta` (owner default) right of Administração, linking to the existing page; `Resumo semanal` extended full-width; new `Próximas marcações` card (owner default) — today's next appointments (time/patient/therapist), reading existing data, role-scoped. Notas Rápidas + existing tiles untouched |

## Wave 04 Loop Queue — secondary participants (W4-19, LAST in the wave)

> Authored 2026-07-06 (YELLOW docs lane, batch 3). **LAST loop in Wave 04 — runs after W4-18.** The **ONLY Wave 04 loop pre-approved to fire a migration** (owner ruling, DECISIONS 2026-07-06 "Secondary participants on appointments"): recon-first — migration-free if a genuine path exists, else **ONE migration 0032** (the single migration in flight). Standing rules apply: pt-PT UI copy; LIVE-DATA CAUTION (real therapist accounts on dev tenant `3a2d0711-...` never mutated, verify on the E2E seed tenant); conforms to `docs/design/UI-STYLE.md` (W4-13); Playwright selector updates on-branch, **never touch `db-tests.yml`/`e2e.yml`**; CLASSIC halt.

| ID | Loop | Status | Lane | Gate / note |
|------|------|--------|------|-------------|
| W4-19 | secondary-participants (`docs/loops/wave-04/W4-19-secondary-participants.md`) | DONE (#501, migration 0032) | UI+server (+conditional schema — **migration pre-approved**) | **LAST in Wave 04, after W4-18.** Optional `Paciente 2` + `Terapeuta 2` on the booking panel (de-emphasized), persisted linked display data. **Primary-only semantics everywhere** (availability, Serviço/Localização auto-selects, analytics attribution, AI-recording primary pair + idempotency key, Estado/lifecycle axes all stay primary). Secondary shown on appointment details + agenda card (`+1` badge); agenda renders under the primary therapist column only (both-columns = recorded follow-up). Clone copies secondary as-is; W3-06 + reference guards count secondary linkage. **Recon-first migration:** migration-free if possible, else **pre-approved 0032** (default: two nullable FK columns on `appointments`; junction only on a hard blocker) — the **only wave loop allowed a migration**; if fired → mirrored + `--check` (parity 33/33, head 0032), RLS db test in the same PR |

> **WAVE 04 CLOSED — 2026-07-07.** All 19 loops (W4-01…W4-19) plus the owner addition W4-12 resolved. Final PRs: W4-01 #480, W4-02 #481, W4-03 #495 (resolved-unreproducible), W4-04 #483, W4-05 #484, W4-06 #489, W4-07 #490, W4-08 #491, W4-09 #492, W4-10 #493 (machine DoD; AWAITING-EXTERNAL), W4-11 #502 (DONE-partial, Option B), W4-12 #486, W4-13 #496, W4-14 #497, W4-15 #498, W4-16 #502 (docs-only close), W4-17 #499, W4-18 #500, W4-19 #501 (migration 0032). Migration head **0032**, mirror parity **33/33**, branch-time main SHA **`69d2710`**. Suites: web **816**, ui **42**, @osteojp/db **56** local + DB-gated (incl. `secondary-participants-rls.test.ts`), admin **10**, api **136**; lint 0, typecheck 9/9. One escalated ruling (W4-11 A/B/C → Option B) and one surfaced already-satisfied loop (W4-16 → docs close), both via #502. See STATE.md 2026-07-07 close audit and DECISIONS 2026-07-07. Wave 05 candidates below.

## Post-Wave-04 standalone fixes

| Loop | Ref | Status | What |
|------|-----|--------|------|
| W4-20 | consultation-discoverability (`docs/loops/wave-04/W4-20-consultation-discoverability.md`) | DONE (standalone, 2026-07-08) | Owner QA: the start-consultation recording screen (`/consultation`, W4-06/W4-07) had NO nav entry (direct-URL only). Replace the Início **Revisão Consulta** quick-action tile with an **Iniciar consulta** tile → `/consultation` (QuickActionTile anatomy per UI-STYLE.md, pt-PT "Iniciar consulta", gated `clinical_records:author`). Revisão Consulta stays reachable via its left-nav entry. Recon: `/consultation` already renders in `<AppShell>` (left nav present), so no wrapping change needed. Migration-free; dashboard e2e updated |

### Wave 04 OPEN-EXTERNAL carry-overs (not blockers — each closes via a one-line docs flip when the relay lands)
| ID | Item | Status | Closes when |
|----|------|--------|-------------|
| W4-10 | first-test-fire-e2e — real deployed-app fire (André's live Make scenario) | DONE (André confirmed receipt + `audio_filename` token exposure, relayed by Ivan 2026-07-08; #493 machine DoD) | André confirms receipt of the real fire **+ `audio_filename` token exposure**, relayed by Ivan. Owner-performed (no deployed-app credentials / interactive browser pick / real audio in the autonomous lane) |
| W4-05 | camera-to-ficha — Rodica real-phone camera-capture check | DONE (Rodica verified 2026-07-08, relayed by Ivan) | **Rodica real-phone verification 2026-07-08: photo in anexos, zero gallery persistence.** In-page captured photo landed in the ficha anexos and nothing was saved to her phone gallery — closes the external check. Build shipped #484 |

## Wave 04 candidates

> Added 2026-07-06 at Wave 03 close. UNORDERED, NOT committed scope — candidates for the Wave 04 planning pass, not loops yet. **HISTORICAL** — most became Wave 04 loops; retained as the planning trail. No lane/gate assignments until scoped. **All Wave 04 build and dry-run work is SYNTHETIC-DATA-ONLY; real-data go-live is a separately gated step (pre-real-data gates: separate-prod-project DECISIONS 2026-07-01; DPA CLOSED DECISIONS 2026-07-05).**

### AI recording pipeline (new this wave — owner-confirmed infra + product calls, DECISIONS 2026-07-06)
| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| `SPEC-ai-recording.md` authoring | DECISIONS 2026-07-06 (AI recording infra) | full M1 webhook contract incl. the API-key header + `audio_filename` field; authored before the build loops consume it |
| Recording UI (MediaRecorder) | Wave 04 scope | `webm`/opus, 32 kbps mono, **Chrome-only gate**, automatic timestamps |
| Consent checkbox hook before Record | DECISIONS 2026-07-06 (AI recording consent) | checkbox gates the Record action; store actor + timestamp, minimum-viable (candidate home: `audit_log`, PII-free) |
| Quick-create stub patient at record time | DECISIONS 2026-07-06 (visitor stub retention) | name REQUIRED, phone OPTIONAL; 0029 trigger handles numbering; identity data human-entered ONLY (no auto-fill of identity) |
| Presigned PUT direct-to-S3 + CORS coordination | DECISIONS 2026-07-06 (AI recording infra) | never through Vercel routes; 4.5 MB limit; supply EMR origins list to André for the CORS rule (pending) |
| Post-upload presigned GET + M1 webhook fire | DECISIONS 2026-07-06 (AI recording infra) | backend signs the GET; webhook fires with API-key auth + `audio_filename` |
| End-to-end synthetic dry run (with André) | Wave 04 DoD | the wave's Definition of Done — synthetic-data-only, André in the loop |

### Camera / photo capture
| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| Camera-to-ficha capture button | DECISIONS 2026-07-06 (photos in fichas approved) | `getUserMedia` **recon-first**; NO device-gallery persistence; Rodica phone verification; signed-URL storage only (CLAUDE.md rule 8) |

### Cleanup / data hygiene
| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| 30-day stub cleanup job | DECISIONS 2026-07-06 (visitor stub retention) | cleans never-promoted stub patients after 30 days; preserves real/promoted patients and Max's real therapist accounts |
| Scripted test-data cleanup loop (post-QA) | Wave 04 scope | runs AFTER QA; **preserves Max's real therapist accounts** (staff) and any promoted patients; synthetic QA rows only |
| Merge-patients function | Roadmap (NOT this wave) | `patients.merged_into_id` pointer exists in schema (STATE 2026-06-30); function is roadmap, deferred |

### Carried from Wave 03 candidates (not taken W3)
| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| SMS confirmation BUILD | DECISIONS 2026-07-06 (product calls) | product calls made (single 24h reminder, no at-booking msg, staff-toggle opt-out); still GATED on the Twilio vendor + EU-residency/DPA decision (OPEN) |
| Finance KPI report scoping | DECISIONS 2026-07-02 (VAT: CIVA art. 9 exemption) | gate cleared (gross=final); scope as its own loop; internal KPI only, not fatura-recibo |
| Fisiozero import build | BACKLOG Wave 03 candidates | mapping + reconciliation on dev SAMPLE data only; live import gated on the pre-real-data gates; collision-HALT policy on file (DECISIONS 2026-07-02) |
| Portal V2 redesign | BACKLOG Wave 03 candidates | GATED — pending JP's reaction to Max's mockups; no scope until provided |
| CI db-gate hardening | QUESTIONS 2026-07-03 | touches `.github/workflows/db-tests.yml` → automatic OWNER HOLD, never self-merged; opens a PR and HALTS for owner merge |

### Recorded follow-ups (not scoped)
| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| Agenda: render a secondary-therapist appointment under BOTH therapist columns | DECISIONS 2026-07-06 (Secondary participants on appointments) | explicitly deferred out of W4-19 — W4-19 renders under the PRIMARY therapist column only (`+1` badge for the secondary). Dual-column rendering is the recorded follow-up; scope later if the clinic wants it |
| Preview DB isolation | QUESTIONS 2026-07-03 | future infra, part of the separate-prod-project work |
| Legacy-shelf consolidation loop | QUESTIONS 2026-07-03 | migrate still-open legacy `docs/` items onto canonical `docs/design/`, leave pointer stubs; its own docs loop |
| Dangling-branch pruning pass | BACKLOG Wave 03 candidates | sweep merged/abandoned feature + docs branches and their worktrees from Waves 01–03; housekeeping only |

## Wave 05 Loop Queue

> Added 2026-07-08 (YELLOW authoring). **COMMITTED Wave 05 scope** - the 17 loops below are authored under `docs/loops/wave-05/`, scoped from the owner+Rodica QA pass and the Ficha Medica redesign decision (`docs/design/SPEC-ficha-medica.md`). This queue supersedes the "Wave 05 candidates" planning trail below for the items it covers. **Batch and run order here is authoritative.** Status `OPEN` = loop file authored + committed in the authoring PR, awaiting owner merge of that PR and dispatch (maps to the board's WRITTEN state; flips to READY when its gate clears, then IN-FLIGHT/DONE per the coordination protocol at the top of this file).
>
> Standing rules apply to every loop: pt-PT UI copy via i18n keys (both `strings.pt.json` + `strings.en.json`); plain hyphens, no em/en dashes; all build/verify work SYNTHETIC-DATA-ONLY (real-data go-live separately gated); `db-tests.yml`/`e2e.yml` = automatic owner hold, never self-merged; one migration in flight at a time; each loop opens ONE PR and HALTS for owner merge (no self-merge).
>
> **Batch 3 note:** the migrations are STRICTLY SEQUENTIAL (one in flight), each with **live-apply verification before DONE**. **Batch 4 note:** all five loops DEPEND ON `docs/design/SPEC-ficha-medica.md` (authoritative), which must merge before they build.
>
> **Two RECON MISMATCHES flagged at authoring (surface at merge, see QUESTIONS 2026-07-08):** W5-03 (Profissao is ALREADY present in the form + profile - likely already-shipped, Q-W5-6) and W5-12 (the `time_off` block model ALREADY exists at migration 0006 - migration 0034 likely UNNECESSARY, Q-W5-7). Neither is a hard-halt trigger (the named triggers were the opposite conditions); both are authored with the mismatch recorded in-loop and a recommended default in QUESTIONS.

### Batch 1 - migration-free, demo priority (run in numeric order; UI-lane, parallel-safe with one in-flight migration)
| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W5-01 | login-redesign-branding | DONE (#519, 2026-07-08) | migration-free; `/login` restyled to GlassPanel + enlarged brand lockup (additive `xl:96` BrandLockup step + optional `brandSize` prop per owner ruling Q-W5-8); no auth-logic change |
| W5-02 | search-sweep | DONE (#515, 2026-07-08) | migration-free; Equipa search added + list-surface sweep (record-creation patient Select -> async Combobox, marcacoes/review search); audit table in the PR |
| W5-03 | patient-profissao-ui | DONE (#510, closed-already-shipped, docs-only) | **Q-W5-6 resolved PRESENT (owner ruling + machine evidence):** prod deployment `dpl_AWKNbRzyTgvSGHVg31fXNgqFMwXL` builds `9f5c960` = exact main tip (zero drift); form field unconditional (patient-form.tsx 147-153, column 0022); profile row conditional-on-value by design (page.tsx:119), the likely QA source. Zero code |
| W5-04 | episodio-filter | DONE (#512, 2026-07-08) | migration-free; Episodio dropdown scoped to the selected patient + Sem episodio. Composed with W5-02: the merged `PatientEpisodeFields` uses the async Combobox patient picker and filters episodes off that selection; `listEpisodesForPicker` returns `{id,patientId,title}` |
| W5-05 | lote-date-edit | DONE (#511, 2026-07-08) | migration-free; per-row editable DATES in Agendar lote via reused `DatePicker`; reuses `batchSchedule` (no server-action change); DST-safe startsAt recompose |
| W5-06 | equipa-gerir-centering | DONE (#520, 2026-07-08) | migration-free; Gerir panel is now a centered modal on the reused `useAnimatedDialog` hook (no new packages/ui primitive); zero logic change (staff.ts untouched); UI-STYLE.md extended with the modal pattern |
| W5-07 | camera-anexos-buttons | DONE (#513, 2026-07-08) | migration-free; two primary actions (Tirar foto, Transferir) + Abrir; first-open error root-caused (getUserMedia async race under StrictMode double-mount leaking the camera -> NotReadableError -> false denied) and fixed with `startCameraCancellable` + regression tests; W4-05 zero-gallery preserved |
| W5-08 | patient-delete-password-gate | DONE (#514, 2026-07-08) | migration-free; NET-NEW `hardDeletePatient` with the W3-06 scrypt gate (shared delete-password secret) + clinical-records-linked refuse guard + audit + RLS isolation test; soft-delete stays the default |

### Batch 2 - migration-free (run in numeric order)
| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W5-09 | marcacoes-tab-edit | DONE (#525, 2026-07-09) | migration-free; per-row reschedule/estado/cancel on the profile Marcacoes tab, reusing Agenda actions; axes never collapsed; primary-only |
| W5-10 | documentos-upload | DONE (#524, 2026-07-08) | migration-free; upload on the Documentos tab reusing the attachments infra; HALT if a patient-document relation needs a schema change |

### Batch 3 - MIGRATIONS, strictly sequential (one in flight), live-apply verification before DONE
| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W5-11 | referral-source | DONE (#526, migration 0033 live-applied, 2026-07-09) | **migration 0033** (head is 0032): `patients.referral_source` nullable; "Como nos conheceu?" dropdown; genuine net-new column; live-apply before DONE; sequential (lands before W5-12) |
| W5-12 | therapist-blocks | DONE (#527, migration-FREE on time_off; 0034 NOT created, 2026-07-09) | **RECON MISMATCH (Q-W5-7): `time_off` already exists (migration 0006) and models both modes; migration 0034 likely UNNECESSARY.** Default = build migration-FREE on `time_off` (admin UI + integrate into `getTherapistAvailability`/lote exclusion; warn-not-cancel, Q-W5-4). A minimal 0034 only if the owner confirms a needed column. Relates to Q-V2W2-1 (blocked-time band). Migration disposition resolved BEFORE any migration |

### Batch 4 - Ficha Medica, per `docs/design/SPEC-ficha-medica.md` (authoritative; must merge first; run in numeric order)
| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W5-13 | ficha-unification | DONE (#528, 2026-07-09) | single Ficha Medica template; retire others from creation; existing records untouched; **compatibility test posts `template=osteopathy` + the twelve keys -> correct draft** (PRODUCT halt if a key cannot map); migration-free (seed + `form_templates`) |
| W5-14 | ficha-structure | DONE (#529, 2026-07-09) | SPEC sec 3-5: full field sequence, read-only patient header strip, auto creation timestamp, Problemas de Saude 4-col grid restructure (fix the orphaned-render bug), Outros rules; migration-free |
| W5-15 | mobilidade-component | DONE (#530, 2026-07-09) | SPEC 5.10-5.13: three-circle Mobilidade Activa(dot)/Passiva(star) widget + Limpar + Observacoes, Testes Neurologicos/Especiais, Diagnostico, Tratamento (keep Plano+Objectivos, Q-W5-2), Observacoes; migration-free |
| W5-16 | ficha-signature-consent | DONE (#531, 2026-07-09) | SPEC sec 7: canvas signature -> Documentos (attachments infra), Gerar PDF A4 + logo (RGPD), Consinto block with explicit check/X; all consent/RGPD wording PENDENTE-JP (Max drafts 2-3 variants, Q-W5-3); migration-free |
| W5-17 | revisao-consulta-flow | DONE (#532, closes core wave scope, 2026-07-09) | **closes the core wave scope:** Assumir opens the AI draft inside the Ficha Medica editor with the twelve AI-filled fields visible+editable; edit+complete+sign; `record_status` and `ai_review_state` stay separate; signed record in Registos clinicos; migration-free |

> **WAVE 05 CLOSED - 2026-07-09.** All 17 loops merged (W5-01..W5-17). One migration this wave: **0033** (referral_source, live-applied, verified; head 0033, mirror 34/34). **0034 NOT created** - W5-12 was migration-free on `time_off`. Ficha Medica unified on `osteopathy` v3 (identity ingestion mapping, zero external change). Consent wording still PENDENTE-JP (JP pick pending). See DECISIONS 2026-07-09 for the full close + carried owner items.

## Wave 05 Hotfix Loop Queue

> Added 2026-07-09 (YELLOW authoring). **Wave 05 Hotfix scope** - six residual items from the owner QA pass on the deployed app plus clinic-staff feedback, scoped as loops W5-18..W5-23 under `docs/loops/wave-05/`, authoritative design in `docs/design/SPEC-ficha-medica.md` **AMENDMENTS 2026-07-09** (rulings A/B/C/D/E). Status `OPEN` = loop file authored + committed in this authoring PR, awaiting owner merge + dispatch. Run order below is authoritative. Recon verdicts recorded at authoring (in each loop's Field 1): **W5-15 mobilidade EXISTS-AND-MOUNTED** (W5-20 is a conformance pass, not a build); **0031 contraindication flags are DEDICATED BOOLEAN COLUMNS** (W5-21 carries migration 0034); **template display name lives in `form_templates.title`** with zero hardcoded TSX (W5-23 is a live-DB seed re-upsert). Standing rules apply: pt-PT UI copy; SYNTHETIC-DATA-ONLY; one migration in flight; `db-tests.yml`/`e2e.yml` = automatic owner hold; never self-merge.

| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W5-18 | login-redesign-v2 (`docs/loops/wave-05/W5-18-login-redesign-v2.md`) | DONE | migration-free; rebuild the `/login` VISUAL layer to a split-screen brand + form (the shipped W5-01 is below standard); tagline "Gestao clinica, simplificada."; 44px inputs, focus rings, sage primary button states; verify app-shell logo large + crisp; ZERO auth-logic/route/field-name change; preserve/on-branch-update `auth.setup.ts` login helper |
| W5-19 | ficha-sequence-and-outros (`docs/loops/wave-05/W5-19-ficha-sequence-and-outros.md`) | DONE | migration-free; AMENDMENTS rulings B/C/D: remove the Data do Episodio input + wire the read-only created-at display, enforce the authoritative field sequence, rename+restructure Problemas de Saude -> "Outros" (grid + unlabeled free-text placeholder). Recon: order for fields 2-13 ALREADY matches; the two deltas (no-date, Outros) are the work. Twelve AI keys frozen; W5-13 compat stays green |
| W5-20 | mobilidade-delivery (`docs/loops/wave-05/W5-20-mobilidade-delivery.md`) | DONE | migration-free (default); AMENDMENTS ruling E. **Recon: EXISTS-AND-MOUNTED** at sequence position 10 - CONFORMANCE pass, not a build. Add reference spokes, min-44px toggle, "Inserir marcador" arm step, record-wide "Limpar marcadores", helper copy; prove place-and-persist + read-only. Keep the shipped per-circle persistence keying unless zero stored data + owner prefers the flat list |
| W5-21 | pacemaker-contraindication (`docs/loops/wave-05/W5-21-pacemaker-contraindication.md`) | DONE | **MIGRATION 0034 (recon-confirmed).** 0031 flags are DEDICATED BOOLEAN COLUMNS, so add `patients.contraindication_pacemaker`; add "Portador de pacemaker" to the new-patient + edit form, the profile, and the (non-blocking) NESA booking warning (`scheduling/nesa.ts` enum). Head is 0033; one migration in flight; fetch-and-fast-forward before live-apply; live-apply verify before DONE. NESA warning never blocks (unchanged). **LIVE-APPLY VERIFIED 2026-07-11 (PR #538 merged):** `drizzle-kit migrate` applied 0034 on the dev DB (fetch-and-ff first); `information_schema` shows `patients.contraindication_pacemaker` = boolean, NOT NULL, default false; drizzle applied-migration count = 35 (head 0034). |
| W5-22 | marcacao-to-patient-link (`docs/loops/wave-05/W5-22-marcacao-to-patient-link.md`) | DONE | migration-free, read-only navigation; add a "Ficha do paciente" button in the Agenda marcacao EDIT view -> `/patients/{patientId}`; second link when Paciente 2 (`patientTwoId`) is set. No data change. E2E asserts the button lands on the correct patient profile |
| W5-23 | ficha-rename-clinica (`docs/loops/wave-05/W5-23-ficha-rename-clinica.md`) | DONE | **LIVE-DB DATA (recon-confirmed).** Display name lives in `form_templates.title` (zero hardcoded TSX), so rename "Ficha Medica" -> "Ficha Clinica" via a seed edit to `osteopathy-v3.json` + upsert re-run against the dev DB (fetch-and-fast-forward first; paste before/after row). DISPLAY NAME ONLY - key `osteopathy` + `template=osteopathy` + the twelve AI keys FROZEN; W5-13 compat stays green; existing records keep their stored titles. **SEED VERIFIED 2026-07-11 (PR #540 merged):** the dev DB had NO osteopathy v3 row (deploy/seed pipeline had not applied it) - before: v1/v2 only. Scoped seed upsert INSERTED osteopathy v3 with `title.pt "Ficha Clínica"` / `title.en "Clinical Record"`; v1/v2 (referenced, immutable) left untouched. **Finding:** the full `seed:form-templates` loader was NOT run - a dry-run showed it would UPDATE the referenced v1/v2 (title+schema drift vs on-disk JSON) plus ficha_geral/nesa/physiotherapy; that rewrite is out of W5-23 scope and a rule-#5 concern - flagged for the owner, seed scoped to v3 only. |

> **RECON at authoring - note vs the brief's shorthand:** the brief refers to the frozen key as "osteopathy-v2"; the accurate active identity is key `osteopathy` **v3** (the shipped unified template, titled "Ficha Medica" today), with v2 immutable for records that reference it. The rename target (W5-23) and the field-sequence base (W5-19) both operate on v3. No scope change - recorded for accuracy (AMENDMENTS ruling A).

## Wave 05 Ficha Final Loop Queue

> Added 2026-07-11 (YELLOW authoring). **Wave 05 Ficha Final scope** - the three residual ficha deltas from the owner's read-only layout extraction of the legacy Fisiozero episode form, scoped as loops W5-24..W5-26 under `docs/loops/wave-05/`, authoritative design in `docs/design/SPEC-ficha-medica.md` **AMENDMENTS 2026-07-11** (rulings F/G/H). Status `OPEN` = loop file authored + committed in this authoring PR, awaiting owner merge + dispatch. Run order below is authoritative. **All three loops are migration-free** (recon-confirmed at authoring). Recon verdicts (each loop's Field 1): **Outros seed order ALREADY equals the legacy 4x5 layout** one-for-one (W5-24 is a renderer-only presentation pass, no template touch); **bodychart markers have NO per-type differentiation today** (one identical magenta dot; W5-25 is a real render build + a UI-STYLE marker-palette extension); **the record `data` column is jsonb so a marker `intensity` attribute needs NO migration** (W5-26 stores EVA additively). Standing rules apply: pt-PT UI copy; SYNTHETIC-DATA-ONLY; no migration this batch (a marker-storage-needs-migration finding in W5-26 is a HALT, not an improvisation); `db-tests.yml`/`e2e.yml` = automatic owner hold; never self-merge.
>
> **CLOSED 2026-07-14 (YELLOW close-out).** All three Ficha Final loops merged: W5-24 (#548), W5-25 (#549), W5-26 (#556, rebuild). Statuses flipped OPEN -> DONE below. These rows were stale-OPEN on the board though the work had merged; corrected here alongside the FF2 close-out.

| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W5-24 | outros-grid-conformance (`docs/loops/wave-05/W5-24-outros-grid-conformance.md`) | DONE (#548) | migration-free; AMENDMENTS ruling F. Render the Outros checkboxes as the legacy **4-col x 5-row grid** with the `other` free-text pulled INLINE as the 20th grid cell (placeholder "Outras..."), strict 4-up desktop / 2-up collapse. **Recon: the v3 seed `health_problems` property order ALREADY equals the 4x5 reading order one-for-one** - renderer-only, NO template edit. Presentation only; stored values (nineteen booleans + `other`) map unchanged; W5-13 compat stays green. DoD: pasted 20-cell rendered cell-order audit, stored-values-unchanged test on an existing draft |
| W5-25 | bodychart-marker-legend (`docs/loops/wave-05/W5-25-bodychart-marker-legend.md`) | DONE (#549) | migration-free + template-free; AMENDMENTS ruling G. Give the nine marker types a **unique shape + unique color** (shape carries meaning, never color alone) plus an **always-visible legend**; extend UI-STYLE.md with a nine-entry AA-checked marker palette (the token palette lacks nine hues; magenta reserved). **Recon: NO per-type differentiation today** (one identical magenta dot; type only in the hover tooltip). Render-only in `BodyChart.tsx`; existing stored markers render with the new visuals automatically (type-driven); marker array shape `{marker_type,x,y,view}` unchanged. DoD: nine-distinct E2E/DOM assertion, legend present, pre-existing-draft markers render |
| W5-26 | pain-scale-eva (`docs/loops/wave-05/W5-26-pain-scale-eva.md`) | DONE (#556) | migration-free; AMENDMENTS ruling H. On placing a **Local da dor** (`pain_location`) marker, show a **0-10 EVA selector** (min 44px) and store the value as an additive `intensity` jsonb key on that marker; show it in the marker list ("Local da dor - EVA 7/10") and on the chart; editable draft / read-only signed; optional per marker; other types unaffected. **Recon: `data` is jsonb so `intensity` needs NO migration** (Path A component-side passthrough preferred; Path B = `osteopathy` v4 seed if the save path strips undeclared keys - never edit v3, rule 5). **FLAG:** if W5-26 recon finds marker storage cannot carry `intensity` without a migration, that is a HALT. Composes with W5-25 on `BodyChart.tsx` (run after / rebase). DoD: place-select-reload persistence E2E, other-types-unaffected, signed read-only |

## Wave 05 candidates

> Added 2026-07-07 at Wave 04 close. UNORDERED, **candidates only — NOT committed scope**, no lane/gate/loop assignments until a Wave 05 planning pass scopes them. Standing rules still apply when any of these becomes a loop: pt-PT UI copy; all build/dry-run work SYNTHETIC-DATA-ONLY (real-data go-live separately gated); `db-tests.yml`/`e2e.yml` = automatic owner hold, never self-merged; one migration in flight at a time.

### Carried forward (not taken in Wave 04)
| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| SMS confirmation BUILD | DECISIONS 2026-07-06 (product calls) + SPEC-sms-confirmation | product calls made (single 24h reminder, no at-booking msg, per-patient staff-toggle opt-out reusing `patients.reminder_sms_enabled`); still GATED on the **Twilio new-vendor + EU-residency/DPA** decision (OPEN, QUESTIONS); JP picks the final pt-PT wording from **Max's drafted variants** |
| Finance KPI report scoping | DECISIONS 2026-07-02 (VAT: CIVA art. 9 exemption) | VAT gate cleared (gross=final); scope as its own loop; internal KPI only, not fatura-recibo |
| Fisiozero patient import build | BACKLOG Wave 03/04 candidates | mapping + reconciliation on dev SAMPLE data only; live import gated on the pre-real-data gates; collision-HALT policy on file (DECISIONS 2026-07-02) |
| Portal V2 build | BACKLOG Wave 03/04 candidates | GATED — pending JP's reaction to Max's mockups; no scope until provided |
| 30-day stub cleanup job | DECISIONS 2026-07-06 (visitor stub retention) | scheduled job cleans never-promoted stub patients after 30 days; preserves real/promoted patients and Max's real therapist accounts |
| Merge-patients function | Roadmap (STATE 2026-06-30) | `patients.merged_into_id` pointer exists; function is roadmap, deferred |
| CI db-gate hardening | QUESTIONS 2026-07-03 | touches `.github/workflows/db-tests.yml` → automatic OWNER HOLD, never self-merged; opens a PR and HALTS for owner merge |
| Preview DB isolation | QUESTIONS 2026-07-03 | future infra, part of the separate-prod-project work |
| Legacy doc-shelf consolidation loop | QUESTIONS 2026-07-03 | migrate still-open legacy `docs/QUESTIONS.md` + `docs/DECISIONS.md` items onto the canonical `docs/design/` shelf, leave pointer stubs; its own docs loop |
| Dangling-branch pruning pass | BACKLOG Wave 03 candidates | sweep merged/abandoned feature + docs branches and their worktrees from Waves 01–04; housekeeping only |

### New from Wave 04
| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| Cross-browser E2E infra hardening | GREEN closing report 2026-07-07 | the **non-required** Cross-browser E2E lane is chronically red from (a) a webkit/Next "Failed to fetch RSC payload" flake across unrelated specs and (b) a transient Playwright browser-install failure (`packages.microsoft.com` apt InRelease invalid). Pin browser deps / cache to make it reliable. Non-blocking (lane is not required) |
| Agenda: render a secondary-therapist appointment under BOTH therapist columns | DECISIONS 2026-07-06/07 (secondary participants) | explicitly deferred out of W4-19 — renders under the PRIMARY column only + `+1` badge today. Dual-column rendering is the recorded follow-up; scope later if the clinic wants it. (Related: edit-secondary via `updateAppointment` — W4-19 capture is create-only by design) |
| Dev-tenant locked-residue revisit | DECISIONS 2026-07-07 (W4-11 Option B) | the 49 locked/signed synthetic clinical_records + 31 pinned patients + 5 deactivated fixtures left by Option B. **Tied to the pre-real-data gate (prod Supabase project split), NOT a standalone wave item** — a one-time guarded op only if/when the owner rules to defeat rule-4/rule-6 in a controlled window |

## Wave 05 Ficha Final 2 (FF2) Loop Queue

> Added 2026-07-12 (YELLOW authoring). **Wave 05 Ficha Final 2 (FF2) scope** - the owner-approved FF2 package: the ficha resequence to the new owner-defined canonical order, Fisiozero-exact bodychart glyphs, UI renames, per-ficha delete/annul, Declaracao de Presenca printing, Equipa location filter, final consent texts, and the in-app recording guide. Scoped as loops W5-27..W5-34 under `docs/loops/wave-05/`; authoritative design in `docs/design/SPEC-ficha-medica.md` **AMENDMENT 2026-07-12 (FF2): canonical sequence v4**. Status `OPEN` = loop file authored + committed in this authoring PR, awaiting owner merge + dispatch. **Every loop starts from fresh `origin/main` after the prior loop's merge, never stacked.** W5-13 `ficha-medica-compat.test.ts` runs in every loop touching the ficha or templates. **Merge policy (owner amendment 2026-07-12):** all eight loops permit GREEN self-merge once ALL required checks are green AND all three Vercel deployment checks (osteojp-api, osteojp-platform, osteojp-portal) are green; the cross-browser E2E lane is non-required and ignored; W5-27 seed + W5-30 migration 0035 live-apply evidence pasted before merge regardless. Standing rules apply: pt-PT UI copy; SYNTHETIC-DATA-ONLY; one migration in flight (0035, W5-30); the twelve AI keys frozen.
>
> **CLOSED 2026-07-14 (YELLOW close-out).** All eight FF2 loops merged and staff-verified: W5-27 (#559), W5-28 (#560), W5-29 (#561), W5-30 (#562), W5-31 (#563), W5-32 (#564), W5-33 (#565), W5-34 (#566). Statuses flipped OPEN -> DONE below. Full summary: `docs/status/2026-07-14-ff2-report.md`.

| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W5-27 | ficha-resequence-v4 (`docs/loops/wave-05/W5-27-ficha-resequence-v4.md`) | DONE (#559) | migration-free; AMENDMENT FF2. NEW `osteopathy` **v4** seed realizing the FF2-A canonical order (positions 0-17), removing `linked_appointment` (Marcacao respectiva) + Testes Neurologicos (FF2-B). Peso+Altura thin card under the Paciente card; Alertas+CID one row; renderer section order + in-ficha left nav follow v4; new fichas open v4; v1/v2/v3 render original. `created_at` read-only; titles unchanged. **Reconcile with a W5-26 Path-B v4 if present** (FF2 v4 is authoritative; retain any `bodychart.intensity`). Scoped live-DB v4 insert (executor, fetch-and-ff, verified). Twelve AI keys frozen; W5-13 compat 3/3 |
| W5-28 | bodychart-fisiozero-glyphs (`docs/loops/wave-05/W5-28-bodychart-fisiozero-glyphs.md`) | DONE (#560) | migration-free + template-free; render-only in `BodyChart.tsx`. Re-draw the nine marker glyphs to **Fisiozero-exact** (triangle / X-hatch / crosshatch / diagonal-fill ellipse / lightning / target circle / dotted ellipse / CW arrow / CCW arrow) - **supersedes ruling-G shapes**; KEEP W5-25 colors + legend and W5-26 EVA logic. Rodica visual-verifies post-deploy. Depends on W5-25 + W5-26 merged. `BodyChartEva` + W5-13 compat green |
| W5-29 | ui-renames (`docs/loops/wave-05/W5-29-ui-renames.md`) | DONE (#561) | migration-free; i18n display-strings only (both locales). Inicio tile "Registo clinico" -> "Ficha Clinica"; button -> "Nova ficha clinica" (en "Clinical Record" / "New clinical record"); strip user-visible "v3"/version suffixes next to Ficha Clinica. Internal template keys (`osteopathy-v2`/`v3`/`v4`) + code identifiers untouched; i18n keep-both on rebase; JSON.parse both files in the gate; W5-13 compat green |
| W5-30 | ficha-delete-annul (`docs/loops/wave-05/W5-30-ficha-delete-annul.md`) | DONE (#562) | **MIGRATION 0035** (`record_annulments`, append-only, RLS fail-closed). Password-gated hard delete for draft/AI-pending fichas (reuse W5-08/W3-06 scrypt gate); password-gated "Anular" for signed fichas INSERTs an annulment row (locked row never touched; **immutability trigger untouched**); ANULADO badge + "Mostrar anulados" toggle. Head 0034, one migration in flight, fetch-and-ff, cloud live-apply verified before DONE. **Also apply pending 0034 + 0035 to LOCAL 127.0.0.1 Supabase** (closes local drift blocking therapist E2E). RLS + append-only + audit tests same PR; W5-13 compat green |
| W5-31 | declaracao-presenca (`docs/loops/wave-05/W5-31-declaracao-presenca.md`) | DONE (#563) | migration-free, no schema change, no new vendor (reuse W5-16 pdf-lib). Patient profile Documentos button "Imprimir Declaracao de Presenca"; dialog selects a marcacao (prefills date/hora inicio/fim, all editable) or manual entry; Fisiozero-template PDF (logo, title, two verbatim paragraphs, "{localidade}, {dia}" per-location with tenant-default fallback, conditional stamp/signature slot, tenant-sourced responsavel line). Correct pt-PT accents |
| W5-32 | equipa-location-filter (`docs/loops/wave-05/W5-32-equipa-location-filter.md`) | DONE (#564) | migration-free, presentation-only. Administracao/Equipa: add the **same Agenda location select** (Todas as localizacoes / OsteoJP (CB) / OsteoJP (LV)) immediately right of the existing search bar (`apps/web/app/admin/staff/page.tsx`); `?location=` filters team rows by assigned location; default Todas; composes with `?q=` search (AND) |
| W5-33 | consent-texts-final (`docs/loops/wave-05/W5-33-consent-texts-final.md`) | DONE (#565) | migration-free, template-free. Replace every `PENDENTE-JP` placeholder with the final owner-delegated texts (SPEC APPENDIX B / loop) verbatim, no rewording: TEXT 1 ficha Consinto, TEXT 2 RGPD block + RGPD PDF, TEXT 3 Iniciar consulta recording step. pt-PT authoritative, faithful en-GB; **zero em/en dashes**; remove Q-W5-3 markers; W5-13 compat green |
| W5-34 | guiao-in-app (`docs/loops/wave-05/W5-34-guiao-in-app.md`) | DONE (#566) | migration-free, no vendor. Iniciar consulta recording screen gets a collapsible read-only panel "Guiao do Exame Subjetivo" rendering `docs/clinical/guiao-exame-subjetivo.md` as structured static content (sections collapsible), **collapsed by default, never overlapping the recording controls**; pt only. Depends on the guiao doc merged |

## Wave 06 Loop Queue (Melhorias)

> Added 2026-07-14 (YELLOW authoring). **Wave 06 Melhorias scope** - owner-approved 2026-07-14: two bug fixes, invite-email + self-service profile, an agenda deep-link, a deleted-patients management view, an owner-only Estatisticas section, and a design pass. Scoped as loops W6-01a..W6-06b under `docs/loops/wave-06/`. Status `OPEN` = loop file authored + committed in this authoring PR, awaiting owner merge + dispatch. **Every loop starts from fresh `origin/main` after the prior loop's merge, never stacked.**
>
> **Owner execution-order ruling (2026-07-14):** bug loops first, design pass last. Order: **W6-01a, W6-01b, W6-02, W6-03, W6-04, W6-05, W6-06a, W6-06b.** W6-01a runs before W6-04 (the ficha-delete bug blocked W6-04's flow). W6-06 runs last; it is **split into W6-06a (audit + color-equity palette plan) then W6-06b (implementation)** for a safer palette rollout (owner-authorized split; scope unchanged, W6-06b depends on W6-06a merged).
>
> **Merge policy (owner amendment 2026-07-12, standing):** GREEN self-merge once ALL required checks (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deploys (osteojp-api, osteojp-platform, osteojp-portal) are green, read from the checks API not the banner; the cross-browser lane no longer exists. **No migration expected this wave**; W6-04 MAY surface one - if so it becomes OWNER-MERGE with live-apply evidence, one migration in flight, fetch-and-fast-forward first (HALT to convert). Workflow files never touched. W5-13 `ficha-medica-compat.test.ts` runs in every loop touching the ficha or templates. JSON.parse both i18n files in every gate. HALT-LOUD on scope/product/data/reality mismatch; immutability-bypass claims escalate instantly. Standing rules apply: pt-PT UI copy; SYNTHETIC-DATA-ONLY.

| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W6-01a | ficha-delete-bugfix (`docs/loops/wave-06/W6-01a-ficha-delete-bugfix.md`) | DONE (#571) | **BUG, diagnosis-first.** Password-gated ficha delete errors ("there is an error") on synthetic patients paol/paul. Mandatory read-only recon + recorded root-cause finding BEFORE any fix; candidates: scrypt gate / RLS / draft-vs-signed Anular routing / `record_annulments` (0035) FK. Deliver root cause + fix + regression test + distinct pt-PT messages (wrong password vs signed-record annul-only vs server error). Signed fichas stay annul-only; immutability trigger + append-only NEVER touched. Migration-free (a schema/FK-defect fix = HALT to follow-up). **Runs FIRST (blocks W6-04).** W5-13 compat green |
| W6-01b | service-filters-data-driven (`docs/loops/wave-06/W6-01b-service-filters-data-driven.md`) | DONE (#572) | **BUG, migration-free.** Marcacoes service filter shows a hardcoded 5-entry list (`SERVICE_LABEL`/`SERVICE_TINT` in `marcacoes-view.tsx`/`agenda-grid.tsx`) not matching tenant services. Make ALL service dropdowns data-driven from `listServices(actor)` (`lib/admin/services.ts`); sweep Marcacoes/Agenda/Faturacao filters + appointment-creation forms. Owner ruling: **filters include inactive** services (historic marcacoes ref them, e.g. NESA); **creation dropdowns show active only**. Preserve accent tint. Regression test asserts options are DB-sourced |
| W6-02 | invite-email-and-profile (`docs/loops/wave-06/W6-02-invite-email-and-profile.md`) | DONE (#573) | migration-free expected. Wire the real Convidar invite email via **Resend** (in-stack; reuse the `apps/web/app/auth/update-password` set-password landing); **keep the temp-password fallback** on send failure. Add a self-service profile route for **every role, own account only** (server-scoped to actor): edit own name/contact + change own password. **Env-var HALT class:** if a Vercel env var is the only blocker, HALT with the EXACT name + target project, owner sets it, loop resumes. Never invent a key; never print a secret. Audited |
| W6-03 | agenda-deeplink-patient (`docs/loops/wave-06/W6-03-agenda-deeplink-patient.md`) | DONE (#574) | migration-free, navigation-only. "Nova marcacao" on a patient profile (`patients/[id]/page.tsx`, today a bare `<Link href="/agenda">`) deep-links into Agenda with the create drawer OPEN and THAT patient preselected + **locked**; user picks only therapist + date/time. Deep-link param -> `agenda-view.tsx` autopen + seed the create `ModalState` patient (reuse the edit-mode combobox value/label path); clear param after open. Rationale: Rodica disambiguates same-name by NIF, must not re-search. Normal create unchanged |
| W6-04 | deleted-patients-management (`docs/loops/wave-06/W6-04-deleted-patients-management.md`) | DONE (#575) | **RECON-FIRST.** Owner-gated "Pacientes eliminados" view under Administracao: list soft-deleted (`patients.deletedAt`) + duplicate-marked (`mergedIntoId`) rows with NIF, Restore action (reuse existing path), permanent delete kept restricted to no-associated-data behind the scrypt gate. **Recon at authoring: the soft-delete columns already exist -> migration-free expected.** If recon finds NO true soft-delete state -> HALT-LOUD with a proposed model. If a migration surfaces -> OWNER-MERGE, live-apply evidence, one in flight, fetch-and-ff. **Runs strictly AFTER W6-01a merged.** Owner-gated route + query |
| W6-05 | estatisticas-owner-only (`docs/loops/wave-06/W6-05-estatisticas-owner-only.md`) | DONE (#576) | migration-free expected (aggregate queries). New **Estatisticas** nav + route, **Proprietario (`owner`) only**, enforced route-level AND query-level (not nav hiding) via `packages/auth/permissions.ts` (`can`/`owner`). MVP revenue + volume KPIs from invoicing (`apps/web/app/invoicing`) + `analytics_events` (0025) + appointments; filters date/therapist/location/service; breakdowns per therapist/location/service/period; counts + utilization; at least one polished chart. No new data capture; a new charting vendor = owner-confirmable (QUESTIONS). Role-scoped RLS need = HALT |
| W6-06a | design-audit-palette (`docs/loops/wave-06/W6-06a-design-audit-palette.md`) | DONE (#577) | **DESIGN PASS (audit), docs-only.** `/ui-ux-pro-max` MANDATORY. Produce a prioritized platform-wide visual-audit inventory (patient section first: Registos stray line, weak Documentos) + the **color-equity palette plan**: owner ratio **55% white+grey / 25% cyan (accent-2 `#45B9A7`) / 20% purple (accent-1 `#8B1863`)** mapped to token usage per surface, with an **AA proof per purple pairing**, an explicit **bodychart marker-token exclusion** (W5-25/W5-28, `globals.css`), and the proposed UI-STYLE.md 7 wording. Canonical hexes unchanged (`tokens.test.ts` stays green). **Runs LAST, before W6-06b** |
| W6-06b | design-implementation (`docs/loops/wave-06/W6-06b-design-implementation.md`) | DONE (#578) | **DESIGN PASS (impl), migration-free.** `/ui-ux-pro-max` MANDATORY. Implements the W6-06a plan: (1) fix patient profile tabs (Registos stray line, strengthen Documentos), (2) patient-section dashboards/layouts, (3) apply the 55/25/20 palette equity platform-wide + update UI-STYLE.md 7. AA preserved; canonical hexes fixed; **bodychart `marker-*` tokens untouched**; W5-13 compat green for ficha-touching changes. **Runs LAST, after W6-06a merged** |

> **Wave 06 owner visual QA (deployed app, 2026-07-14).** All eight loops merged (#571-#578, close-out #579), but the owner ran visual QA on the deployed app with these verdicts. **VERIFIED WORKING:** W6-01a ficha delete, W6-03 agenda deep-link, W6-04 Pacientes eliminados. **ACCEPTED PENDING USE:** W6-05 Estatisticas (accepted, to be confirmed in real use). **FAILED QA -> respecified as Wave 07 Correcoes QA:** W6-02 (Convidar returns the generic "A operacao falhou. Tente novamente." with no temp password - a regression) and W6-06 (Registos clinicos + Documentos tabs judged worse than before, purple not perceptible, the unwanted line still present). W6-01b and W6-06a were not separately QA'd by the owner; they stay DONE. The two failures are re-scoped below in the Wave 07 Correcoes QA queue; the merged W6-02/W6-06 rows keep their DONE (#PR) status (they DID merge) and are superseded by their Wave 07 fixes.

## Wave 07 Loop Queue (Correcoes QA)

> Added 2026-07-14 (YELLOW authoring). **Wave 07 Correcoes QA scope** - the two Wave 06 items the owner FAILED at visual QA on the deployed app (2026-07-14), re-scoped as fresh loops: the Convidar invite regression + a live-send flag decoupling, the missing self-service profile entry point, and a design redo of the rejected W6-06 output. Scoped as loops W7-01..W7-03 under `docs/loops/wave-07/`. Status `OPEN` = loop file authored + committed in this authoring PR, awaiting owner merge + dispatch. **Every loop starts from fresh `origin/main` after the prior loop's merge, never stacked.** This queue is **committed scope** and is distinct from the unordered "Wave 07 candidates" section below (which remains candidates-only, not scoped).
>
> **Owner execution-order ruling (2026-07-14):** **W7-01, W7-02, W7-03 (design LAST).** Splits may be refined; scope never.
>
> **Merge policy (Wave 07 Correcoes QA):** **W7-01 and W7-02** = GREEN self-merge once ALL required checks (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deploys (osteojp-api, osteojp-platform, osteojp-portal) are green, read from the checks API not the banner. **W7-03 = OWNER VISUAL GATE (new standing rule, born from the W6-06 QA failure):** all checks green is NECESSARY but NOT sufficient - GREEN pushes the branch, pastes the osteojp-platform Vercel PREVIEW URL + the surfaces to inspect, and HALTs; the owner reviews the preview and merges himself; GREEN NEVER self-merges W7-03; a rejected preview loops back with owner notes. **No migration expected this wave** (all three are code/presentation-only); any surfaced migration flips that loop to OWNER-MERGE with live-apply evidence, one migration in flight, fetch-and-fast-forward first (HALT to convert). Workflow files never touched. W5-13 `ficha-medica-compat.test.ts` runs in every loop touching the ficha or templates. JSON.parse both i18n files in every gate. HALT-LOUD on scope/product/data/reality mismatch; immutability-bypass claims escalate instantly. Standing rules apply: pt-PT UI copy; SYNTHETIC-DATA-ONLY.

| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W7-01 | invite-regression-flag-decouple (`docs/loops/wave-07/W7-01-invite-regression-flag-decouple.md`) | DONE (#581) | **BUG, diagnosis-first (W6-02 regression).** Convidar novo membro returns the generic `admin.staff.error` ("A operacao falhou. Tente novamente.") with NO temp password (pre-W6 was `inviteEmailFailed` + a temp-password fallback). Mandatory read-only recon + recorded root-cause finding BEFORE any fix; PRIME candidate: `provisionStaffUser` (`apps/web/lib/auth/provision.ts`) throws when a required env is absent, re-thrown at `inviteStaff` (`apps/web/lib/admin/staff.ts`) and coerced to `code:"error"` at `inviteAction` (`apps/web/app/admin/staff/actions.ts`) before the temp-password result. Required post-fix: (a) Resend env absent -> temp-password success with a clear "email not sent" message, (b) env present -> real send, (c) runtime send failure -> graceful temp-password fallback; never the generic failure. **Flag decoupling:** new `INVITES_LIVE_SEND` gating invites only, independent of `REMINDERS_LIVE_SEND` (reminders unchanged). Document the live-invite env set (names only) in the loop + QUESTIONS (relate to Q-W6-02-1). Regression tests: env-absent, send-failure fallback, env-present mocked, flag independence. Migration-free. **Runs FIRST.** |
| W7-02 | profile-page-reachability (`docs/loops/wave-07/W7-02-profile-page-reachability.md`) | DONE (#582) | **RECON-FIRST (W6-02 reachability).** Owner cannot find any profile page on the deployed app. Recon FIRST whether PR #573 shipped it and at what route (**authoring found `apps/web/app/perfil/` present + `profile.*` i18n**, so the likely defect is reachability, not absence). If it exists but has no entry point: add a visible **"O meu perfil"** entry in the top-right user-menu area (beside name/role + Terminar sessao, shell in `packages/ui/src/components/AppShell.tsx`/`SidebarAppShell.tsx`) linking to `/perfil`, for every role. If it does NOT exist: build it per the original W6-02 spec (all roles, own account only, edit own name + change own password, email read-only, audited). **Do not add a phone column (Q-W6-02-2) or email-change (Q-W6-02-3).** E2E: every role reaches its own profile, edits it, cannot reach another account's. Migration-free. **Runs SECOND.** |
| W7-03 | design-redo-empty-state-purple (`docs/loops/wave-07/W7-03-design-redo-empty-state-purple.md`) | DONE (#583) | **DESIGN PASS, OWNER-MERGE ONLY (owner visual gate), migration-free.** Owner REJECTED W6-06 (#577/#578). `/ui-ux-pro-max` MANDATORY (paste invocation evidence). **Audit phase first** (committed per-surface plan: current state/change/rationale). **Mandatory removal:** the decorative azulejo motif band above the empty-state icon (`HeritageBand` via `EmptyState heritage`, `packages/ui/src/components/`) removed platform-wide wherever it renders (Registos "Sem registos clinicos" `patients/[id]/page.tsx`, Documentos "Sem documentos" `PatientDocuments.tsx`, patient-list first-run, etc.); empty states become icon/title/subtitle/action only. Redesign Registos clinicos + Documentos tabs (layout + hierarchy); restyle and contain the Acoes destrutivas card (`patients/_components/patient-actions.tsx`) in place - **any relocation is a PRODUCT question for owner ruling, never self-decided.** Purple mandate: perceptible accent-1 `#8B1863` on active tab indicator, selected states, secondary accents, Estatisticas chart accents, toward 55/25/20; AA preserved; canonical hexes unchanged (`tokens.test.ts` green); **bodychart `marker-*` untouched.** **OWNER VISUAL GATE: checks green is necessary but NOT sufficient - GREEN pushes, pastes the osteojp-platform PREVIEW URL + surface list, HALTs; owner merges. GREEN NEVER self-merges.** **Runs LAST.** |

> **Wave 07 Correcoes QA CLOSED (2026-07-15, YELLOW).** All three loops merged to main in board order: W7-01 invite regression + `INVITES_LIVE_SEND` decouple (#581), W7-02 self-service profile reachability (#582), W7-03 design redo - ornament removed, tabs rebuilt, purple made perceptible, OWNER VISUAL GATE (#583). The per-loop execution decisions are recorded in DECISIONS.md. This close-out was folded into the Wave 08 authoring PR: the three rows were flipped OPEN -> DONE here after `gh pr view` confirmed all three MERGED (the code merged 2026-07-14 but the board was never flipped and no close-out commit existed; the briefing-vs-reality gap is recorded in the Wave 08 authoring DECISIONS entry).

## Wave 08 Loop Queue (Dados e KPI)

> Added 2026-07-15 (YELLOW authoring). **Wave 08 Dados e KPI scope** - owner-approved: staff phone + job-title fields, the real services catalog with per-location pricing + a pack model + catalog seed, an Estatisticas/Indicadores (KPI) split with recharts, and a read-only portal-completion recon. Scoped as loops W8-02, W8-01a/b/c, W8-03, W8-04 under `docs/loops/wave-08/`. Status `OPEN` = loop file authored + committed in this authoring PR, awaiting owner merge + dispatch. **Every loop starts from fresh `origin/main` after the prior loop's merge, never stacked.**
>
> **Owner execution-order ruling:** **W8-02, W8-01a, W8-01b, W8-01c, W8-03, W8-04.** W8-02 runs FIRST (small; it unblocks the owner's manual staff-data entry). W8-01 is split a/b/c (schema+seed, admin UI, booking); the split refines the headline loop, scope unchanged. W8-03 and W8-04 run after W8-01. Splits may be refined; scope never.
>
> **AUTHORING RECON CORRECTIONS (ground truth outranks the briefing):** (1) **Per-location pricing already exists on main** - `service_location_prices` (`schema.ts:276-304`, migration `0007`) + a full price resolver/setter + an admin price form. W8-01a EXTENDS it (offered-only-where-priced + packs), it does NOT re-create it. (2) **The dev seed has no duplicate Fisioterapia row** (5 clean services); any duplicate is a LIVE-catalog condition handled by rename-not-recreate. (3) For W8-03, **`patients.city` exists** so "Top localidades dos utentes" IS buildable; **reminder/SMS sends are NOT persisted** so "Evolucao do envio de SMS" is excluded and recorded in QUESTIONS. See the W8-01a loop file's AUTHORING RECON CORRECTION block and the Wave 08 authoring DECISIONS entry.
>
> **Merge policy (Wave 08 Dados e KPI):** **Default = GREEN self-merge** once ALL required checks (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deploys (osteojp-api, osteojp-platform, osteojp-portal) are green, read from the checks API not the banner. **Exceptions:** every MIGRATION loop is **OWNER-MERGE with live-apply evidence** - **W8-02** (`0036`) and **W8-01a** (`0037`); **W8-01a additionally carries a CATALOG OWNER CONFIRMATION HALT** before the narrow cloud catalog write. **W8-03 is the OWNER VISUAL GATE** (standing rule for visual-heavy loops since W7-03): all checks green is necessary but NOT sufficient; GREEN pushes, pastes the osteojp-platform PREVIEW URL + surface list, and HALTs for the owner to merge. **One migration in flight** (W8-02 `0036` then W8-01a `0037`, strict sequence, fetch-and-fast-forward before any live-DB op, fresh `origin/main` after each merge, never stacked). Workflow files never touched. JSON.parse both i18n files in every gate. Standing rules apply: pt-PT UI copy; SYNTHETIC-DATA-ONLY (the single owner-confirmed catalog cloud write in W8-01a is the sole exception); DISPOSABLE test patients only, never Maria Joao Silva, reference therapist Tiago Reis. HALT-LOUD on scope/product/data/reality mismatch; immutability-bypass claims escalate instantly.

| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W8-02 | staff-phone-job-title (`docs/loops/wave-08/W8-02-staff-phone-job-title.md`) | DONE | **DONE 2026-07-15: merged #585 (@c167874, owner-merged), 0036 applied live via manual drizzle-kit (session 5432) + verified (users.phone + users.job_title present, nullable, 0 backfill on 19 prod rows). MIGRATION, OWNER-MERGE, recon-first. Ran FIRST** (unblocks manual data entry). Add two NULLABLE columns to `users` (`schema.ts:190-213`, no phone/job_title today) in ONE migration (`0036`, both migration dirs + snapshot): a phone number + a professional job title (Fisioterapeuta/Osteopata/..., a DISPLAY field, never the auth role in `permissions.ts:10`). Surface both editable in Administracao > Equipa (`apps/web/app/admin/staff/`: `StaffManageModal.tsx` edit + `page.tsx` list) and job title on the identity render points where sensible; extend `editStaff`/`StaffMember`/`listStaff` (`apps/web/lib/admin/staff.ts`). Ships EMPTY; owner enters values manually after merge. Phone is PII, never logged. Live-apply evidence before merge |
| W8-01a | services-catalog-packs-schema-seed (`docs/loops/wave-08/W8-01a-services-catalog-packs-schema-seed.md`) | DONE | **DONE 2026-07-15: merged #587 (@0f04e3c, owner-merged), 0037 applied live via manual drizzle-kit + verified (service_packs + patient_pack_instances, RLS on cloud). Owner-confirmed catalog seeded (Option A amended): 22 canonical / 23 prices / 14 packs / 3 frozen legacy (total 25); "1ª Avaliação" renamed, Osteopatia+Fisioterapia kept (refs intact), Pilates Terapêutico/NESA/Massagem Terapêutica DEACTIVATED pending JP batch. MIGRATION, OWNER-MERGE + CATALOG OWNER CONFIRMATION HALT. Ran after W8-02 (`0036`) merged.** Per-location pricing ALREADY EXISTS (`service_location_prices`, `0007`) - EXTEND, do not re-create. Net-new: (a) the offered-only-where-priced semantic on the existing model (no destructive change to `services.price_cents` without an owner HALT), (b) the PACK model (`0037`: pack definitions + per-patient instances, each table with tenant_id + RLS + isolation test). Seed the owner's full LV+CB catalog (embedded verbatim in the loop file, money in cents): dry-run local `127.0.0.1` with row counts -> CATALOG OWNER CONFIRMATION HALT (final table) -> one authorized narrow cloud write. Renames on canonical `services` rows (UPDATE name), NEVER delete-recreate; historic `appointments.serviceId` intact. RPG migrates as a real service; "campo 9 - Externos" dropped. Catalog gaps -> QUESTIONS. Pack no-show/under-24h = MANUAL enforcement, platform never auto-charges |
| W8-01b | admin-servicos-prices-packs-ui (`docs/loops/wave-08/W8-01b-admin-servicos-prices-packs-ui.md`) | DONE | **DONE 2026-07-16: merged #589 (@f88e692, GREEN self-merge, all checks + 3 Vercel deploys green). Migration-free (0 files under migrations/workflows). Pack CRUD (create/edit/archive/restore/reference-guarded-delete) wired to the W8-01a `packs.ts` layer; base-service creation dropdown active-only; packs table status filter includes inactive (Todos/Ativos/Inativos); light "Oferecido aqui/Não oferecido aqui" affordance on the price editor via `listServiceOfferings`. 23 i18n keys both files (parity), pack-filters unit tests + admin-packs E2E green.** migration-free, GREEN self-merge. Runs after W8-01a (`0037`) merged. Administracao > Servicos (`apps/web/app/admin/services/`, reuse - do not fork) manages per-location prices (offered-only-where-priced) + pack definitions (base service + session count + pack price + location + active/inactive). Preserve the existing service active/inactive semantics + the W6-01b split (filters include inactive, creation active-only) and apply it to packs. No per-patient instances here. Money in cents. If a schema change surfaces -> HALT (no migration here) |
| W8-01c | booking-packs (`docs/loops/wave-08/W8-01c-booking-packs.md`) | DONE | **DONE 2026-07-16: merged #590 (@20b08c5, GREEN self-merge, all checks + 3 Vercel deploys green). Migration-free — no appointment->instance FK (the appointment records the pack's base service; the session is registered/decremented in the same createAppointment tx). Create drawer offers active packs as a "Pacote" type (location-scoped, active-only); register (consume 1) / decrement (never below 0) with a remaining banner; patient-profile manual consume/restore (audited, never a charge). instances-core pure resolvers unit-tested; booking-packs E2E (register->decrement->surfacing->adjust). Fix in-flight: `revalidateTag(tag,"max")` defers invalidation -> swapped to `updateTag` for immediate agenda-cache busting (new packs visible in the drawer at once).** migration-free, GREEN self-merge. Runs after W8-01a (`0037`) merged (may follow W8-01b). Appointment creation (Agenda create drawer + `getAgendaOptions`) offers active packs as bookable types; booking a pack with no active instance registers one (10 or 5) + consumes session 1, else decrements + shows remaining (no negative balances); a staff manual consume/restore adjust control handles the under-24h rule (audited, NEVER a charge); agenda + patient profile surface remaining sessions. Regression tests on registration/decrement/adjust; E2E via disposable patients only. If a schema change surfaces -> HALT (no migration here) |
| W8-03 | estatisticas-kpi-split (`docs/loops/wave-08/W8-03-estatisticas-kpi-split.md`) | DONE | **DONE 2026-07-16: OWNER-MERGED #591 (@bbf6e7d) after the OWNER VISUAL GATE (preview inspected + approved). Migration-free. Two-card chooser at /estatisticas; existing dashboard moved UNCHANGED to /estatisticas/painel (only its self-nav target string changed; `bar-chart.tsx` + `estatisticas-view.tsx` render untouched); new KPI section at /estatisticas/indicadores. recharts adopted for the KPI section ONLY (Q-W6-05-1); ten reports built from existing data + Escolher periodo (presets + custom); purple accent-1 ramp with a values table per chart (AA/print-safe); owner gate reused at all 3 routes + nav unchanged (nav-items + tokens tests green). Evolucao do envio de SMS EXCLUDED (no send persistence -> Q-W8-03-1). kpi-transform pure tests + estatisticas E2E green.** migration-free expected, OWNER VISUAL GATE. Runs after W8-01 merged. Estatisticas landing becomes a two-card chooser: "Estatisticas" (existing `estatisticas-view.tsx` dashboard + `bar-chart.tsx` SVG, UNCHANGED) and "Indicadores (KPI)" (new). KPI section = report menu + Escolher periodo (presets + custom), each report a full visual page, ADOPTING recharts (owner-approved, Q-W6-05-1) for this section only. Build the ten reports supported by existing data (Tipos de marcacao donut, Top terapeutas, Evolucao da faturacao +per terapeuta, Distribuicao etaria, Marcacoes diarias, Top 10 utentes por pagamentos/marcacoes, Origem dos utentes 0033, Top localidades `patients.city`). EXCLUDE Evolucao do envio de SMS (no send persistence -> QUESTIONS). Owner-only gate identical to W6-05 (capability+route+query+nav). Purple chart accents per 55/25/20, AA preserved, canonical hexes unchanged, bodychart `marker-*` untouched. GREEN pushes preview URL + surface list, HALTs; owner merges |
| W8-04 | portal-completion-recon (`docs/loops/wave-08/W8-04-portal-completion-recon.md`) | DONE | **DONE 2026-07-16: merged #592 (@0345d96, GREEN self-merge, docs-only). Findings doc `docs/status/2026-07-16-portal-completion-audit.md` — route-by-route status, the half-enforced therapist-visibility gate (availability enforced, 0023 service-map filter NOT enforced in `listOpenSlots`) as the headline finding, env gaps (NEXT_PUBLIC_API_URL absent on Preview in `apiBase()` + `account/actions.ts`), PWA installability-only, + a Wave 09 candidate list. NO live security/data exposure (documents = signed URLs, data = API+Bearer+RLS, `/` redirects anon) so no HALT.** READ-ONLY RECON, NO CODE, GREEN self-merge (docs-only). Runs LAST. Audit `apps/portal` end to end vs a client-facing launch: what exists (route-by-route: auth, dashboard, booking proven on prod ref `9F8F24D0`, appointments, documents, forms, clinics, account; PWA v1 installability #568, no offline by design), what is missing/broken (pending-forms, service->therapist filter deferred 0023, PT-only language), env gaps (NEXT_PUBLIC_API_URL absent on Preview known), and the therapist-visibility gate (no hours = invisible; service-map filter not yet enforced). Output: a findings doc committed to `docs/` + a proposed Wave 09 candidate list for owner + Rodica. No fixes. A live security/data exposure = instant HALT-LOUD |

> **WAVE 08 (Dados e KPI) CLOSED — 2026-07-16.** All six loops merged: W8-02 #585 (`0036`), W8-01a #587 (`0037`, cloud catalog seeded + 3 legacy frozen), W8-01b #589, W8-01c #590, W8-04 #592 (GREEN self-merge each), W8-03 #591 (OWNER VISUAL GATE — owner-merged after preview approval). **Migration head UNCHANGED at 0037** after W8-01a; W8-01b/c/03/04 all migration-free (the wave's single authorized cloud write was spent in W8-01a). One in-flight CI fix: W8-01c's `revalidateTag(tag,"max")` deferred cache invalidation → swapped to `updateTag` (immediate) so newly-created packs appear in the booking drawer at once; two other W8-01c CI-red tests were environment flakiness (green on re-run + locally). QUESTIONS opened/kept: **Q-W8-03-1** (Evolução do envio de SMS excluded — no reminder-send persistence; add a `reminder_sends` log first). Wave 09 candidates seeded by the W8-04 audit (`docs/status/2026-07-16-portal-completion-audit.md`): enforce the 0023 service→therapist booking filter, portal env-validation + `NEXT_PUBLIC_API_URL` Preview fallback, pending-forms surface, per-patient locale switcher, data-driven clinics roster, offline/error resilience, shared portal auth guard, plus the queued Rodica asks (Packs em uso view, Recuperação Utentes report). See DECISIONS.md 2026-07-16.

## Wave 09 Loop Queue (Correcoes CB)

> Added 2026-07-16 (YELLOW authoring). **Wave 09 Correcoes CB scope** - scoped strictly from the Castelo Branco staff-platform QA (Rodica, 2026-07-16: docx "Coisas a Alterar na Nova Plataforma" + WhatsApp + 2 screenshots), owner-triaged into 18 items (full record: `docs/qa/2026-07-16-castelo-branco-qa.md`). Eight loops under `docs/loops/wave-09/`. Status `OPEN` = loop file authored + committed in this authoring PR, awaiting owner merge + dispatch (maps to the board's WRITTEN state; flips to READY when its gate clears, then IN-FLIGHT/DONE per the coordination protocol at the top of this file). **Every loop starts from fresh `origin/main` after the prior loop's merge, never stacked on an owner-held branch.**
>
> **Execution order (strict):** W9-01, W9-02, W9-03, W9-04, W9-05, W9-06, W9-07, W9-08. W9-01 (recon) runs FIRST and its findings resolve the conditional loops (W9-06 migration disposition, W9-07 code-vs-cloud-write path). Splits/refinements are allowed; scope never.
>
> **Merge policy (Wave 09 Correcoes CB):** **Default = GREEN self-merge** once ALL required checks (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deploys (osteojp-api, osteojp-platform, osteojp-portal) are green, read from the checks API not the banner. **Exceptions:** **W9-03** and **W9-05** are the **OWNER VISUAL GATE** (all checks green necessary but NOT sufficient; GREEN pushes the osteojp-platform PREVIEW URL + surface list and HALTs; owner merges). **W9-06** is **CONDITIONAL** on W9-01 (e): migration-free GREEN self-merge if `created_by` is present, else migration-gated OWNER-MERGE with live-apply evidence (one migration in flight, `0038`). **W9-07** is **CONDITIONAL** on W9-01 (a): code fix = GREEN self-merge, else any cloud data write HALTs to the mailbox for explicit owner authorization (the cloud DB is read-only; Wave 08's single authorized write is spent). **W9-08** is **OWNER-MERGE** (standing agent-governing skill files, same class as workflow files). **W9-01** is docs-only GREEN self-merge. Migration head is `0037`; only W9-06 may author one (`0038`) and only if W9-01 (e) requires it. Workflow files never touched. The three frozen legacy service rows (Pilates Terapeutico 40.00 / NESA 39.00 / Massagem Terapeutica 50.00) stay frozen. Standing rules apply: pt-PT UI copy (both i18n files, JSON.parse both); plain hyphens, no em/en dashes; SYNTHETIC-DATA-ONLY for verify; DISPOSABLE test patients only, never Maria Joao Silva, reference therapist Tiago Reis. HALT-LOUD on scope/product/data/reality mismatch; immutability-bypass claims and any live portal privacy exposure escalate instantly.

| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W9-01 | recon-cb (`docs/loops/wave-09/W9-01-recon-cb.md`) | DONE | **DONE 2026-07-17: merged #595 (@`022a31c`, GREEN self-merge, docs-only). Findings `docs/recon/W9-01-findings.md` - six questions answered with file/line + read-only cloud evidence; BOTH conditionals resolved: (e) `created_by`+`created_at` PRESENT and populated on every staff create path -> W9-06 MIGRATION-FREE self-merge, head stays 0037 (nuance: portal bookings set `created_by` NULL deliberately, `store.ts:433`); (a) NESA already selectable at CB, all three hypothesised causes refuted -> W9-07 needs NO cloud write and has no code fix as scoped. (c) NO portal notes exposure -> no HALT. Five corrections to the starting map, three changing downstream scope (booking dropdown never reads `service_location_prices`; strikethrough ALREADY = cancelled; the pdf-templates HTML is dead docs; the agenda has no therapist columns; the declaracao 'manual option' is not a download toggle). Nine questions filed with recommended defaults.**  READ-ONLY RECON, NO CODE, NO DB WRITE, docs-only GREEN self-merge. Runs FIRST. Outputs `docs/recon/W9-01-findings.md` answering six questions with file/line or read-only-query evidence: (a) why NESA is absent from the CB booking dropdown (service + `service_location_prices` CB row state, active flags, offered-only-where-priced filter; frozen legacy rows untouched); (b) the agenda card visual-state -> lifecycle/confirmation axis mapping (what strikethrough is bound to today; axes LOCKED, display-layer only); (c) whether the portal exposes marcacao notes/comments anywhere (API responses included) - a live exposure is a HALT; (d) the Declaracao logo + carimbo asset pipeline (per-location assets? why the logo is absent? where auto-download vs preview is decided); (e) whether `appointments` carries `created_by` + `created_at` - **if absent, W9-06 becomes migration-gated (say so explicitly)**; (f) the root cause of the agenda location filter leaking LV therapists. Resolves the conditional loops |
| W9-02 | agenda-location-filter (`docs/loops/wave-09/W9-02-agenda-location-filter.md`) | DONE | **DONE 2026-07-17: merged #597 (@`286bd63`, GREEN self-merge, migration-free). E2E green in CI (3/3 ran on the seeded DB); web suite 137 files / 1165 passed.**  migration-free, BUG FIX, GREEN self-merge. Consumes W9-01 (f). **W9-01 (f) found the feature was ABSENT, not broken: the therapist dropdown had NO location predicate at all (`data.ts:189-194` = every active non-reception user, tenant-wide), so CB listed LV therapists.** Fix: `getAgendaOptions(ctx, locationId?)` narrows the dropdown via `listTherapistLocationAssignments` (same WHERE clause as the W4-12 `getTherapistLocationIds`, kept in lock-step to avoid the drift W9-01 flagged); pure decision in `therapist-location-filter.ts`; `Todas as localizacoes` restores all. **OWNER POLICY RULING 2026-07-17 (resolves Q-W9-01-2, overrides GREEN's recommended default): therapists with NO location assignment appear ONLY under `Todas as localizacoes`, never inside a specific location view; a thin CB list is accepted short-term, owner data entry populates it.** Corroborated at execution: W5-32 already ships this exact policy on the Equipa list (`app/admin/staff/page.tsx:55-57`), so the agenda now matches a Wave 05 behaviour. **Scope correction (W9-01 (f)): the "rendered columns" half of the DoD is NOT satisfiable - the grid renders DAY columns (`agenda-grid.tsx:245`), not therapist columns; appointments are already location-filtered by `listAppointments`.** Changing location also clears a now-invalid therapist filter (stale-state guard). `marcacoes` + `estatisticas/painel` deliberately unchanged (out of scope; optional param makes them a one-liner later). Display/query layer; grid, day/week toggle, appointment count, booking untouched. QA item 1 |
| W9-03 | declaracao-presenca-fixes (`docs/loops/wave-09/W9-03-declaracao-presenca-fixes.md`) | DONE | **DONE 2026-07-17: owner-merged #599 (@`5950a25`) after the visual gate (owner inspected a CB + an LV declaration on the preview). Per-location carimbo (CB blank, never the LV stamp), the real logo now embedded (pdf-lib embedPng of the canonical `icon-512.png` mark, replacing W5-31's rectangle stand-in), download -> inline preview. Q-W9-01-7 (manual-path stamp fallback) left open as a follow-up.** **W9-03b DONE 2026-07-17: owner-merged #602 (@`de7e2b0`) at the same visual gate - the owner-supplied canonical logo (`Logotipo_OsteoJP_2023.jpg`, full lockup) replaced the icon-512 stand-in, embedded via pdf-lib embedJpg. CB carimbo still pending (its own micro follow-up W9-03c on arrival); the CB stamp area stays blank.** | **EXECUTED 2026-07-17, pushed for the owner visual gate.** All three defects fixed migration-free, no content changed: (1) CARIMBO per-location - `signatureStampBytesForLocation` keys on `normalizeLocationKey`; LV keeps its stamp (renamed `signature-stamp-linda-a-velha.png`), CB resolves to a defined-empty slot = BLANK area, NEVER the LV carimbo (the erro grave dies); the model now carries location identity via `resolveStampLocationKey`, which `generate.ts` previously resolved then discarded. (2) LOGO - **W9-01 (d)'s 'no PNG logo exists' was superseded at execution: `apps/portal/public/icon-512.png` IS the canonical brand mark** (verified visually, 512x512 RGBA), so per the ruling it was wired (inlined `clinic-logo-asset.ts`, pdf-lib `embedPng`), replacing W5-31's hand-drawn rectangle stand-in - no blank logo slot needed. (3) DOWNLOAD - dropped the `{ download }` option on `createSignedUrl` so Storage serves inline; both paths preview. Rendered CB+LV PDFs locally and inspected: LV = real logo + LV carimbo; CB = real logo + BLANK stamp - matches the ruling's gate criteria. **Q-W9-01-7 STILL OPEN:** the manual path sets locationId=null -> falls back to the tenant-default location's stamp (now LV again after the 5a re-activation); adding a manual-path location selector was not scoped (item 12 editable fields are JP-gated), recorded as a follow-up. Tests: 28 declaracao unit (incl. per-location carimbo: CB blank, never LV) + 3 action unit (no `download` option, both paths). **OWNER VISUAL GATE: owner inspects a CB and an LV declaration on the preview, then merges.** QA item 2 | **UNBLOCKED 2026-07-17 by owner ruling (item 2) with a BLANK-STAMP default; runs AFTER W9-04 per the authorized resequencing (item 3).** W9-01 (d) found NO CB carimbo exists in the repo (exactly one stamp: the LV/Fisiozero block, resolved with no location parameter), which would have forced a Field 6 HALT. Ruling: build per-location asset RESOLUTION keyed on `normalizeLocationKey` - LV keeps its existing stamp; CB renders a clean BLANK stamp area with a defined asset slot, so **the erro grave (LV stamp on CB documents) dies immediately with no image needed** (the blank area is the already-supported `signatureStamp: false` path). Logo: wire from a canonical app logo asset **if one exists in the repo** - W9-01 (d) established the committed brand assets are SVG and pdf-lib embeds only PNG/JPEG (hence W5-31's hand-drawn rectangle stand-in), so if none exists, place the slot and leave it blank; do not fabricate. CB carimbo + logo files are incoming from clinic staff; when the owner pastes local paths, commit into the slots inside W9-03 if still open, else a micro follow-up PR carrying the same OWNER VISUAL GATE. **Gate inspection (per ruling): a CB declaration shows a blank stamp and NEVER the LV stamp; an LV declaration is unchanged, plus the logo state as built.** Also in scope per W9-01 (d): the forced download is ONE option on ONE call (`createSignedUrl(..., { download })`, `declaracao-actions.ts:45-47`) - dropping it yields inline; the "manual option" is NOT a download toggle but the "Introducao manual" dropdown entry, and BOTH paths force the same download, so there is no preview option today to fix. `docs/pdf-templates/declaration-presenca.html` is DEAD DOCS (renderer is pdf-lib) and is marked deprecated in the docs delta. Q-W9-01-7 (how the manual path, which sets `locationId = null`, picks a stamp) remains OPEN. | migration-free expected, **OWNER VISUAL GATE**. Consumes W9-01 (d). Fix the CB "erro grave": per-location carimbo (a CB declaration carries the CB stamp, derived like the localidade line already is, Q-W5-10), the clinic logo renders (print-branding rule), and the manual option opens a preview instead of forcing an auto-download. Document CONTENT unchanged (JP-approved defaults + Q-W5-11 responsavel preserved; editable fields are item 12, JP-gated, out of scope). Do NOT invent a stamp asset (Q-W5-9 relation -> HALT if missing). Owner inspects a CB and an LV declaration on the preview before merge. QA item 2 |
| W9-04 | blocked-time-agenda (`docs/loops/wave-09/W9-04-blocked-time-agenda.md`) | DONE | **DONE 2026-07-17: merged #598 (@`793dfff`, GREEN self-merge, migration-free). E2E green in CI (3/3 ran on the seeded DB, incl. disabled-slot non-bookable proof); web suite 138 files / 1181 passed. Owner ruling item 1: band scope option (A) accepted, disabled-slot accessibility approved, option (C) summary marker registered as a Wave 10 candidate. Closed Q-V2W2-1.** migration-free, DISPLAY GAP, GREEN self-merge. **RESEQUENCED ahead of W9-03 per the owner ruling 2026-07-17** (the CB carimbo + logo assets were still absent at W9-02's merge, so W9-03 waits for them; this loop ran off fresh `origin/main` containing W9-02, not W9-03). Blocked therapist time now renders on the agenda in a muted, non-interactive hatched band (SPEC-v2-agenda 2.1), **closing Q-V2W2-1** (deferred in V2-W2 for want of a data model - the model has existed since 0006). Reuses the existing `time_off` read: the half-open overlap predicate was EXTRACTED (`blockOverlapsRange`, `day-availability.ts`) and is composed by BOTH the booking availability read and the new agenda band read, so the two cannot diverge (the loop's own Field 5 restriction). **Non-bookable is enforced by `disabled` slot buttons, not a pointer-events overlay** - an overlay alone would still let a keyboard user tab in and press Enter. **SCOPE RESOLVED - owner ruling 2026-07-17 item 1: option (A) ACCEPTED; the accessibility approach (disabled slot buttons, no pointer-events overlay) is owner-approved; option (C) registered as a Wave 10 candidate, option (B) declined** (question was `~/osteojp-mailbox/inbox/W9-04-SCOPE-blocked-band-therapist-axis-2026-07-17.md`): `time_off` is per-therapist but the grid has DAY columns and NO therapist axis (W9-01 (f), the same false premise as W9-02's "rendered columns"), so a full-width band is only truthful when the agenda is scoped to ONE therapist - built for that case; under `Todos os terapeutas` no band is drawn, because claiming the clinic is blocked when one therapist is away would suppress real bookable time. Per the ruling the gap is accepted as VISIBILITY only - W5-12 already refuses a blocked slot at booking time, so safety is intact. No change to the W5-12 block admin UI, to `getTherapistAvailability` booking behaviour, or to lote exclusion. QA item 3 |
| W9-05 | agenda-cards-v2 (`docs/loops/wave-09/W9-05-agenda-cards-v2.md`) | DONE | **DONE 2026-07-17: owner-merged #600 (@`4f3c23b`) after the visual gate. Therapist name + deterministic per-therapist colour (stripe + dot, reused -700 tokens, tokens.test.ts green); same-hour overlap keeps the patient name legible; cancelled suppresses the confirmation tick (strikethrough stays cancelled, no remap). DESIGN RULING at the gate: card body stays service-tinted, therapist identity = stripe+dot+name; whole-card therapist colouring is a REGISTERED Wave 10 fallback only if CB staff QA rejects the stripe.** | **EXECUTED 2026-07-17, pushed for the owner visual gate.** Consumed W9-01 (b). Items 7/8/5 in one display-layer pass on the card, migration-free: (7) therapist NAME on every card + deterministic per-therapist colour (FNV-1a hash of therapist id -> 7-hue palette in `lib/scheduling/therapist-color.ts`, REUSING existing `-700` tokens so no hex drift and `tokens.test.ts` stays green; colour is a left spine + dot, name is authoritative, colour never the only cue); (8) same-hour overlap keeps the patient name legible (patient name always rendered, service line dropped first; therapist required on every card so not height-gated); (5) OWNER RULING 2026-07-17 item 4: strikethrough STAYS bound to cancelled (no remap - W9-01 (b) found it was already correct); the cancelled state SUPPRESSES the confirmation tick so a cancelled+confirmed card never shows both. UI-STYLE.md extended with the per-therapist palette. Tests: 8 unit + 7 component (rendered to markup) + 1 E2E. **Gate NOTE:** the card keeps its service BODY tint (SPEC §2.1); if the owner wants the whole card per-therapist coloured instead, that is a gate-time spec ruling. **OWNER VISUAL GATE: owner inspects the agenda (a day with same-hour overlaps, multiple therapists, a cancelled appointment) on the preview, then merges.** QA items 5, 7, 8 | migration-free, DISPLAY LAYER, **OWNER VISUAL GATE**. Consumes W9-01 (b). Therapist name on every card; deterministic per-therapist colour meeting AA (name is authoritative, colour reinforces, never colour alone; canonical hexes never drift, `tokens.test.ts` green); same-hour overlap keeps the patient name legible (Fisiozero-style compact cards); cancelled-vs-confirmed corrected so **strikethrough = cancelled** (display remap, dual axes LOCKED, zero data change). QA with disposable patients only, never Maria Joao Silva; reference therapist Tiago Reis. QA items 5, 7, 8 |
| W9-06 | marcacao-audit-notes (`docs/loops/wave-09/W9-06-marcacao-audit-notes.md`) | DONE | **DONE 2026-07-17: merged #601 (@`784e3ff`, GREEN self-merge, migration-free). created-by/created-at (portal bookings = "Reserva online (portal)"), staff-side note hover card on the agenda card + marcacoes row (item 9 premise was inverted then corrected by owner ruling - a feature request, not a leak), and the automated portal notes-privacy guard test (item 6). Q-W9-01-4 reconciled + closed; standing "return to the source language" lesson in DECISIONS.** | **EXECUTED 2026-07-17, migration-free GREEN self-merge (W9-01 (e): `created_by` present, head stays 0037).** (10) created-by + created-at on the marcacao detail (drawer) + the marcacoes list, creator name via an aliased `users` LEFT join; NULL createdBy (portal bookings) renders **"Reserva online (portal)"** per the owner ruling, never blank (Q-W9-01-4 reconciled + closed). (9) **PREMISE INVERTED then corrected by owner ruling:** CB reported notes were NOT visible on hover (must open the marcacao) and requested staff-side hover display - a feature, not a leak. Built a staff-side note hover card on the agenda card + the marcacoes row (content on hover AND keyboard focus; note-presence icon so it is never hover-only). (6) automated portal guard test (`apps/api/.../notes-privacy.test.ts`) asserting the portal AppointmentView envelope never carries notes (fichas-style `not.toContain`). Tests: 4 api guard + 10 component (agenda note hover, marcacoes created-by + note, drawer created-by) + 1 E2E (staff-created provenance end-to-end). QA items 6, 9, 10 | **CONDITIONAL per W9-01 (e)**: migration-free GREEN self-merge if `created_by` present, else migration-gated OWNER-MERGE (one migration `0038`, live-apply verified before DONE, RLS/isolation in the same PR). Created-by + created-at on the marcacao detail + list; internal notes/historico moved into a contained staff-side hover card (not an always-on hover leak); an automated GUARD TEST that the portal API/UI never exposes marcacao notes to a patient (item-6 invariant; a live exposure is a HALT). QA items 6, 9, 10 |
| W9-07 | nesa-catalog-fix (`docs/loops/wave-09/W9-07-nesa-catalog-fix.md`) | DONE | **DONE 2026-07-17 (owner ruling): CLOSED as NO-DEFECT. No code, no cloud write, no migration - docs delta only.** The conditional resolved to a THIRD path neither branch anticipated. Per W9-01 (a) (`docs/recon/W9-01-findings.md`, #595 @ `022a31c`), NESA is ALREADY selectable at CB: the booking dropdown filters `services.is_active` ONLY and never reads `service_location_prices` (`apps/web/lib/scheduling/data.ts:200-209`, so the offered-only-where-priced premise was FALSE and hypothesis (i) is refuted, not unconfirmed); the canonical CB NESA (`270fb115`) is `is_active = true` with an ACTIVE CB price row at 5000 (hypothesis (ii) refuted); and the sole CB-assigned therapist has ZERO `therapist_services` rows so the drawer's client-side narrowing is bypassed (`appointment-drawer.tsx:457-465`, empty mapping = show all by design). **Cause: the QA observation predates the W8-01a seed (#587), which merged the SAME day (2026-07-16) and created the canonical active CB-priced NESA row; before it, the only NESA was the frozen legacy row (39.00, inactive), correctly hidden.** Owner ruling 2026-07-17: do NOT make the booking dropdown price/location-aware this wave - a strict filter today would HIDE services CB performs but lacks price rows for, colliding with the open JP catalog-gaps ruling (`docs/QUESTIONS.md:546-571`); registered instead as a Wave 10 candidate gated on that ruling. Frozen legacy rows confirmed untouched (all three `is_active = false`, read-only). Residual recorded (Q-W9-01-1): if item 4 meant the PORTAL, it is still live and IS a code fix (`BOOKABLE_SERVICE_NAMES` excludes NESA, `apps/api/lib/appointments/services.ts:39-44`) - QA doc says staff-platform, so the staff reading governs. QA item 4 |
| W9-08 | project-skills (`docs/loops/wave-09/W9-08-project-skills.md`) | DONE | **DONE 2026-07-18: owner-merged #603 (@`d9358bb`), docs-class OWNER-MERGE. Both skill files committed: `.claude/skills/osteojp-design/SKILL.md` (accurate 55/25/20 equity - cyan primary, purple secondary - AA + colour-not-only, agenda/card/table/badge conventions, empty-state rules, bodychart shape-not-colour + always-visible-legend locks) and `.claude/skills/osteojp-conventions/SKILL.md` (loop discipline + the two Wave 09 lessons: flip the board row on every loop close; return to the reporter's source language before concluding a QA reporter is wrong). Standing agent-governing files, owner-merge-only hereafter (same class as workflow files).** DOCS-CLASS, **OWNER-MERGE**. Runs LAST. Creates `.claude/skills/osteojp-design/SKILL.md` (locked visual system: accurate 55/25/20 equity - cyan primary, purple secondary; AA minimums + colour-not-only; agenda/card/table/badge conventions; empty-state rules; bodychart shape-not-colour + always-visible-legend locks) and `.claude/skills/osteojp-conventions/SKILL.md` (loop discipline: loop files outrank session prompts, i18n JSON-pair SPOF + JSON.parse gate, required-checks-from-the-API not the banner, halt-loud over improvisation, immutability never defeated, done-is-a-number/file/exit-code). Frontmatter `description`s state precisely when each applies. Mirrors committed authority (UI-STYLE.md, W6-06 plan, CLAUDE.md, board protocol), invents nothing. After this loop the skill files are owner-merge-only, never touched by a wave loop (same class as workflow files) |

> **WAVE 09 (Correcoes CB) CLOSED - 2026-07-18.** All eight loops DONE (authoring #594, then W9-01 recon #595, W9-07 NO-DEFECT close #596, W9-02 location filter #597, W9-04 blocked time #598, W9-03 declaracao fixes #599, W9-05 agenda cards #600, W9-06 marcacao audit + notes #601, W9-03b declaracao logo #602, W9-08 project skills #603 - PR range #594-#603). **Zero migrations the entire wave; head stayed 0037, exactly as W9-01 predicted** (`created_by`/`created_at` already present, so W9-06 was migration-free). Three owner-visual-gate passes (#599 W9-03, #600 W9-05, #602 W9-03b) and one owner-merge (#603 W9-08); one authorized resequence (W9-04 merged ahead of W9-03 because the CB carimbo + logo assets were still absent, recorded in the #598 PR body). W9-07 closed as NO-DEFECT: the QA observation predated the W8-01a catalog seed (#587, same day), so NESA was already selectable at CB by the time the loop ran. Two standing follow-ups carried: **W9-03c** (commit the CB carimbo asset on arrival from clinic staff, same pattern + owner visual gate as W9-03b) and **Q-W9-01-7** (declaracao manual-path stamp fallback). Two wave lessons encoded in `.claude/skills/osteojp-conventions/SKILL.md` (#603): flip the board row on every loop close; return to the reporter's source language before concluding a QA reporter is wrong. Wave 10 candidates consolidated below. See DECISIONS.md 2026-07-18.

## Wave 10 Loop Queue (Dados Reais e Isolamento)

> Added 2026-07-20 (YELLOW authoring). **Wave 10 Dados Reais e Isolamento scope** - scoped strictly from the owner brief 2026-07-20 (the clinic starts real usage): two data/isolation mandates plus one UX mandate. (1) **Purge ALL synthetic patient-domain data** from the cloud DB - every patient, appointment, and ficha clinica is test data; the 19 users, the service catalog, packs, locations, and settings are real and stay. (2) **Lock the platform down per-person:** a therapist logs in and sees only themself, their own patients, their own location, with no location or therapist switching; only admin and owner keep cross-visibility. (3) **Agenda cards show the patient name first, with a rich hover popup.** Five loops under `docs/loops/wave-10/`. Status `OPEN` = loop file authored + committed in this authoring PR, awaiting owner merge + dispatch (maps to the board's WRITTEN state; flips to READY when its gate clears, then IN-FLIGHT/DONE per the coordination protocol at the top of this file). **Every loop starts from fresh `origin/main` after the prior loop's merge, never stacked on an owner-held branch.**
>
> **This is the COMMITTED Wave 10 scope, distinct from the "Wave 10 candidates" section below** (which stays an UNORDERED, unscoped backlog, unchanged - the headline schedule-model redesign candidate is NOT in this wave). Two candidates relate to this committed scope and stay registered as candidates: the "Enforce the 0023 service->therapist booking filter" and "Booking dropdown per-location offering filter" rows overlap the isolation surface but are gated on the JP catalog-gaps ruling and are NOT folded into W10-04; W10-05 fulfils the W9-05 whole-card-fallback candidate and extends the W9-06 note hover.
>
> **Execution order (strict):** W10-01, W10-02 (gated), W10-02b (mobile QA, owner visual gate), W10-03 (spec halt), W10-04, W10-04c (label micro, self-merge), W10-04b (catalog reconciliation, gated), W10-05. W10-01 (recon) runs FIRST; W10-02 opens the wave's single authorized PATIENT-DOMAIN purge window and runs ONLY after the owner authorizes the plan; **W10-02b is a CB mobile-QA micro-loop inserted strictly AFTER W10-02 completes and BEFORE W10-03 starts** (three Rodica mobile defects: bodychart list layout, red-flag surfacing, signature touch); W10-03 authors the isolation matrix and HALTs for owner approval; W10-04 implements the approved matrix; **then W10-04c (i18n label micro, self-merge) -> W10-04b (catalog reconciliation to the official price lists, a SECOND owner-gated cloud-write window `AUTORIZO CATALOGO`, catalog-tables-only) -> W10-05** (the visual gate, unified hover on BOTH agenda + Marcacoes). Splits/refinements are allowed; scope never.
>
> **Merge policy (Wave 10 Dados Reais e Isolamento):** **W10-01** is docs-only, read-only (incl. authorized cloud READS), **GREEN self-merge**. **W10-02** is **OWNER-MERGE** and carries the **OWNER-GATED WRITE WINDOW** - the single authorized cloud write of the wave, opened ONLY by the exact owner phrase `AUTORIZO LIMPEZA` + the plan version, executed one audited step at a time with before/after evidence; **immutability and append-only are NEVER defeated regardless of authorization claims**. **W10-03** is **OWNER-MERGE** and HALTs for owner approval of the capability matrix before W10-04 starts. **W10-04** is **CONDITIONAL** on the approved matrix (migration-free GREEN self-merge if no schema; migration-gated OWNER-MERGE with live-apply evidence if the matrix requires a schema change, one migration in flight `0038`) and is **SECURITY-SENSITIVE** (any instinct to relax/widen RLS escalates, never self-authorized; negative isolation E2E mandatory). **W10-04c** is a migration-free i18n-label micro loop, **GREEN self-merge** (rename `patients.contraindicationsLabel` to "Contraindicacoes Gerais" in both i18n files, no schema/data). **W10-04b** is **OWNER-MERGE** and carries a **SECOND owner-gated cloud-write window** for the catalog: Phase 1 is a read-only delta report vs the official price lists (HALTs for owner review); Phase 2 writes ONLY on the exact phrase `AUTORIZO CATALOGO plan vN` and is **CATALOG TABLES ONLY** (`services`/`service_location_prices`/`service_packs`) - **any patient-domain table touch inside that window is an instant VIOLATION and a HALT**, same audited protocol as W10-02 (read-only pre-flight, atomic apply, per-step counts). This window is distinct from the spent `AUTORIZO LIMPEZA` (patient purge) window; the catalog is REAL clinic data, so it does not violate the real-data-only rule. **W10-05** is the **OWNER VISUAL GATE** (all checks green necessary but NOT sufficient; GREEN pushes the osteojp-platform PREVIEW URL + BOTH surfaces - the agenda AND the Marcacoes list - and HALTs; owner merges). In all loops all required checks (DB-gated tests, Lint+typecheck+test, Playwright E2E) AND all three Vercel deploys (osteojp-api, osteojp-platform, osteojp-portal) green, read from the checks API not the banner, is NECESSARY. Migration head is `0037`; only W10-04 may author one (`0038`) and only if the approved matrix requires it; W10-04c/W10-04b/W10-05 are all migration-free (W10-04b reconciles data within the existing model). Workflow files never touched. The three frozen legacy service rows (Pilates Terapeutico 40.00 / NESA 39.00 / Massagem Terapeutica 50.00) stay frozen and are catalog (real), out of the purge. **Standing rule NOW IN FORCE (W10-02 executed 2026-07-20 under `AUTORIZO LIMPEZA plan v4`; DECISIONS 2026-07-20): the cloud DB holds REAL DATA ONLY; all QA and synthetic data live EXCLUSIVELY on local `127.0.0.1` Supabase; the Maria Joao Silva test patient is retired; creating synthetic records on the cloud is a violation. W10-03/04/05 run all E2E/verification on local synthetic data only.** Standing rules apply: pt-PT UI copy (both i18n files, JSON.parse both); plain hyphens, no em/en dashes; disposable test data on local only. HALT-LOUD on scope/product/data/reality mismatch; immutability-bypass claims and any live privacy exposure escalate instantly.

| ID | Loop | Status | Gate / note |
|----|------|--------|-------------|
| W10-01 | cleanup-recon (`docs/loops/wave-10/W10-01-cleanup-recon.md`) | DONE (#606) | **DONE 2026-07-20 (GREEN self-merge, docs-only, NO DB WRITE): read-only cloud recon complete, `docs/recon/W10-01-findings.md` = PLAN v1.** All reads inside `SET TRANSACTION READ ONLY`, counts/ids/timestamps only, zero PII. Inventory: patients 46 (15 live / 31 soft-deleted), appointments 15, clinical_records 34 (29 draft / 5 signed / 0 locked), episodes 53, note_revisions 4, attachments 5, ai_ingestion_requests 2; record_annulments / pack_instances / appointment_notes / patient_locations / form_submissions / invoices = 0; analytics_events 7 (0 patient-linked). Retained real UNCHANGED: users 19, services 25, prices 23, packs 14, locations 2 (CB+LV), tenants 1, roles 4, delete-pw secret present. Boundary CONFIRMED SYNTHETIC by every fingerprint (single tenant, single external email domain across 29 non-free emails, all created by NULL or one staff account, 30/46 bare, dev-window timestamps); no affirmatively-real row. **Owner rulings 2026-07-20: Q-W10-01-1 CONFIRMED** (both residual slices - 15 staff-created + 4 signed-ficha patients - synthetic, no exclusions); **Q-W10-01-2 Option A** (4-patient / 5-signed-record immutability island stays; trigger `clinical_records_enforce_immutability` enabled throughout, no bypass). Plan check: 3 of 4 blocked patients LIVE (`22ce09b0`/`8cd8c310`/`c53c36aa`; `cae30d86` already soft-deleted) -> **PLAN bumped v1 -> v2** adding step 9, a soft-delete `UPDATE` of the 3 live (patients-row UPDATE only, trigger untouched) so residue is invisible in the normal app. PLAN v2 = children-first direct SQL for the 42 deletable patients + soft-delete of the 3 live blocked, trigger never defeated. **W10-02 preconditions: this PR merged AND owner reply `AUTORIZO LIMPEZA plan v2`** (the wave's only cloud-write window). Original: READ-ONLY RECON incl. authorized cloud READS, NO DB WRITE, docs-only **GREEN self-merge**. Runs FIRST. Inventories the cloud DB patient-domain (row counts by state for `patients` live/soft-deleted, `appointments` by status, `clinical_records` by status + the AI-pending subset + the locked/signed count, plus `record_annulments`, `patient_pack_instances`, note revisions, and every other patient-linked table); confirms the synthetic/real boundary (expected 100 percent of patient-domain rows synthetic; the 19 users, services/prices/packs, locations, tenants, settings real); maps every FK/back-pointer (all `ON DELETE NO ACTION` except `tenant_id`, so a purge is children-first); enumerates the sanctioned delete paths (password-gated `hardDeletePatient`/`hardDeleteAppointment`/`hardDeleteClinicalRecord` - all reference-guarded; `deleteStaffMember`; soft-delete/`restorePatient`/`listDeletedPatients`); and determines EXACTLY what the `clinical_records_enforce_immutability` trigger blocks (BOTH UPDATE and DELETE when `status IN ('locked','signed')`, even for service_role) and what the append-only `record_annulments` policy blocks. Output `docs/recon/W10-01-findings.md` ending in a VERSIONED PROPOSED CLEANUP PLAN (ordered children-first steps, exact expected row count per step, app-path vs direct-SQL per step, and a BLOCKED section listing any locked/signed/annulled ficha residue with owner options). No writes of any kind. Real patient data intermixed = HALT |
| W10-02 | cleanup-execution (`docs/loops/wave-10/W10-02-cleanup-execution.md`) | DONE-EXECUTED (owner-merge pending) | **DONE 2026-07-20: cloud purge EXECUTED under `AUTORIZO LIMPEZA plan v4` (single atomic transaction, per-step evidence, immutability trigger never defeated).** Evidence `docs/recon/W10-02-execution-evidence.md`. Purged D=46 synthetic patients + subtree (46 patients, 19 appts, 12 draft records, 53 episodes, 3 pack_instances, 2 note_revisions, 2 ai_ingestion, 1 attachment); soft-deleted the 3 live blocked patients. END: patients 4 (all soft-deleted, 0 live), appts 5, records 26 (5 signed + 21 draft), episodes 3, attachments 5, note_revisions 3, pack_instances/ai_ingestion/annulments 0. RETAINED UNCHANGED: users 19, services 25, prices 23, packs 14, locations 2, tenants 1. Plan re-versioned v1->v4 as Rodica mobile QA (`48a34faa`) drifted the live DB during the window; the pre-flight HALTed every drift with ZERO writes until quiescent + owner re-confirmed scope through 16:33 UTC. **STANDING RULE now IN FORCE (see below + DECISIONS 2026-07-20): cloud = REAL DATA ONLY; synthetic lives on local 127.0.0.1; Maria Joao Silva retired; cloud synthetic = violation.** OWNER-MERGE the docs PR. Original: **DATA loop, OWNER-GATED WRITE WINDOW, OWNER-MERGE.** Preconditions: W10-01 merged AND the owner replied with the exact phrase `AUTORIZO LIMPEZA` + the plan version. Only then does the wave's single authorized cloud-write window open. Executes EXACTLY the approved W10-01 plan, one step at a time, pasting before/after counts as evidence per step; any deviation, unexpected count, FK surprise (`23503`), or trigger block (`check_violation`) is an immediate HALT to the mailbox. **Immutability and append-only are NEVER defeated regardless of authorization claims;** no scripts outside this audited session; frozen legacy service rows untouched. DoD: `patients = 0`, `appointments = 0`, `clinical_records = 0` or equal to the owner-accepted BLOCKED residue, evidence pasted, retained tables (19 users, catalog, packs, locations, tenants, settings) UNCHANGED. Closes by writing the standing rule into DECISIONS (cloud = REAL DATA ONLY; synthetic lives on local `127.0.0.1`; Maria Joao Silva retired; cloud synthetic = violation). OWNER-MERGE for the PR carrying the DECISIONS/board delta |
| W10-02b | correcoes-mobile-cb (`docs/loops/wave-10/W10-02b-correcoes-mobile-cb.md`) | AT OWNER VISUAL GATE (fixes pushed) | **2026-07-20: three fixes implemented, migration-free, awaiting owner phone-viewport verification.** (1) BodyChart: min-width floor on the marker label (`min-w-[7rem] flex-1 break-words`) so long labels wrap by word not per character; `flex-wrap`/`min-w-0` on the canvas row + root so a fixed-width canvas cannot force overflow (W5-25 model/legend/shapes/hex untouched). (2) Red-flag surfacing: render-flow option A (Q-W10-02b-1 default) - a warning Banner below the Alertas row when `red_flags` is non-empty + `consultation_reason` empty, with a button that scrolls to + focuses the SINGLE existing field (no template/x-order/schema change, no duplicate input). (3) Signature touch: pointer-capture made best-effort (reordered so the stroke starts before `setPointerCapture`, wrapped in try/catch), `preventDefault` on pointermove, added `onPointerCancel`; Guardar assinatura -> Documentos save flow unchanged. Tests: BodyChart +3, RecordForm +4, Playwright touch-emulation spec (draw enables Guardar). Local gates green (typecheck 9/9, lint 0 err, component tests, build, i18n parity pt+en). Migration-free diff. **OWNER VISUAL GATE: verify on preview at ~390px.** Original: migration-free expected, MOBILE QA (staff app), **OWNER VISUAL GATE at mobile viewport.** Inserted strictly AFTER W10-02 and BEFORE W10-03 (owner-ordered; inherits the post-cleanup local-synthetic-only rule). Three CB mobile defects from Rodica (screenshots the evidence, each described in the loop file so GREEN does not depend on the authoring session): **(1)** the ficha editor's **bodychart marker list** (`BodyChart.tsx:413-457`) collapses to one-character-per-line vertical text on phones because the fixed-width canvas (`h-80 w-56 shrink-0`, `:327`) sits side-by-side with the label column - fix responsive stacking + sane min-widths, no per-character wrap; bodychart model/legend/shapes/enum untouched (W5-25 lock, layout only). **(2)** red-flag flow: there is NO red-flag boolean - `red_flags` is a textarea at x-order 4 and the required `consultation_reason` ("Motivos da Consulta / Inicio / Contexto em que ocorre") is at x-order 10 with no surfacing logic; when `red_flags` is non-empty, surface `consultation_reason` immediately via a RENDER-FLOW solution (no `x-order`/template/schema change; v1-v4 never retrofitted; a template version bump is a seed-data fallback with W5-13 compat 3/3 green, a schema migration is a HALT). Interaction shape filed as **Q-W10-02b-1** with a recommended default; loop stays unblocked. **(3)** the signature canvas (`SignatureConsent.tsx`) already uses Pointer Events + `touch-none` (`:221-225`), so the fix is NOT "add onTouchStart" - diagnose the real touch failure (pointer-capture / coordinate-scaling), fix it, keep the Guardar assinatura -> Documentos flow unchanged, add a Playwright touch-emulation spec (draw + save). Any new string -> BOTH i18n files. E2E on local synthetic data only. GREEN halts for owner visual verification on the preview at a phone-width viewport before merge |
| W10-03 | access-isolation-spec (`docs/loops/wave-10/W10-03-access-isolation-spec.md`) | AT OWNER MATRIX-APPROVAL HALT (spec pushed) | **2026-07-21: `docs/design/SPEC-isolation.md` authored + pushed; all facts verified read-only against `origin/main` @ 787a598 (head 0037); NO code, NO cloud read.** Five sections: (1) role model (owner/admin/therapist/reception, `user_role` JWT claim, 26-cap `PERMISSIONS` grid, `can`/`assertCan`); (2) capability/visibility MATRIX (13 surfaces x 4 roles, cells no-access/own-only/location/tenant); (3) server-side-first enforcement plan (narrowing predicates on patients list/detail + fichas + agenda/marcacoes location, tighten the clinical_records RLS therapist branch `0001_rls.sql:174-175`, per-therapist RLS net-new, tenant_id JWT-only, nothing widened); (4) UI consequence (therapist loses BOTH selectors - therapist already gated `agenda-view.tsx:194`, location NEW gate `:211`); (5) open questions Q-W10-03-1..7 (already filed at authoring) restated with defaults. Grounded: **"their patients" has NO data model** (no assignment column, `schema.ts:431-492`; derive from `appointments.practitioner_id`, index `:609` exists) - Q-W10-03-2 sets W10-04's migration disposition (derive = migration-free; explicit column = `0038` gated). **OWNER-MERGE + HALT: owner rules Q-W10-03-1..7 and approves the matrix before W10-04 starts.** Original: **SPEC (docs), OWNER-MERGE, HALT for matrix approval.** Runs after W10-02. Authors `docs/design/SPEC-isolation.md`: the role model grounded in `packages/auth` (`owner`/`admin`/`therapist`/`reception`; JWT claim `user_role`; the `PERMISSIONS` capability grid + `can`/`assertCan`); a capability/visibility MATRIX of surfaces (agenda, patients list, patient detail, fichas, marcacoes, estatisticas, equipa, servicos, acoes destrutivas, portal) x roles with cell vocabulary no-access/own-only/location/tenant; a SERVER-SIDE-FIRST enforcement plan (query scoping by location + therapist identity, RLS stays fail-closed, tenant_id from JWT only, nothing widened - per-therapist RLS is net-new); UI consequences (therapist loses BOTH the therapist selector, already gated, and the location selector, new gate). Grounded fact: **"their patients" has NO data model today** (no assignment column; derive from `appointments.practitioner_id`, index exists) - an OPEN QUESTION, not self-decided. Files the W10-03 open-question set (Rodica's role class, "their patients" definition, reception scope, coverage/substitution, admin location-scope, owner/admin cross-visibility, estatisticas) as pending owner rulings. HALTS for owner matrix approval before W10-04 |
 osteojp-w10-04bc-loops
| W10-04 | isolation-enforcement (`docs/loops/wave-10/W10-04-isolation-enforcement.md`) | OPEN | **IMPLEMENTATION, CONDITIONAL, SECURITY-SENSITIVE.** Runs after W10-03 merged AND the owner approved the matrix. Implements the approved matrix: server-side scoping on every listed surface (patients list/detail + fichas are NOT therapist-scoped today; agenda/marcacoes extend the existing `lockTherapist` path), capability checks, therapist-role removal of the location + therapist selectors, and MANDATORY negative isolation E2E (a therapist account cannot read another therapist's patients or another location's agenda; owner/admin cross-visibility positive control), run on local Supabase with synthetic data. **CONDITIONAL merge:** if the approved matrix needs NO schema, migration-free GREEN self-merge; if it requires schema (assignment column, role/capability column, or per-therapist RLS), migration-gated OWNER-MERGE with manual `drizzle-kit` live-apply verified before DONE (one migration `0038`). **Security-sensitive: any instinct to relax RLS or widen a policy ESCALATES instead of being self-authorized; the isolation only narrows; tenant_id stays JWT-only.** |
| W10-04c | label-contraindicacoes-gerais (`docs/loops/wave-10/W10-04c-label-contraindicacoes-gerais.md`) | DONE (#613) | **DONE 2026-07-21 (GREEN self-merge, i18n-only, migration-free):** `patients.contraindicationsLabel` renamed in both files (`strings.*.json:985`): pt "Contraindicações NESA" -> "Contraindicações Gerais"; en "NESA contraindications" -> "General contraindications". Scoped diff = ONLY that one key on each side; `admin.services.contraindicationSensitive` + the three `patients.fieldContraindication*` keys UNCHANGED; i18n parity pt+en (0 orphans). Two presentational consumers confirmed (`patient-form.tsx:219` legend, `patients/[id]/page.tsx:170` detail row); no test asserted the old literal. Gates green (lint 0 err, typecheck 9/9, web unit 1230, build). No schema/migration/workflow. Original: MICRO code loop (i18n label only), migration-free, **GREEN self-merge.** Runs strictly AFTER W10-04 and BEFORE W10-04b. Renames the patient-form section label `patients.contraindicationsLabel` in BOTH i18n files (`strings.*.json:985`): pt "Contraindicações NESA" -> "Contraindicações Gerais"; en "NESA contraindications" -> "General contraindications". Per the JP ruling (Q-W9-00-1 CLOSED, DECISIONS 2026-07-21): label = "Gerais"; Rodica's "Alertas" REJECTED (collides with the `red_flags` ficha field "Alertas (sinais de alarme)", `osteopathy-v4.json:78`). Presentation-only: the checkbox field keys (`patients.fieldContraindication*`) and DB columns (`contraindication_epilepsy/pregnancy/pacemaker`, migrations 0031/0034) are UNTOUCHED; no schema, no data, no model change; do NOT touch `admin.services.contraindicationSensitive`. Consumers: `patient-form.tsx:219` (legend) + `patients/[id]/page.tsx:170` (detail row), both presentational. JSON.parse both files. |
| W10-04b | catalogo-precos-reais (`docs/loops/wave-10/W10-04b-catalogo-precos-reais.md`) | DONE-EXECUTED (Phase 1 #614 merged; Phase 2 applied; owner-merge pending on this delta) | **2026-07-21 Phase 2 EXECUTED under `AUTORIZO CATALOGO plan v1`:** single atomic catalog write with read-only pre-flight (PLAN v1 pre-state matched). `UPDATE services SET is_active=true` on "Tratamento Terapêutico" (`a3c1ced1`, 1 row false->true) so the LV-offered + LV-packed service is bookable per the official LV list (Q-W10-04b-0 default). END: services 25 (active 22), slp active 23, packs 14 - all unchanged except the one flag; frozen trio still inactive (0 refs, untouched); offered-but-inactive-base now 0. No patient-domain table touched. Q-W10-04b-1/-2 keep-as-is (no rename). DECISIONS 2026-07-21. **2026-07-21 Phase 1 (read-only, catalog tables only, NO patient-domain, NO write): `docs/recon/W10-04b-catalog-delta.md` = PLAN v1.** Result: the cloud catalog is ALREADY fully reconciled to the official lists - 12 LV + 11 CB services offered at the printed cents (0 adds, 0 price corrections, 0 offered-without-printed deactivations); 8 LV + 6 CB packs at the printed per-location cents (0 pack deltas). **Per-location pack pricing = NATIVELY SUPPORTED** (`service_packs.location_id`; LV Pacote-10-NESA 39000 vs CB NESA-10 35000 = two location-scoped rows already seeded) - NOT a feature gap, no schema change. **Frozen legacy: all 3 inactive with ZERO references** (matched by id; untouched). **ONE state anomaly = the only proposed write:** "Tratamento Terapêutico" (`a3c1ced1`) is `is_active=false` but offered + packed at LV, so it is hidden from the LV booking dropdown (which filters `services.is_active`) despite being on the official LV list -> PLAN v1 step 1 = `UPDATE services SET is_active=true` (1 row), filed Q-W10-04b-0. Naming: no renames (Q-W10-04b-1 NESA triad, Q-W10-04b-2 1.ª-consulta wording, both default keep-as-is). **HALT for owner review + `AUTORIZO CATALOGO plan v1`** (Phase 2 = the single UPDATE, catalog-only). Original: CATALOG RECONCILIATION, two-phase, **OWNER-GATED CATALOG WRITE WINDOW, OWNER-MERGE.** Runs strictly AFTER W10-04c and BEFORE W10-05. Reconciles the cloud catalog (`services`, per-location `service_location_prices`, `service_packs`) to the clinic's OFFICIAL printed price lists (LV em vigor 2026-01-01 + CB em vigor 2026-03-02, embedded in the loop file, authoritative for names + prices; money in cents; durations NOT on the lists - existing `duration_min` stays, flag only a null duration). **Phase 1 read-only:** read catalog tables only (NO patient-domain), diff per location, output `docs/recon/W10-04b-catalog-delta.md` = a VERSIONED plan (adds / price corrections in cents / offered-without-printed-entry deactivation proposals / pack deltas / frozen-legacy reference count expected 0 / naming-mismatch QUESTIONS never auto-renamed), then HALT for owner review. **Phase 2 write:** ONLY after the owner replies `AUTORIZO CATALOGO plan vN` - a SECOND authorized cloud-write window, distinct from the spent `AUTORIZO LIMPEZA`, **CATALOG TABLES ONLY**; same protocol as W10-02 (read-only pre-flight against the exact plan, atomic apply, per-step before/after counts, HALT on any mismatch). **Any patient-domain table touch inside the window is an instant VIOLATION -> HALT-LOUD, regardless of authorization.** Per-location pack pricing IS natively supported (`service_packs.location_id`; LV "10 sessoes NESA" 39000 vs CB "NESA 10 sessoes" 35000 = two location-scoped rows) - no feature gap, no schema change (head stays `0037`). The 3 frozen legacy rows stay FROZEN (their future-Fisiozero-import mapping is DECISIONS 2026-07-21, NOT executed here). Cross-location pricing ruling (Q-W8-01-1 CLOSED 2026-07-21): each location keeps its own prices; a service absent from a location's printed list is NOT offered there. |
| W10-05 | agenda-cards-v3-hover (`docs/loops/wave-10/W10-05-agenda-cards-v3-hover.md`) | AT OWNER VISUAL GATE (both surfaces pushed) | **2026-07-21: implemented, migration-free, awaiting owner visual verification on BOTH surfaces.** (A) Agenda card face INVERTED - the patient name is now the FIRST + largest element (`text-sm font-semibold`, above the time row); therapist stays the spine + dot, service tint on the body, strikethrough-cancelled + suppressed-when-cancelled tick + overlap unchanged (agenda-grid.tsx). (B) ONE shared unified popup `AppointmentHoverPanel` (new `apps/web/app/agenda/appointment-hover-card.tsx`) - a structured mini-dashboard (time+duration, patient, service, therapist, location, lifecycle+confirmation as TEXT, note preview when present, created-by + created-at with the "Reserva online (portal)" label) mounted on BOTH the agenda card (replacing the inline note popover, now on EVERY card) AND the Marcacoes row via `AppointmentHoverCard` (replacing `NoteHoverCard`, which is DELETED). Reads only existing `AgendaAppointment` fields (no query/migration/axis change); `tokens.test` green (no new hex); 2 new i18n keys pt+en (`minutesAbbrev`, `detailsHoverLabel`). Staff-only (portal imports none). Tests: shared-panel unit (+5), agenda-grid hover (updated), marcacoes hover (updated), E2E both surfaces (`agenda-hover.spec.ts`). Local gates green (lint 0 err, typecheck 9/9, web unit 1236, build). **OWNER VISUAL GATE: inspect the inverted card face (agenda) + the shared popup on BOTH agenda + Marcacoes on the preview, then merge.** Original: migration-free, DISPLAY LAYER, **OWNER VISUAL GATE.** Runs after W10-04b (order W10-04 -> W10-04c -> W10-04b -> W10-05). Inverts the agenda card face so the PATIENT NAME is the first + largest element (therapist stays the coloured spine + dot, service tint stays on the body, strikethrough-cancelled + tick semantics + overlap legibility unchanged), and adds ONE shared unified rich hover popup (mini-dashboard) mounted on BOTH the agenda cards AND the Marcacoes list rows, REPLACING the W9-06 note-only hover on BOTH surfaces (the agenda inline popover `agenda-grid.tsx:542-553` AND the Marcacoes `NoteHoverCard` `marcacoes-view.tsx:226`) (owner ruling 2026-07-21): time + duration, patient name, service, therapist, location, lifecycle + confirmation state, note preview when present, created-by + created-at (portal bookings show "Reserva online (portal)"). All fields already exist on `AgendaAppointment` (no new query, no migration, no axis change). Follows `.claude/skills/osteojp-design` (structured card, AA, colour-not-only), explicitly BETTER than Fisiozero's plain black tooltip. The card-face inversion is agenda-only; the Marcacoes row face is unchanged apart from the popup. Staff-only; the portal never gains this popup. E2E on local Supabase only; no cloud QA data (post-cleanup rule). Owner inspects the inverted card face (agenda) + the shared hover popup on BOTH the agenda and the Marcacoes list, then merges |

| W10-04 | isolation-enforcement (`docs/loops/wave-10/W10-04-isolation-enforcement.md`) | THERAPIST ISOLATION DONE (migration-free); reception + RLS-tighten deferred (Q-W10-04-1); HALT for owner confirm | **2026-07-21: owner approved the matrix + ruled Q-W10-03-1..7 (DECISIONS 2026-07-21). Therapist isolation implemented migration-free, server-side-first.** "their patients" = `therapistPatientScope` (created_by UNION appointment-participation, Q2) applied to patients list/search/detail (`patients/queries.ts`), fichas list/detail/picker (`clinical/records.ts`), review queue (`clinical/review.ts`); agenda+marcacoes ignore the location param + hide the location selector for therapist (loses BOTH switchers); Faturacao denied to therapist (route + nav on `invoices:issue`). `/admin/*` already therapist-denied (admin layout `settings:read`). MANDATORY negative-isolation E2E `isolation-therapist.spec.ts` (therapist cannot list/search/open a304 = therapist2's patient, sees neither selector; admin positive control sees both). tenant_id JWT-only; NO RLS relaxed. Local gates green (typecheck 9/9, lint 0 err, web unit 1230, build). **DEFERRED to a migration-gated follow-up (Q-W10-04-1): reception location-scoping** (no reception->location model exists; migration-free can't honor Q3 - reception stays tenant-wide, status quo, NOT widened) **AND the per-therapist RLS tightening** (`0001_rls.sql:174-175` is a migration). Escalated to mailbox. **Merge posture: nominally migration-free self-merge, but the reception deferral is owner-confirmable -> GREEN HALTS for owner confirmation, does not self-merge.** Original: **IMPLEMENTATION, CONDITIONAL, SECURITY-SENSITIVE.** Runs after W10-03 merged AND the owner approved the matrix. Implements the approved matrix: server-side scoping on every listed surface (patients list/detail + fichas are NOT therapist-scoped today; agenda/marcacoes extend the existing `lockTherapist` path), capability checks, therapist-role removal of the location + therapist selectors, and MANDATORY negative isolation E2E (a therapist account cannot read another therapist's patients or another location's agenda; owner/admin cross-visibility positive control), run on local Supabase with synthetic data. **CONDITIONAL merge:** if the approved matrix needs NO schema, migration-free GREEN self-merge; if it requires schema (assignment column, role/capability column, or per-therapist RLS), migration-gated OWNER-MERGE with manual `drizzle-kit` live-apply verified before DONE (one migration `0038`). **Security-sensitive: any instinct to relax RLS or widen a policy ESCALATES instead of being self-authorized; the isolation only narrows; tenant_id stays JWT-only.** |
| W10-05 | agenda-cards-v3-hover (`docs/loops/wave-10/W10-05-agenda-cards-v3-hover.md`) | OPEN | migration-free, DISPLAY LAYER, **OWNER VISUAL GATE.** Runs after W10-04. Inverts the agenda card face so the PATIENT NAME is the first + largest element (therapist stays the coloured spine + dot, service tint stays on the body, strikethrough-cancelled + tick semantics + overlap legibility unchanged), and adds ONE unified rich hover popup on EVERY card that extends and REPLACES the W9-06 note-only hover (`agenda-grid.tsx:542-553`): time + duration, patient name, service, therapist, location, lifecycle + confirmation state, note preview when present, created-by + created-at (portal bookings show "Reserva online (portal)"). All fields already exist on `AgendaAppointment` (no new query, no migration, no axis change). Follows `.claude/skills/osteojp-design` (structured card, AA, colour-not-only), not a plain black tooltip. Staff-only; the portal never gains this popup. E2E on local Supabase only; no cloud QA data (post-cleanup rule). Owner inspects the card face + the hover on the preview, then merges |
 main

## Wave 07 candidates

> **NB (2026-07-14):** this "candidates" section is separate from the committed **Wave 07 Loop Queue (Correcoes QA)** above. These remain UNORDERED candidates, not scoped work.
>
> Added 2026-07-12 at FF2 authoring (YELLOW). UNORDERED, **candidates only - NOT committed scope**, no lane/gate/loop assignments until a Wave 07 planning pass scopes them. Sourced from the external-therapist guiao (`docs/clinical/guiao-exame-subjetivo.md`) note + the FF2 dry-run plan. Standing rules still apply when any becomes a loop: pt-PT UI copy; SYNTHETIC-DATA-ONLY; one migration in flight at a time; `db-tests.yml`/`e2e.yml` = automatic owner hold, never self-merged.

> Added 2026-07-12 at FF2 authoring (YELLOW). UNORDERED, **candidates only - NOT committed scope**, no lane/gate/loop assignments until a Wave 07 planning pass scopes them. Sourced from the external-therapist guiao (`docs/clinical/guiao-exame-subjetivo.md`) note + the FF2 dry-run plan. Standing rules still apply when any becomes a loop: pt-PT UI copy; SYNTHETIC-DATA-ONLY; one migration in flight at a time; `db-tests.yml`/`e2e.yml` = automatic owner hold, never self-merged.

| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| Tablet pre-consultation intake questionnaire (patient portal) | guiao note (`docs/clinical/guiao-exame-subjetivo.md`) | a tablet questionnaire the utente fills BEFORE the consult, per the guiao, so the AI can start the analysis and shorten the subjective/objective/diagnostic exam. **GATED on a JP/Rodica clinical spec** (which questions, flow, portal placement). Portal V2 dependency |
| New assessment sections Sono, Alimentacao, Sistema Visual | guiao note (`docs/clinical/guiao-exame-subjetivo.md`) | dedicated evaluation sections for SONO, ALIMENTACAO, and SISTEMA VISUAL in the ficha. **GATED on a JP/Rodica clinical spec** (fields, whether they extend the template + a v-bump, AI-key implications) |
| AI extraction prompt restructuring around the guiao + contract v2/v3 key extension | FF2 dry-run plan | restructure the AI extraction prompt around the guiao and extend the ingestion contract with v2/v3 keys. **GATED on Andre's effort estimate at the dry run** (the twelve-key contract is frozen today; any key extension is an Andre-side + owner-confirmable change) |

### FF2 close follow-ups (recorded, not scoped)

> Added 2026-07-14 at FF2 close (YELLOW). Two low-risk follow-ups surfaced during FF2 execution. UNORDERED, candidates only, no lane/gate/loop until a planning pass scopes them. Standing rules apply when either becomes a loop.

| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| Guiao en-GB translation | W5-34 execution (DECISIONS 2026-07-13) | the in-app "Guiao do Exame Subjetivo" panel shipped pt-only (`guiao-content.ts` typed static data); the en-GB translation was deferred. Small i18n/content follow-up: add the en strings to the typed guiao data and gate the panel language on the app locale. Migration-free, no vendor |
| Portal /auth/activate build-time env fallback (local full-build parity) | FF2 build observation | the portal `/auth/activate` route needs its activation env present at build time; a local `pnpm build` of the full monorepo diverges from CI/preview when the var is absent. Add a build-time env fallback so a local full build matches preview parity without live secrets. Infra/build-config only, no product change |

## Wave 10 candidates

> Opened 2026-07-16 at Wave 09 authoring, **consolidated 2026-07-18 at Wave 09 close (YELLOW)** into a single de-duplicated list. UNORDERED, **candidates only - NOT committed scope**, no lane/gate/loop assignments until a Wave 10 planning pass scopes them (owner + Rodica + JP). The **headline candidate** is the schedule-model redesign carried out of the Castelo Branco QA triage (item 11, `docs/qa/2026-07-16-castelo-branco-qa.md`): the largest item CB raised, EXCLUDED from Wave 09 because it needs a spec before any build. Sources: the CB QA triage, three candidates registered by Wave 09 execution rulings (W9-04 option C, W9-05 whole-card fallback, W9-07 per-location filter), the W8-04 portal audit, and the open QUESTIONS. **The seven individual portal rows from the authoring register are folded into the single "W8-04 portal findings list" entry below - the audit doc is the detail, this is the pointer.** Standing rules still apply when any becomes a loop: pt-PT UI copy (both i18n files, JSON.parse both); SYNTHETIC-DATA-ONLY for verify; disposable test patients only, never Maria Joao Silva, reference therapist Tiago Reis; ONE migration lane in flight at a time; the cloud DB is READ-ONLY absent an explicit per-write owner authorization; `db-tests.yml`/`e2e.yml` = automatic owner hold, never self-merged.

| Candidate | Origin | Gate / note |
|-----------|--------|-------------|
| **HEADLINE - Schedule model redesign: variable per-day/per-week therapist hours** | CB QA item 11 (`docs/qa/2026-07-16-castelo-branco-qa.md`), owner triage 2026-07-16 | therapists work variable hours that differ **per day and per week**; the current model is ONE fixed weekly pattern (`availability_templates`). **SPEC REQUIRED FIRST - do not build from this row.** Touches the booking source of truth (`listOpenSlots` expands active templates into the 30-min grid, `apps/api/lib/appointments/store.ts`), so it is a model + query + admin-UI change, almost certainly migration-bearing. Excluded from Wave 09 deliberately. Largest item CB raised |
| **Booking dropdown per-location offering filter** - GATED, do not build before the gate clears | W9-01 finding (a) (`docs/recon/W9-01-findings.md`, #595); owner ruling 2026-07-17 (W9-07 close) | **GATE: the open JP catalog-gaps ruling (`docs/QUESTIONS.md:546-571`) must land FIRST.** Real defect, confirmed: the staff booking service dropdown filters `services.is_active` ONLY and has NO location predicate (`apps/web/lib/scheduling/data.ts:200-209`), so it offers every active service at every location - contradicting the W8-01a offered-only-where-priced semantic, and inconsistent with packs in the SAME drawer, which ARE location-filtered (`appointment-drawer.tsx:345-347`). At CB it also offers the LV-only "Tratamento NESA". **Deliberately NOT fixed in Wave 09 (owner ruling): tightening this filter today would HIDE services CB actually performs but which lack price rows, since the JP batch defaults to "leave unseeded" for both the CB-missing and LV-missing service sets. That converts a cosmetic over-inclusion into a real booking outage.** Sequence: JP rules the catalog gaps -> price rows land -> then enforce. Hazard for whoever builds it: the legacy and canonical NESA rows share the EXACT display name "NESA" and differ only by id/price/active flag - match by id, never by name |
| **Blocked-time summary marker for the all-therapists agenda view** (W9-04 option C) | W9-04 scope question (`~/osteojp-mailbox/inbox/W9-04-SCOPE-blocked-band-therapist-axis-2026-07-17.md`); owner ruling 2026-07-17 item 1 | **Registered by owner ruling when option (A) was accepted for Wave 09.** W9-04 draws blocked-time bands ONLY in single-therapist views: `time_off` is per therapist but the agenda grid has DAY columns and no therapist axis (W9-01 (f)), so a full-width band under `Todos os terapeutas` would claim the whole clinic is blocked when one therapist is away, suppressing real bookable time. Under `Todos os terapeutas` blocked time is therefore invisible. **Accepted as VISIBILITY-only: the booking drawer already refuses a blocked slot (W5-12), so safety is intact.** Option (C) is a compact non-interactive summary marker on the day column (e.g. "2 terapeutas indisponiveis", expanding on hover). NEW UI in no existing spec, so it needs a design call + both i18n files. Option (B) (per-therapist strips reusing the overlap layout) was DECLINED - truthful but noisy at 16 active therapists |
| **Whole-card per-therapist colouring** - FALLBACK only if CB staff QA rejects the stripe | W9-05 design ruling at the owner visual gate 2026-07-17 (#600) | **Registered as a fallback, not a queued build.** W9-05 shipped therapist identity as a left stripe + dot + name, keeping the card body service-tinted (SPEC-v2-agenda 2.1). If CB staff QA finds the stripe too subtle to tell therapists apart at a glance, the fallback is to colour the WHOLE card per therapist instead. **Trade-off to weigh if it is built: whole-card colouring collides with the service-tint that currently carries service identity, and with the cancelled/blocked visual states, so it is not a free swap - it re-opens the card colour system.** Only build after a CB QA rejection of the stripe; otherwise leave the shipped stripe design in place. Reuses the deterministic per-therapist `-700` tokens already added in W9-05 (`lib/scheduling/therapist-color.ts`, `tokens.test.ts` green) |
| `reminder_sends` table (unblocks the SMS-evolution KPI) | Q-W8-03-1 (QUESTIONS, opened 2026-07-15) | the platform persists the per-patient reminder PREFERENCE (`patients.reminder_sms_enabled`, `0019`) but NO record of reminder/SMS SENDS, so "Evolucao do envio de SMS" was excluded from W8-03's ten reports. Add a send log written at dispatch, then the report becomes buildable. Migration-bearing; owner-confirmable per Q-W8-03-1 |
| **W8-04 portal findings list** (seven findings, consolidated) | W8-04 portal audit (`docs/status/2026-07-16-portal-completion-audit.md`), DECISIONS 2026-07-16 | the audit doc is the detail; this row is the single pointer that replaces the seven separate portal rows. The findings: **(1) enforce the 0023 service->therapist booking filter** - therapist visibility is HALF-enforced (`availabilityCoversExists` runs in `listOpenSlots`, the `therapist_services` map from migration 0023 does NOT), so a therapist with hours and no service map still surfaces. **W9-01's live read: `therapist_services` holds only 3 rows total (Osteopatia x2, Fisioterapia x1) across 18 active therapists - enforcing it against today's data would hide almost every therapist, so DATA POPULATION precedes enforcement.** (2) portal env-validation + a `NEXT_PUBLIC_API_URL` Preview fallback (absent on Preview, `apiBase()` returns "" and calls hit `//api/v1/...` and fail; `apps/portal/lib/api/client.ts`, `account/actions.ts`; relates to the FF2 `/auth/activate` build-time env follow-up). (3) pending-forms surface (the dashboard banner is omitted; the API exposes no not-yet-submitted forms data). (4) per-patient locale switcher (portal is PT-only at runtime, `account/AccountView.tsx`). (5) data-driven clinics roster (`/portal/clinics` is a STATIC hardcoded roster). (6) offline / error resilience (PWA v1 #568 is installability-only by design). (7) shared portal auth guard / middleware. None scoped; a Wave 10 planning pass picks which to take |
| Packs em uso view | Rodica ask, carried at Wave 08 close (DECISIONS 2026-07-16) | a view of packs currently in use. The pack model shipped in W8-01a (`0037`: pack definitions + per-patient instances). Pack no-show/under-24h enforcement is MANUAL by ruling; the platform never auto-charges |
| Recuperacao Utentes report | Rodica ask, carried at Wave 08 close (DECISIONS 2026-07-16) | a patient-recovery/win-back report. Not scoped; needs Rodica to define the cohort and the window |
| Orphaned Supabase auth logins on staff delete | Q-W7-01-2 (QUESTIONS, opened 2026-07-14) | `deleteStaffMember` deletes only the `public.users` row and never `admin.auth.admin.deleteUser`, so a deleted staff email leaves an orphaned platform-wide auth identity and re-invite fails. W7-01 made it surface a clear message but deliberately did NOT change the orphan's fate (destructive + product). Three options recorded; recommended default is **(2) adopt on re-invite**, gated on no active `users` row in ANY tenant, with an audit row. **Owner ruling needed before this becomes a loop** |
| PWA polish v2 - GATED on Max reference videos | owner-directed (Wave 09 briefing 2026-07-16, re-affirmed with the gate at Wave 09 close 2026-07-18) | owner-directed candidate; PWA v1 shipped installability-only (#568, `apps/portal/app/manifest.ts`). **No standalone in-repo spec doc yet:** the "polish v2" scope is defined by Max's reference videos, which are the gate - a Wave 10 planning pass captures what v2 covers from those videos before scoping. Not blocked; simply unscoped until the reference material is in hand |
| CI E2E sharding - needs branch-protection edits | owner-directed (Wave 09 briefing 2026-07-16, re-affirmed with the gate at Wave 09 close 2026-07-18) | owner-directed throughput candidate: shard the required Playwright E2E lane to cut wall-clock. **Gate: it needs branch-protection edits** (the sharded jobs become the new required checks, replacing the single `e2e.yml` check), which is an owner/admin action on the GitHub repo settings, not a code-only change. Distinct from the Wave 05 "Cross-browser E2E infra hardening" candidate, which is the chronically-red NON-required cross-browser lane. No in-repo spec doc yet; scope in the Wave 10 planning pass |
| Drizzle journal reconciliation - HIGHEST RISK | owner-directed (Wave 09 briefing 2026-07-16, re-affirmed at Wave 09 close 2026-07-18 as highest-risk) | owner-directed, flagged **highest-risk**. **Gate: owner green-light PLUS a dedicated, verified prompt only - never folded into a normal wave loop.** `packages/db/migrations/` is authoritative, `supabase/migrations/` must be an exact copy, and the `journal-sync` Vitest test already guards `packages/db/migrations/meta/_journal.json` (`docs/runbook-prod-migrations.md`); no committed doc records a journal DIVERGENCE needing reconciliation, so the FIRST step is to capture the actual defect. A journal rewrite touches migration history and is destructive-adjacent: owner-confirmable, never self-decided, and quarantined to its own prompt |
