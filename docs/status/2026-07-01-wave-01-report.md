# Wave 01 Status Report — 2026-07-01

> Source of truth: `gh pr list --state merged --search "merged:2026-07-01" --json number,title,mergedAt --limit 30`, run against this repo, plus direct inspection of the resulting PRs/commits/files. Every PR number below was checked to exist and to be merged before being cited; a few claims from the original brief for this report turned out to be stale or partly inaccurate against that check — noted inline rather than silently corrected.

## Merged today

Chronological, 23 PRs:

| Time (UTC) | PR | Title | What it did |
|---|---|---|---|
| 13:30 | #384 | feat(nav): rename Revisao to Revisao Consulta in nav and page title | Renamed the nav item/page title (Max queue item 2, zero-dep). |
| 13:56 | #383 | chore(dashboard): fix stale comment — quick_notes saves to public.quick_notes not tenants.settings.notes | Comment-only fix on the Início dashboard page. |
| 14:08 | #385 | fix(dashboard): remove Proximas marcacoes card from Inicio | Removed the card (Max queue item 1, zero-dep). |
| 14:30 | #386 | feat(i18n): extract 13 hardcoded PT strings into i18n string files | Routed 13 previously-hardcoded strings (layout metadata, SidebarAppShell/DatePicker/Toast defaults) through i18n keys. |
| 14:54 | #388 | docs: add staff FAQ (PT-PT) docs/staff-faq.md | Added a 15-question staff FAQ. |
| 14:56 | #387 | docs: add plain-English test scenarios (docs/test-scenarios.md) | Created `docs/test-scenarios.md` (new file, 286 additions). |
| 15:10 | #390 | fix(i18n): replace Red Flags anglicism with PT-PT clinical term | Renamed "Red Flags" → "Sinais de Alarme" in two PT keys. |
| 15:22 | #392 | fix(i18n): standardize PT-PT string capitalization per brand-voice rule | Fixed 3 documented capitalization inconsistencies (episode label, row-action verb, review title) + matching EN casing-only fixes. |
| 15:31 | #382 | feat(db): migration 0022 — patients.profession + region (columns-only) | Added `profession`/`region` text columns to `patients`. Had a mid-flight DB-gated RLS test failure — see Flags §3. |
| 15:35 | #389 | docs: rewrite in-app help text source of truth (Screen/Element format) | Restructured the in-app help-text reference doc. |
| 15:43 | #391 | fix(i18n): standardize clinical record terminology (registo not ficha) and fix consulta/marcação mixed-term violations | Renamed the primary nav/title/action strings from "ficha" to "registo clínico"; **incomplete** — ~15 `clinical.*` keys still say "ficha" (see `docs/design/DECISIONS.md`, "Clinical record terminology" entry, and PR #395). |
| 15:47 | #394 | docs: flush 0022 DONE, persist loop 0023 board file | Updated `docs/design/BACKLOG.md` to mark 0022 DONE / 0023 committed. |
| 16:00 | #393 | feat(patients): surface profession, city, region, patient notes on profile | Surfaced the four patient-profile fields (Max queue item 3, partial — patient ID still not surfaced). |
| 16:17 | #395 | docs: update migration-notes, DECISIONS, and verify open owner questions after 2026-07-01 session | Logged the sex-normalization + i18n-path notes, corrected the clinical-terminology claim, confirmed the two JP questions still open. |
| 16:36 | #397 | docs: add plain-English test scenarios docs/test-scenarios.md | Substantial rewrite of the same file #387 created (222 additions / 192 deletions), not a duplicate. |
| 17:02 | #396 | feat(scheduling): read-only therapist availability query (booked vs free, migration-free) | Non-migration Ivan item; clears the "availability query DONE" gate for Max item 6. |
| 17:39 | #399 | docs: flag dead i18n keys and Bodychart term decision in QUESTIONS.md | Logged the two dead keys and the Bodychart term question (this report's Flags §2 and Open JP questions §3 draw on it). |
| 17:45 | #398 | feat(db): migration 0023 — therapist-service mapping (greenfield) | Tenant-scoped join table mapping therapists to the services they deliver. Clears the 0023 gate for Max item 5. |
| 17:57 | #400 | fix(a11y): add explicit role=dialog and aria-modal to Drawer primitive | Accessibility fix on `packages/ui/src/components/Drawer.tsx`. |
| 18:04 | #402 | docs: persist loops 0024 and 0025 as committed board files | Loop-board bookkeeping for 0024/0025 (BACKLOG.md itself was not re-touched after this — see note below the Queue status table). |
| 18:26 | #403 | feat(db): migration 0024 — appointment confirmation state | Adds a confirmation axis (pending/confirmed/declined) to `appointments`, orthogonal to lifecycle `status`. Clears the 0024 gate for Max item 4. |
| 18:44 | #404 | feat(db): migration 0025 — analytics_events KPI/event feed | Greenfield append-only event log for KPI capture (SPEC-events.md). |
| 19:57 | #401 | fix(a11y): add visually-hidden title elements to DatePicker popover and mobile nav dialog | Accessibility fix on `packages/ui/src/components/DatePicker.tsx` and `SidebarAppShell.tsx`. |

## Queue status

Numbering follows the original Max queue in `docs/design/WAVE-01.md` (items 1–9), plus the ad-hoc zero-dep item added later to `docs/design/BACKLOG.md`'s UI lane (item 10, not in the original WAVE-01 list).

- **Item 1 — Remove "Proximas marcacoes" card:** done (#385).
- **Item 2 — Rename Revisao → Revisao Consulta:** done (#384).
- **Item 10 — Stale comment fix at `page.tsx:360`:** done (#383). Zero-dep, added to the BACKLOG.md UI lane after the original WAVE-01 numbering was written.
- **Item 3 — Patient-profile fields surfacing:** done for profession/city/region/notes (#393, migration 0022 / #382 landed first). **Not fully done** — patient ID next to NIF is explicitly deferred, still blocked on JP's ID-format ruling (see Open JP questions).
- **Items 4–9 — gated, with named upstream dependency:**
  - Item 4 (confirmation thumbs on appointment preview) — gated on migration 0024. **Gate cleared today** (#403 merged 18:26); UI work itself not yet started.
  - Item 5 (auto-select service from therapist) — gated on migration 0023. **Gate cleared today** (#398 merged 17:45); UI work not yet started.
  - Item 6 (availability panel in new-appointment flow) — gated on the availability query. **Gate cleared today** (#396 merged 17:02); panel UI not yet started.
  - Item 7 (Fichas-as-tab inside patient profile) — gated on migration 0026 (gated-completion design) AND JP's gated-completion ruling. **Still blocked** — 0026 isn't drafted yet and the JP ruling is still open (see Open JP questions).
  - Item 8 (schedule-again action) — gated on the appointment-clone endpoint. **Still blocked** — that endpoint is still `DRAFT` in BACKLOG.md; no PR for it merged today.
  - Item 9 (batch failure pop-up) — gated on migration 0028 (batch scheduling engine), which itself needs migrations 0026 and 0027 first. **Still blocked**, furthest out.

Note: `docs/design/BACKLOG.md`'s MIG-lane table was last edited by #394 (15:47) and has not been updated since — it still shows 0023 as READY and 0024/0025 as DRAFT, even though all three merged later today (#398, #403, #404). The table above reflects the actual merge state, not the board file, which needs a refresh.

## Flags for Ivan

1. **`packages/ui/src/components/AppShell.tsx` (exports `StaffAppShell`) is dead code.** Zero live callers in `apps/`, confirmed both by grepping `apps/` for `StaffAppShell` (no hits) and independently by PR #386's own "Out of scope" note ("`StaffAppShell`... has no call site in `apps/web` (Storybook-only)"). The app actually uses `SidebarAppShell` (used in `apps/web/components/app-shell.tsx`, `staff-shell.client.tsx`, and several page routes). Removal is Ivan's call (`packages/ui`).
2. **Dead i18n keys `dashboard.upcomingToday` and `intake.state.pendingReview`** — documented in `docs/QUESTIONS.md` (added via #399), zero references anywhere in the repo for either key. Need Ivan's scope confirmation (non-web consumers: email templates, API responses, other apps) before deletion.
3. **Migration 0022 (PR #382) had a DB-gated RLS test failure mid-flight before it merged.** Root cause is already documented in `docs/QUESTIONS.md` ("2026-07-01 - RESOLVED: 0022 dev-applied; root cause was env-file resolution"): `drizzle-kit migrate` runs with `cwd=packages/db` and only reads `packages/db/.env` (not the repo-root `.env`/`.env.local`), so the rotated dev DB password was never in a file the tooling actually read. Fix: dev DB creds now live in `packages/db/.env`. Worth Ivan confirming every other local checkout has picked up that convention.
4. **`packages/db/migrations/meta/_journal.json` reconciliation — flagged earlier today, appears resolved now, not still pending.** The concern (from `docs/QUESTIONS.md`, "Availability query: DoD live-seed gap + dirty working tree") was that PR #396 was cut from a working tree with uncommitted changes to `_journal.json` from an in-flight 0023 loop. Checked against current `main`: `_journal.json` now has 26 entries ending in `0022_patient_profession_region` → `0025_event_schema`, exactly matching the four `.sql` files on disk, and `node scripts/sync-supabase-migrations.mjs --check` reports `supabase/migrations` byte-identical (26 files). This looks resolved by the subsequent 0023/0024/0025 merges — flagging for Ivan to confirm no other local checkout still carries that original uncommitted diff, rather than treating it as an open item.

## Open JP questions

All three confirmed still open (`- [ ]`, unresolved) in `docs/QUESTIONS.md` / `docs/design/QUESTIONS.md` as of the latest commit on `main`:

- **Patient ID format (JP)** — sequential, prefixed, or per-tenant scoped. Blocks patient ID surfacing (the remaining half of Item 3).
- **Gated completion: hard block or soft warning (JP, clinical)** — blocks Item 7 (Fichas-as-tab) and migration 0026.
- **"Bodychart" term: brand name or translate (JP or Ivan)** — blocks touching `clinical.bodychart` / `clinicalRecord.bodychart`.

## Audits completed, all clean or resolved

- **Accessibility:** staff platform clean. Drawer (#400), DatePicker, and `SidebarAppShell` (#401) defensive fixes merged today (`role=dialog`/`aria-modal` on Drawer; visually-hidden titles on the DatePicker popover and the mobile nav dialog).
- **i18n:** terminology (#391), capitalization (#392), and anglicisms (#390) fixed today. Key parity verified: **690/690** — `strings.pt.json` and `strings.en.json` have identical key sets as of the latest commit (the figure quoted in the original brief, 688/688, is stale; the count has moved since whatever earlier audit produced it, most likely `#386`'s 13-key extraction plus the terminology/casing PRs). Spot-checked `apps/web/components` for remaining hardcoded PT-looking string literals outside `s[...]` calls — none found, consistent with "clean," though this was a targeted grep, not an exhaustive audit.
- **Storybook and portal audits in progress:** local worktrees exist for `docs/ui-storybook-status` and `feat/portal-i18n-runtime`, neither yet merged or opened as a PR as of this report — consistent with "in progress," not independently verified beyond confirming the branches exist.
