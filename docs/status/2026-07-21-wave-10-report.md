# Wave 10 - Dados Reais e Isolamento - close-out report

**Closed 2026-07-21.** Scope (owner brief 2026-07-20, the clinic starts real usage): (1) purge all synthetic patient-domain data from the cloud DB; (2) lock the platform down per-person (a therapist sees only themselves, their own patients, their own location); (3) agenda cards show the patient name first with a rich hover popup. Delivered in eight loops, PR range #606-#616, **zero migrations, migration head stayed `0037`**.

## Loop / PR / outcome

| Loop | PR | Merge class | Outcome |
|------|----|-----|---------|
| W10-01 cleanup-recon | #606 | GREEN self-merge (docs, read-only) | Read-only cloud inventory (SET TRANSACTION READ ONLY, no PII). Boundary confirmed 100% synthetic. PLAN v1->v2 (added a soft-delete step for the 3 live blocked patients). Owner rulings Q-W10-01-1 CONFIRMED, Q-W10-01-2 Option A. |
| W10-02 cleanup-execution | #608 | OWNER-MERGE, owner-gated write window | Purge EXECUTED under `AUTORIZO LIMPEZA plan v4` (see drift note). 46 synthetic patients + subtree deleted; 3 live blocked patients soft-deleted. Standing "cloud = real data only" rule in force. |
| W10-02b correcoes-mobile-cb | #609 | OWNER VISUAL GATE (mobile) | Three CB phone defects fixed: bodychart per-character wrap, red-flag surfacing (Q-W10-02b-1 option A), signature touch (pointer-capture best-effort + onPointerCancel). |
| W10-03 access-isolation-spec | #610 | OWNER-MERGE, matrix-approval halt | `docs/design/SPEC-isolation.md` - role model, 13x4 capability/visibility matrix, server-side-first enforcement plan, open questions Q-W10-03-1..7. Owner approved the matrix + ruled all seven. |
| W10-04 isolation-enforcement | #611 | OWNER-MERGE (reception deferral) | Therapist isolation, migration-free: "their patients" = created_by UNION appointment-participation; scoped patients/fichas/review; both agenda switchers gone; Faturacao denied; mandatory negative-isolation E2E. Reception + RLS-tighten deferred (Q-W10-04-1). |
| W10-04c label-contraindicacoes-gerais | #613 | GREEN self-merge (i18n) | Patient-form section label "Contraindicacoes NESA" -> "Gerais" (JP ruling Q-W9-00-1); both i18n files, one key, no schema. |
| W10-04b catalogo-precos-reais | #614 (Phase 1) + #615 (Phase 2) | OWNER-MERGE, owner-gated catalog write | Catalog already matched the official LV+CB lists (12+11 services, 8+6 packs, all correct). Per-location pack pricing confirmed natively supported. One fix under `AUTORIZO CATALOGO plan v1`: `UPDATE services SET is_active=true` on "Tratamento Terapêutico" (LV-listed but hidden from booking). Frozen trio untouched (0 refs). |
| W10-05 agenda-cards-v3-hover | #616 | OWNER VISUAL GATE (both surfaces) | Agenda card face inverted (patient name first + largest); ONE shared unified hover popup (mini-dashboard) on both the agenda card and the Marcacoes row, replacing the note-only hover on both; reads only existing fields, no migration. |

## Cleanup evidence summary (W10-02, the purge)

- **Authorization:** `AUTORIZO LIMPEZA plan v4` (exact phrase + version); the single patient-domain cloud-write window of the wave.
- **Purged (D = 46 deletable synthetic patients, children-first, one atomic transaction):** 46 patients, 19 appointments, 12 draft clinical_records, 53 clinical_episodes, 3 patient_pack_instances, 2 patient_note_revisions, 2 ai_ingestion_requests, 1 attachment. Then soft-deleted the 3 live immutability-blocked patients (patients-row UPDATE only).
- **End state:** patients 4 (all soft-deleted, 0 live), appointments 5, clinical_records 26 (5 signed + 21 draft), episodes 3, attachments 5, note_revisions 3, pack_instances/ai_ingestion/annulments 0.
- **Retained real data UNCHANGED:** users 19, services 25, service_location_prices 23, service_packs 14, locations 2 (CB+LV), tenants 1.
- **Immutability:** the `clinical_records_enforce_immutability` trigger stayed enabled (`tgenabled='O'`) throughout; the 4-patient / 5-signed-record island is the owner-accepted BLOCKED residue (Option A). Evidence: `docs/recon/W10-02-execution-evidence.md`.

## Notes / lessons

- **Drift discipline held under live QA.** During the W10-02 authorization window, Rodica's mobile QA kept creating synthetic rows on the live cloud DB (15:02-16:33 UTC). The mandatory read-only pre-flight caught every drift and HALTed with ZERO writes each time, re-versioning the plan v1->v2->v3->v4 until the DB was quiescent and the owner re-confirmed scope. No purge ever ran on a stale or ambiguous snapshot; two mid-execution rollbacks (the 16:33 straggler; a wrong end-state assertion) also wrote nothing.
- **Two on-branch CI-red fixes, both from the same class** (a new UI element duplicating on-page text): W10-04 updated the obsolete "therapist can access /invoicing" test to assert denial; W10-05 scoped a `getByText("11:00-12:00")` to the card because the new hover popup also prints the time. The heavy `therapist-blocks.spec.ts` flaked environmentally on the first run of both loops and passed clean on the re-run each time.
- **Board hygiene:** the committed-queue table accumulated git union-merge artifacts (stray branch-name lines, duplicate stale rows) across the parallel loop-PR merges; rebuilt cleanly at close.

## Open questions carried

- **Q-W10-04-1 (migration-gated follow-up):** reception location-scoping (Q-W10-03-3) + the per-therapist `clinical_records` RLS tightening (`0001_rls.sql:174-175`) both need a reception->location data model + an RLS policy change (net-new schema `0038`). Deferred to one owner-merge migration-gated loop with live-apply. Reception stays tenant-wide until then (status quo, not widened).
- **Q-W10-03-1 note (JP):** admin currently holds `clinical_records:read` tenant-wide; JP may later choose to restrict admin clinical read. Unchanged for now.

## Candidates touched

- **W8-04 portal findings (7 items)** - not taken this wave; still an unscoped candidate on the board.
- **Schedule-model redesign (CB QA item 11)** - headline candidate, still gated on a spec; not taken this wave.
- The "0023 service->therapist booking filter" and "per-location booking-offering filter" candidates remain gated on the JP catalog-gaps ruling; W10-04b did NOT fold them in. The catalog-gaps ruling itself (Q-W8-01-1) was CLOSED 2026-07-21 (per-location printed lists authoritative), which W10-04b consumed for the reconciliation.

No work was invented beyond the eight committed loops.
