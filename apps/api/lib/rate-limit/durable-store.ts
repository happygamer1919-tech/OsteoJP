/**
 * MOVED to `@osteojp/rate-limit` (SEC-r-token-no-rate-limit, 2026-08-13).
 * Re-export shim; see ./limiter.ts for why the shim exists rather than a move
 * of every import.
 */
export * from "@osteojp/rate-limit";
